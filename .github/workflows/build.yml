name: Build

# CI for Outpost Commander (AGENTS.md §6).
#
# ── What blocks, and what is merely skipped ────────────────────────────────────────────────
#
# Every step that has something to run BLOCKS. Nothing here is `continue-on-error`, and a script
# that exists and fails fails the build.
#
# But this repository is a clean start: there is no solution, no project and no checker script in
# it yet, and a gate that cannot run must not be dressed up as a gate that passed. So each step is
# guarded on the file it needs -- the checker script, the solution, the built test binaries -- and
# a step whose input is absent is SKIPPED and says so in the log. The moment the file lands, the
# step starts gating with no edit here. Remove a guard once its input is permanently there; never
# add one to get past a red build.
#
# Nothing is discovered by name. The solution is whatever single .slnx/.sln sits at the root, and
# the test suites are whatever *Tests.vcxproj the tree contains -- so adding a project does not
# mean remembering to edit this file.
#
# ── What this builds, and what it deliberately does not ────────────────────────────────────
#
# DEBUG ONLY. The Windows build is the slow half of the pipeline and a second configuration
# roughly doubles it, for a tree where Debug and Release differ in exactly four things:
# Optimization, _DEBUG vs NDEBUG, FunctionLevelLinking/IntrinsicFunctions, and the linker's
# folding and LTCG switches. What stands in for the missing build is the "Check the build shape"
# step below: the checker fails when Debug and Release drift apart on any setting that is not
# about optimisation, so the Release nobody compiles here still cannot quietly lose an include
# directory or sit on an older language standard. Before a release, somebody builds Release for
# real -- see AGENTS.md §3.
#
# X64 ONLY. There is no second platform to fall back on and nothing left to excuse a red x64.
#
# ── Why the toolchain is not pinned to an image ────────────────────────────────────────────
#
# PlatformToolset is pinned in the project files. The runner image is not: it is whatever GitHub
# ships, and a wrong image pin trades a floating toolchain for a build that does not run at all.
# Instead the first step of each job PRINTS what it got -- image, VS, MSBuild, clang-tidy,
# clang-format -- so a pin can be taken from evidence the day one is wanted, and so "it broke and
# nothing changed" has somewhere to look.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  windows:
    name: Debug|x64
    runs-on: windows-latest
    timeout-minutes: 30

    env:
      CONFIGURATION: Debug
      PLATFORM: x64

      # ── Why clang-tidy is pinned and the rest of the toolchain is not ──────────────────────
      #
      # The clang-format job below pins clang-format 18 and says why: 18 and 22 break a long
      # argument list in different places, so a floating version would flap. clang-tidy floated in
      # the sibling tree and did exactly that -- the runner image moved to LLVM 20.1.8 and the
      # build went red on code that had not changed, with two findings that were correct code:
      #
      #   clang-analyzer-optin.core.EnumCastOutOfRange on a BIT-FLAG enum, where an OR-ed pair of
      #   enumerators names no enumerator of its own. Deliberate, and not reported by 22.
      #
      #   clang-analyzer-cplusplus.NewDeleteLeaks on a std::unique_ptr inside an aggregate
      #   returned by value. Correct C++, and not reported by 22 either.
      #
      # A third finding from the same run was REAL -- an enum with no zero enumerator that the
      # decoder cast one into -- and it was fixed in the code rather than pinned away. That is the
      # distinction this pin is drawn on: a version change must not be able to turn main red on
      # its own, and the way to bump it is to change the number here, run it, and fix what it says.
      #
      # From pip rather than from the image, because that is the only way a developer can run the
      # same binary the gate runs. `python -m pip install clang-tidy==<this>` gets it on any
      # machine; the runner's bundled LLVM cannot be asked for by version at all.
      CLANG_TIDY_VERSION: 22.1.8

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v2

      # INCLUDE and LIB are what make the CRT and the Windows SDK visible to clang-tidy, which
      # runs through clang's MSVC driver rather than being told about the SDK by hand. MSBuild
      # does not need them -- it resolves the toolset itself -- so this exists for the linter.
      - name: Import the MSVC environment
        shell: pwsh
        run: |
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $install = & $vswhere -latest -products * -property installationPath
          if (-not $install) { throw "vswhere found no Visual Studio installation." }
          $devcmd = Join-Path $install 'Common7\Tools\VsDevCmd.bat'
          cmd /c "`"$devcmd`" -arch=amd64 -host_arch=amd64 -no_logo && set" | ForEach-Object {
            if ($_ -match '^(INCLUDE|LIB|LIBPATH|VCINSTALLDIR|VCToolsInstallDir|VSINSTALLDIR)=(.*)$') {
              "$($Matches[1])=$($Matches[2])" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
            }
          }
          "VSWHERE_INSTALL=$install" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      # PINNED, and not taken from the runner image. CLANG_TIDY_VERSION at the top of this job
      # carries the version and the reason.
      - name: Install the pinned clang-tidy
        shell: pwsh
        run: python -m pip install --disable-pip-version-check "clang-tidy==$env:CLANG_TIDY_VERSION"

      - name: Record the toolchain
        shell: pwsh
        run: |
          Write-Output "runner image : $env:ImageOS / $env:ImageVersion"
          Write-Output "visual studio: $env:VSWHERE_INSTALL"
          msbuild -version -nologo
          Write-Output "--- clang-tidy (pinned to $env:CLANG_TIDY_VERSION) ---"
          clang-tidy --version
          Write-Output "--- python ---"
          python --version

      # The solution is found, not named. Exactly one at the root, or none while the tree is still
      # empty; two is a mistake worth failing on rather than picking between.
      - name: Locate the solution
        id: solution
        shell: pwsh
        run: |
          $found = @(Get-ChildItem -Path . -File | Where-Object { $_.Extension -in '.slnx', '.sln' })
          if ($found.Count -gt 1) { throw "More than one solution at the repository root: $($found.Name -join ', ')." }
          if ($found.Count -eq 0) {
            Write-Output "::notice::No solution at the repository root yet. The build and test steps will be skipped."
            "path=" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          }
          else {
            Write-Output "Solution: $($found[0].Name)"
            "path=$($found[0].Name)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          }

      # First, because it is two seconds and it catches the class of mistake that would otherwise
      # surface as a confusing build failure: a file missing from a project, a header in a
      # subdirectory clang-tidy will never see, or Debug and Release drifting apart.
      - name: Check the build shape and the project registries
        if: hashFiles('Build/CheckProjectFiles.py') != ''
        shell: pwsh
        run: python Build\CheckProjectFiles.py

      # /warnaserror covers MSBuild's own warnings; /WX for the compiler is set in the projects.
      - name: Build
        if: steps.solution.outputs.path != ''
        shell: pwsh
        run: |
          msbuild "${{ steps.solution.outputs.path }}" `
            /p:Configuration=$env:CONFIGURATION /p:Platform=$env:PLATFORM `
            /m /nologo /v:minimal /warnaserror `
            /flp:LogFile=build.log`;Verbosity=normal

      # One suite per *Tests.vcxproj in the tree (outside Build/Fixtures, where the project-file
      # checker keeps deliberately broken projects for its self-test), and every one of them must
      # have produced a DLL:
      # vstest reports "no tests found" as a pass, so a suite that did not build is not a suite
      # that passed. No suites at all is a state this repository is still legitimately in.
      - name: Run the tests
        if: steps.solution.outputs.path != ''
        shell: pwsh
        run: |
          $projects = @(Get-ChildItem -Path . -Recurse -Filter '*Tests.vcxproj' -File |
            Where-Object { $_.FullName -notmatch '[\\/]Build[\\/]Fixtures[\\/]' })
          if ($projects.Count -eq 0) {
            Write-Output "::notice::No *Tests.vcxproj in the tree yet. Nothing to run."
            exit 0
          }
          $suites = foreach ($project in $projects) {
            $dll = "$env:PLATFORM\$env:CONFIGURATION\$($project.BaseName).dll"
            if (-not (Test-Path $dll)) { throw "$dll was not built. A suite that does not exist is not a suite that passed." }
            $dll
          }
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $vstest = & $vswhere -latest -products * -find '**\vstest.console.exe' | Select-Object -First 1
          if (-not $vstest) { throw "vstest.console.exe not found." }
          # The console logger at detailed verbosity prints what a test wrote (Logger::WriteMessage)
          # for passed tests too, which is how a measurement a test makes reaches the agent: the
          # session cannot fetch the TestResults artefact through its egress proxy, but it can read
          # this log.
          & $vstest $suites /Platform:x64 /Logger:trx /Logger:"console;verbosity=detailed" /ResultsDirectory:TestResults
          if ($LASTEXITCODE -ne 0) { throw "vstest reported failures." }

      # The capture (TechnicalDesign.md §6.1, §10; m1-vertical-slice/G2): the game on WARP with no
      # window, NINE THOUSAND TICKS of a match between two scripted commanders and a BMP every
      # hundredth, so that the frames an agent cannot render on its own machine become artefacts of
      # the pull request. The executable exits 2 when the debug layer reported anything and 1 when
      # it could not run at all, and either fails the job. A Windows-subsystem executable starts
      # without a console and PowerShell would not wait for it on its own, so Start-Process -Wait
      # does; the log the run wrote is printed here, because the agent reads this job log and cannot
      # fetch the artefact. Guarded on the executable, like every other step on its input.
      #
      # NINE THOUSAND AND NOT SIX, on a measurement rather than a preference (G2's acceptance): the
      # two commanders acquire on tick 6,908 and fire first on 6,980, so a capture that stopped at
      # 6,000 could not show combat however the camera was pointed.
      - name: Capture frames on WARP
        if: hashFiles('x64/Debug/OutpostCommander.exe') != ''
        shell: pwsh
        run: |
          $process = Start-Process -FilePath 'x64\Debug\OutpostCommander.exe' -ArgumentList '--warp', '--capture', 'Slice', '9000', 'Captures' -Wait -PassThru -NoNewWindow
          if (Test-Path 'Captures\capture.log') { Get-Content 'Captures\capture.log' } else { Write-Output '::warning::Captures\capture.log was not written.' }
          $frames = Get-ChildItem 'Captures' -Filter '*.bmp' -ErrorAction SilentlyContinue
          $frames | ForEach-Object { Write-Output "$($_.Name): $($_.Length) bytes" }
          Write-Output "$($frames.Count) frame(s), $([math]::Round((($frames | Measure-Object -Property Length -Sum).Sum) / 1MB, 1)) MB before the artefact is zipped"
          # What a glance would tell, as text, for the frames somebody actually checks: the opening
          # pair ADR-005 compares, the three G2 names, and the two that should hold combat. Every
          # frame would be three thousand lines of thumbnails in a log the agent has to read.
          if (Test-Path 'Tools\DescribeCapture.py') {
            $named = @('frame-00800.bmp', 'frame-00800-desaturated.bmp', 'frame-02000.bmp', 'frame-04000.bmp', 'frame-06000.bmp', 'frame-07000.bmp', 'frame-09000.bmp') |
              ForEach-Object { Join-Path 'Captures' $_ } | Where-Object { Test-Path $_ }
            if ($named) { python Tools\DescribeCapture.py @named }
          }
          if ($process.ExitCode -ne 0) { throw "OutpostCommander --capture exited with $($process.ExitCode)." }
          # G2'S OWN ACCEPTANCE, CHECKED RATHER THAN LOOKED AT: at least one frame shows a shot in
          # flight. A capture whose frames show none is a camera pointed at the wrong part of the
          # landscape, and the script in OutpostCommander/App.cpp is what changes. It is asserted
          # here and not in the executable because the exit codes mean "could not run" and "the
          # debug layer spoke", and an artefact that is uninteresting is neither.
          $summary = Select-String -Path 'Captures\capture.log' -Pattern '(\d+) frame\(s\) with a shot in the air' | Select-Object -Last 1
          if (-not $summary) { throw "The capture wrote no summary line; it did not reach the end of the match." }
          $withShots = [int]$summary.Matches[0].Groups[1].Value
          Write-Output "Frames showing a shot in flight: $withShots"
          if ($withShots -lt 1) { throw "No frame of the capture shows a shot in flight (m1-vertical-slice/G2)." }
          # AND K4'S, THE SAME WAY: the panels are in the frames rather than merely compiled. Every
          # frame writes how many interface quads it drew, and a run whose lowest is zero has a
          # capture of the world with an interface that was built and never reached the pass - which
          # is exactly what a green build says nothing about (AGENTS.md §3). The floor is not one:
          # the six panels of §2 are a fill, four borders and a title strip each before a single
          # widget, so a frame that drew fewer than a hundred quads has lost panels rather than
          # merely had little to say.
          $quadLines = Select-String -Path 'Captures\capture.log' -Pattern '(\d+) interface quad\(s\) over (\d+) selected'
          if (-not $quadLines) { throw "No frame of the capture reported its interface quads (m1-vertical-slice/K4)." }
          $quads = $quadLines | ForEach-Object { [int]$_.Matches[0].Groups[1].Value }
          $selected = $quadLines | ForEach-Object { [int]$_.Matches[0].Groups[2].Value } | Measure-Object -Maximum
          $lowest = ($quads | Measure-Object -Minimum).Minimum
          Write-Output "Interface quads a frame: lowest $lowest, highest $(($quads | Measure-Object -Maximum).Maximum); most devices selected $($selected.Maximum)"
          if ($lowest -lt 100) { throw "A frame of the capture drew $lowest interface quad(s): the panels are not in the picture (m1-vertical-slice/K4)." }
          if ($selected.Maximum -lt 1) { throw "No frame of the capture had a device selected, so the selection and orders panels were never drawn (m1-vertical-slice/K4)." }
          # AND THE SCRIPTED COMMANDER RESEARCHED SOMETHING (m1-vertical-slice/S15). This is the only
          # run in the tree that plays the lobby the GAME ships rather than a test fixture's, which is
          # exactly how a commander locked out of the whole research table went unnoticed: every suite
          # passed, because their fixture sets auto-research and the executable's lobby did not. A
          # commander with only the machine gun cannot destroy a command post at all - eight damage
          # taken to two by the matrix and then all of it by twenty armour - so a match it has won can
          # never be finished.
          $researched = Select-String -Path 'Captures\capture.log' -Pattern '(\d+) item\(s\) researched by the watched commander' | Select-Object -Last 1
          if (-not $researched) { throw "The capture did not report what the watched commander researched (m1-vertical-slice/S15)." }
          $items = [int]$researched.Matches[0].Groups[1].Value
          Write-Output "Items researched by the watched commander: $items"
          if ($items -lt 1) { throw "The scripted commander researched nothing over the whole capture: it is locked to the machine gun and cannot finish a won match (m1-vertical-slice/S15)." }

      # The content tables, read and checked by the same code the game runs before its first tick
      # (ADR-006), so that a table edit that would break the game breaks the build first. Guarded
      # on the tables existing, because they arrive with m1-vertical-slice/C2 and the loader is
      # already here; the executable is guarded on separately, so a build that produced neither
      # skips this rather than failing on a missing file.
      - name: Validate the content
        if: hashFiles('GameData/Components.json') != '' && hashFiles('x64/Debug/OutpostHost.exe') != ''
        shell: pwsh
        run: |
          # A console subsystem executable, so PowerShell waits for it and its diagnostics land in
          # this log, which is where a failing table is read from.
          & 'x64\Debug\OutpostHost.exe' --validate GameData
          if ($LASTEXITCODE -ne 0) { throw "OutpostHost --validate exited with $LASTEXITCODE." }

      # After the build, so that a compile error is reported as a compile error rather than as
      # forty linter findings about a file that does not parse.
      - name: Run clang-tidy
        if: hashFiles('Build/RunClangTidy.py') != ''
        shell: pwsh
        run: python Build\RunClangTidy.py

      - name: Upload the build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
          if-no-files-found: ignore

      - name: Upload the test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: TestResults
          if-no-files-found: ignore

      # Always, so that the frames of a capture that then exited non-zero are still there to look at.
      - name: Upload the captures
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: captures
          path: Captures/*.bmp
          if-no-files-found: ignore

  format:
    name: clang-format
    runs-on: ubuntu-latest
    timeout-minutes: 10

    # Its own Linux job, and not a step on the Windows one, for two reasons. It is seconds rather
    # than minutes, so it reports a formatting problem long before the build would have. And the
    # version is reproducible here: Ubuntu ships clang-format 18.1.3 from apt, where the Windows
    # runner's clang-format is whatever Visual Studio happened to bundle -- and 18 and 22 break a
    # long argument list in different places, so a floating version would flap.
    steps:
      - uses: actions/checkout@v4

      - name: Install clang-format 18
        if: hashFiles('Build/CheckFormat.py') != ''
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format-18

      - name: Check formatting
        if: hashFiles('Build/CheckFormat.py') != ''
        run: python3 Build/CheckFormat.py --clang-format clang-format-18

      # The task plans under tasks/ are a graph an agent computes its next task from
      # (Design/ImplementationPlan.md §3). A plan with a cycle, an unresolvable edge or a task
      # marked done ahead of its dependencies would send the next session the wrong way, so the
      # validator gates here, on the Linux job because it is seconds. Guarded like the rest.
      - name: Validate the task plans
        if: hashFiles('Tools/CheckTaskDag.py') != ''
        run: |
          python3 -m pip install --disable-pip-version-check --quiet pyyaml
          python3 Tools/CheckTaskDag.py
