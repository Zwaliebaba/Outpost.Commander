#pragma once

// The precompiled header of GameClient. It includes this library's master include and nothing
// else; the master include carries the chain below it (AGENTS.md section 2).

#include "GameClient.h"
