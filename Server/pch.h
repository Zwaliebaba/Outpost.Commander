#pragma once

// The precompiled header of Server, the host executable. It includes the master include of every
// library the host is built from; each of those carries the chain below it (AGENTS.md section 2).

#include "GameLogic.h"