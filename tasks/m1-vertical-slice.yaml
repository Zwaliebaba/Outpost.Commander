# Schema: Design/ImplementationPlan.md §3    Validate: python3 Tools/CheckTaskDag.py tasks/m1-vertical-slice.yaml
#
# Commands under `verify` that name msbuild, vstest or RunClangTidy are what CI runs on the pull
# request; an agent session runs on Linux and reads their result off the PR (ImplementationPlan.md §2).

plan: m1-vertical-slice
title: M1 Vertical slice - two commanders on a Small landscape build, design, research and fight to annihilation over loopback
phase: 1
summary: >
  What M1 proves (GameDesign.md §12): two commanders, one human and one scripted, on a Small
  landscape build extractors, generators, a factory and a lab, design a device and fight to
  annihilation, with a host thread and the loopback transport in one process and the client a
  replica from the first slice (owner, 2026-09-17). Content is one landscape, five structures
  and one defence, three chassis, three drives, four modules, thirty research items, a
  fixed-panel interface per Design/Interface.md, Net and Replica over loopback, one scripted AI,
  the damage and visibility models complete, and the M1 tables checked by the cost-efficiency
  script.

  Task ids carry a letter for the layer so that a reader can see the shape: D design, C content,
  S simulation, N net, R replica, K client (engine), G game executable. Every task is blocked by
  the M0 task that gives it its library; the checker shows those as "held by another plan" until
  M0 lands.

  What this plan deliberately leaves to M2, so that nobody adds it here: audio, the sprite
  pass and particles, water with waves, the sky and clouds, Eclipse-shaped windows (M1 has fixed
  panels), save and resume, a lobby, hover, walls and hardpoints, and every module not in the
  four. Projectiles are small geometry instances and an explosion is a sound-less wreck.

  Placeholders: the owner makes the models (owner, 2026-09-17), and until they exist every
  model is a generated primitive with markers, so that composition at markers is exercised
  rather than faked. The Species palettes, icons and the Spectrum font come across under
  the provenance ADR.

tasks:
  - id: D1
    title: Write Design/Interface.md - the M1 fixed-panel interface
    intent: >
      The slice cannot run without a specification of what the operator sees and clicks
      (Design/README.md), and the interface is the one design document the drafts did not
      write. It is written as fixed panels, the M2 Eclipse windows being a later restyling of
      the same content.
    project: Design
    depends_on: []
    files:
      - Design/Interface.md
      - Design/README.md
    acceptance:
      - "Specifies, with a layout sketch in text at the authored resolution: the selection panel (what is selected, its parts, health, rank, orders and stances), the production panel (factory queue from the seat's designs), the design panel (chassis, drive, modules with the derived statistics shown before commit, needing a command post), the research panel (available items with cost, time and effect; auto-research toggle), the construction panel (structure choice and placement with a footprint ghost and the slope and water rules shown), the minimap (fog states, structures, selection, camera frustum, click to move the camera), the power readout (stockpile, cap, income), the order hotkeys and mouse bindings, the end-of-match overlay, and Escape's pause menu"
      - Every panel names the Replica data it reads (R2) and the orders it emits (S2), so K4 and G1 have nothing to invent
      - Design/README.md's table lists it as Design, and the owner's merge of the PR is its acceptance; the PR says so
    verify:
      - python3 Tools/CheckTaskDag.py
    parallel_safe: true
    status: done
    notes: >
      Design/Interface.md, written 2026-09-17. Its §11 lists eleven things no document supplied,
      each against the task that owns it, four of which block K4: the inverse of FitAuthored, what
      Core/RenderView.h must grow for R2, a wire record for the order rejections S2 already
      computes, and the chrome palette file. Its §12 lists nine rulings, three against a reference
      document: text at 16 pixels in one face rather than Species's 12 and 13, one-pixel borders,
      and the right button giving orders while the middle button aims the camera, which M0 had
      bound the other way and G1 changes. The owner's merge of the pull request is the acceptance
      of all nine.
      RULING 3 WAS REWRITTEN ON 2026-09-19 and this note is left as it was written, because the
      point of the note is what D1 decided. The owner asked for Species's arrangement - the mouse
      connected to the camera with no button held, Escape releasing it - so aiming comes off the
      buttons altogether, the pointer has the two modes Design/Interface.md §4 now names, and the
      pause menu moves to F10. G1b carries it; K7 carries the ground cursor that goes with it.

  - id: C1
    title: Content loaders, validation with file-and-line diagnostics, and OutpostHost --validate; the content ADR
    intent: >
      Game data is JSON files under Content\ (TechnicalDesign.md §8) read into plain row
      aggregates by one loader that refuses a broken tree with a location, and the same code
      runs in CI as OutpostHost --validate, so that a table edit that would break the game
      breaks the build first.
    project: Content
    depends_on: []
    blocked_by:
      - m0-foundation/T11
      - m0-foundation/T13
      - m0-foundation/T17
    files:
      - Content/ComponentDesc.h
      - Content/ResearchItemDesc.h
      - Content/StructureDesc.h
      - Content/DamageTable.h
      - Content/BiomeDesc.h
      - Content/LandscapeDefinition.h
      - Content/StampDesc.h
      - Content/ModelDesc.h
      - Content/SoundEventDesc.h
      - Content/ContentTree.h
      - Content/ContentLoader.h
      - Content/ContentLoader.cpp
      - Content/ContentValidator.h
      - Content/ContentValidator.cpp
      - Content/DesignStats.h
      - Content/DesignStats.cpp
      - Content/Content.vcxproj
      - Content/Content.vcxproj.filters
      - OutpostHost/Main.cpp
      - Tests/ContentTests/ContentLoaderTests.cpp
      - Tests/ContentTests/ContentValidatorTests.cpp
      - Tests/ContentTests/DesignStatsTests.cpp
      - Tests/ContentTests/BrokenFixtures.h
      - Tests/ContentTests/ContentTests.vcxproj
      - Tests/ContentTests/ContentTests.vcxproj.filters
      - Design/ADR/README.md
      - Design/TechnicalDesign.md
    acceptance:
      - "One row aggregate per table with plain camelCase fields (R8), every number an integer in the unit its name says (hundredths for factors and percentages, ticks for times, subunits for distances, power in hundredths): ChassisDesc, DriveDesc, ModuleDesc, ResearchItemDesc, StructureDesc and its module rows, DamageTable (five weapon classes by ten target classes plus armorFactor and the armour kind per weapon class), BiomeDesc (palette, water and wave bitmaps, the two lights, fog, sky), LandscapeDefinition (the schema m0-foundation/T16 wrote), StampDesc, ModelDesc (positions, colours, triangles, markers, fragments), SoundEventDesc"
      - 'ContentLoader reads GameData\ from Paths::GameDataDirectory() or a directory given to it, file by file, into a ContentTree, and stops at the first file that fails with the file, line and column; ContentValidator then checks the whole tree: every research prerequisite exists and the tree has no cycle, every unlock names a real component or structure, every model, texture and sound a row names exists, no id is duplicated, every number is within the range its field declares, and every landscape''s palette names a biome'
      - "DesignStats derives speed, hit points, armour, cost, build time and sight from a chassis, a drive and modules by the formulas of GameDesign.md §6, in integer arithmetic, and ContentTests reproduces the two worked examples (104 world units per second, 130 power, 13 seconds; 29, 490, 49) once C2's tables exist, against literal rows until then"
      - OutpostHost --validate [directory] loads and validates with that code and exits 0 or non-zero printing every diagnostic; it is a step in CI guarded on Content\\Components.json existing, added to the workflow in this task
      - 'ContentTests loads a tree written to a temporary directory by the test, never GameData\ in place, and refuses each of a set of deliberately broken documents with the right file and line: a missing prerequisite, a cycle, an unknown model, a duplicate id, a number out of range, a malformed file'
      - The content ADR, numbered when written, records the directory layout, every schema with a version field, the range of every numeric field, and the rule that a loader change and a content change ship together
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      Green on CI run 44 (b474d6c), with the loader, the validator, the derivation and the host's
      --validate. ADR-006 is the content ADR. Two things it needed did not exist: Neuron::TICKS_PER_SECOND,
      which ADR-002 had fixed at 20 and no code had yet declared, now in Core/FixedPoint.h beside
      the other units; and a line on a JsonValue, so that a fault in what a value means rather
      than in its syntax points at the value. ContentTree::origins records the file and line of
      every row as it is read, which is how a duplicate id or a missing prerequisite names a line
      although it spans files. The CI step is guarded on GameData\Components.json, which C2 writes,
      so it skips until then. Run 43 was red on clang-tidy alone, for three fair findings the
      session's own linter had been missing because it was passing -p with no compilation
      database, which silently drops the repository's configuration: the fixture documents were
      pointers to const rather than constants, both scratch destructors called a filesystem
      function that can throw, and the host's main built a path from a command-line argument.
      Run without that flag, the session's clang-tidy reproduces every one of them.

  - id: C2
    title: Author the M1 content tables
    intent: >
      The slice's numbers exist as files a reviewer can diff against GameDesign.md §4 to §8, so
      that every simulation task reads content rather than literals and rebalancing is an edit.
    project: Content
    depends_on: [C1]
    # The placeholder models C4 generates are a prerequisite in fact: ContentValidator refuses a
    # row naming a model no file defines, and CI runs OutpostHost --validate the moment
    # GameData/Components.json exists. C2 therefore landed Tools/MakePlaceholderModels.py and its
    # output with the tables (2026-09-18); C4 keeps the importers and the provenance ADR.
    files:
      - GameData/Components.json
      - GameData/Structures.json
      - GameData/Research.json
      - GameData/Damage.json
      - GameData/Biomes.json
      - GameData/Landscapes/Slice.json
      - GameData/Sounds.json
      - Tests/ContentTests/DesignStatsTests.cpp
    acceptance:
      - "Components.json: chassis Light I, Medium I, Heavy I; drives Wheels, Half-track, Tracks; modules Machine gun, Cannon, Mortar, Builder, every number from the GameDesign.md §6 tables"
      - "Structures.json: Command post, Extractor, Generator, Factory, Research lab, Tower, with footprints, strength classes, costs, build times, sight radii, module slots and effects from §4 and §5; the Tower carries the Machine gun"
      - "Research.json: thirty items whose tree lets a commander reach every M1 component and structure once, with costs and times inside the §4 ranges, and upgrades for armour, damage, extractor rate and hit points as class percentages"
      - "Damage.json: the version-1 matrix of §8 and the armorFactor and armour kind per weapon class"
      - "Biomes.json: the Default biome with the palette LandscapeDefault.dds, the light pair SpeciesLook.md §3 measures as typical (a near-white key at 25 degrees elevation, an orange horizontal sun at peak 3), the fog and sky per the ADR of m0-foundation/T20"
      - "Landscapes/Slice.json: a Small landscape whose seed the landscape tool reports as passing all four guarantees, with two starts and twelve deposits placed per GameDesign.md §3 (a modest cluster at each start, the largest midway)"
      - OutpostHost --validate passes over the tree, and the CI step runs it
      - The DesignStats test now reads the tables and reproduces the worked examples from them
      - "The terrain draws from the biome's palette rather than the built-in gradient: the loading and the four Species palettes landed early, on 2026-09-18, so this task points the Default biome at GameData/Terrain/LandscapeDefault.dds and checks the capture's frames are coloured by it. Getting GameData beside the executable is the owner's step and no build target does it, so until that is in place the capture logs the gradient and this line is not yet checkable"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Tools/LandscapeTool.py --report Small --seeds 100
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE LAST ACCEPTANCE LINE IS STALE AS WRITTEN, corrected 2026-09-19. It says getting GameData
      beside the executable is the owner's step and that no build target does it; a build target
      does it now - OutpostCommander.vcxproj's post-build event mirrors GameData into $(TargetDir)
      with robocopy and treats only errorlevel 8 and above as failure, which is right, because
      robocopy reports 0 to 7 on success and a plain check for a non-zero exit would fail every
      build. So the line is checkable, and what is left of it is the check itself: that the
      capture's frames are coloured by LandscapeDefault.dds rather than by the built-in gradient.
      G2's capture is the first one that runs with the content tree beside it, and it confirms this
      line or reopens it.

  - id: C3
    title: Write Tools/CheckBalance.py, the cost-efficiency script
    intent: >
      The tables are checked before the design screen exists (GameDesign.md §8): every design
      against every design per tier and per power spent, so that a design that dominates its
      tier is found as a table bug rather than by a player.
    project: Tools
    depends_on: [C2]
    files:
      - Tools/CheckBalance.py
      - GameData/Components.json
      - Design/GameDesign.md
    acceptance:
      - Reads Components.json, Damage.json, Research.json and Structures.json; enumerates every design (chassis, drive, one module set) available at each research tier; derives its statistics by the same formulas as Content/DesignStats
      - "For each pair of designs computes the expected time to kill in both directions from damage, rate, hit chance at short range, the matrix, armorFactor and hit points, and a power-normalised score; prints per tier the designs that beat every other design of equal or lower cost, which is the dominance finding, and the best three designs per role"
      - Exits non-zero when any tier has a dominator; the M1 tables have none, and the notes record what was changed to get there
      - Runs on Linux on the standard library
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Tools/CheckBalance.py GameData
    parallel_safe: true
    status: done
    notes: >
      ONE TABLE EDIT: the medium chassis was underpriced at 150 and is now 170, in
      GameData/Components.json and in GameDesign.md 6's table. At tier 2, with tracks not yet
      researched, MediumI/HalfTrack/MachineGun beat every design costing no more at equal power
      spent, the light cannon included - and the light cannon is the anti-armour answer that tier
      is meant to have. 288 hit points behind 12 armour outlast 100 behind 5 by more than the
      extra power, and 12 armour floors a machine gun's 8 damage to a third, so nothing cheap
      could punish it either. 160 clears every tier; 170 is taken for margin, because a table
      that only just passes fails on the next edit.
      THE COMPARISON IS AT EQUAL POWER SPENT, not one against one. The first run compared duels
      and reported 48 dominators, every one of them a heavier thing beating a lighter one, which
      is true and useless. Each side now fields cost-many of its design for the same budget and
      the budget cancels, which is what "per power spent" in the acceptance means.
      TWO BLIND SPOTS ARE DECLARED RATHER THAN LEFT TO FIRE. There is no movement in the model,
      so a pair of designs differing only in drive cannot be compared - the drive is bought for
      speed, slope and water - and such a pair is excluded from what a design must BEAT, though
      it still counts as a COUNTER: without the first the script reports that tracks dominate
      half-tracks dominate wheels, its own blind spot restated; without the second it reports
      that a tracked cannon dominates its tier while a wheeled cannon of the same price beats it
      outright. And GameDesign.md 8 says an indirect weapon decides its hit at impact "against
      whatever is in the splash radius at the predicted landing cell, so a moving target can walk
      out from under a mortar" - the table states the hit chance against the landing cell and
      carries no shell flight time to work the difference out from, so a design armed only with
      indirect weapons is listed and left unranked rather than counted a dominator on its stated
      100 percent.
      The arithmetic is Content/DesignStats.cpp's and Content/DamageTable.h's, reimplemented
      because the acceptance asks for Linux and the standard library. Two implementations that
      disagree would be a finding in itself, and DesignStatsTests re-derives GameDesign.md 6's
      worked examples from the same tables, so both ends are pinned.

  - id: C4
    title: Importers, placeholder models, the Species textures and font as DDS; the provenance ADR
    intent: >
      Models compose at markers (TechnicalDesign.md §8) and the owner authors them in OBJ, so
      ImportObj.py exists first; until the owner's models exist, generated primitives with
      markers stand in. The Species palettes, icons and Spectrum font come across as
      SpeciesLineage.md decided, converted to DDS because textures are DDS and nothing else
      (owner, 2026-09-17), and the provenance ADR, numbered when written, records what came and the accepted risk in terms.
    project: Tools
    depends_on: [C1]
    files:
      - Tools/ImportObj.py
      - Tools/ImportShp.py
      - Tools/ImportSounds.py
      - Tools/ImportTextures.py
      - Tools/MakePlaceholderModels.py
      # Tools/MakeTerrainPalette.py and the four general Species palettes landed with C1's
      # follow-up on 2026-09-18 (OpenQuestions.md Q18): GameData/Terrain/Landscape{Default,
      # Desert,Earth,Icecaps}.dds, each with its bottom six rows ramped into the water colour so
      # that any two blend at the shore. C4 brings the water and the wave textures under Terrain
      # and the font, the icons and the cloud noise under Textures; M2 authors the four biomes the
      # Species set does not cover, its other four palettes being recolours of Default for one
      # location each.
      - GameData/Models/ChassisLight.json
      - GameData/Models/ChassisMedium.json
      - GameData/Models/ChassisHeavy.json
      - GameData/Models/DriveWheels.json
      - GameData/Models/DriveHalfTrack.json
      - GameData/Models/DriveTracks.json
      - GameData/Models/ModuleMachineGun.json
      - GameData/Models/ModuleCannon.json
      - GameData/Models/ModuleMortar.json
      - GameData/Models/ModuleBuilder.json
      - GameData/Models/CommandPost.json
      - GameData/Models/Extractor.json
      - GameData/Models/Generator.json
      - GameData/Models/Factory.json
      - GameData/Models/ResearchLab.json
      - GameData/Models/Tower.json
      - GameData/Models/FactoryModule.json
      - GameData/Models/LabModule.json
      - GameData/Models/GeneratorModule.json
      - GameData/Models/Shell.json
      - GameData/Models/Wreck.json
      - GameData/Models/Deposit.json
      - GameData/Textures/SpectrumFont.dds
      - GameData/Textures/Icons.dds
      - Design/ADR/README.md
      - Design/ADR/ADR-<next>-species-content.md
      - Design/SpeciesLineage.md
    acceptance:
      - "ImportObj.py converts a Wavefront OBJ with per-face material colours into a model JSON the C1 loader accepts, with an object named Marker<Name> becoming a marker at its origin with its orientation; a test OBJ committed beside it round-trips"
      - 'ImportShp.py parses a Species .shp with both the Triangles and Strips encodings, fragments, colours and markers (the prototype is the review tool of 2026-09-17), and its output loads; it exists for reference imports and imports nothing into Content\ in this task'
      - "MakePlaceholderModels.py generates the M1 set as primitives: chassis boxes with MarkerMount and MarkerDrive markers scaled per class, drives as wheel cylinders or track boxes, modules with MarkerMuzzle, structures as boxes of their footprint with a team-colour slot, a shell, a wreck and a deposit; every model the tables name exists and validates"
      - ImportSounds.py selects and copies WAVs by the names a Sounds.txt references, ready for M2, and copies nothing now
      - "ImportTextures.py converts a Species BMP (24-bit, or 4- and 8-bit through its palette - Particle and ShapeWireframe are 4-bit) to an uncompressed B8G8R8A8_UNORM DDS with no mip chain, writing the header and the pixels itself, and derives the alpha BMP does not carry by the rule its manifest states per file: the colour key for the four sprites that have one, luminance for the effect sprites, icons and font that are drawn additively against black, and flat opaque for the lookups and gradients (SpeciesLineage.md §4, measured 2026-09-19); --analyze checks every rule against the pixels and --selftest covers the three rules and a DDS round trip; the Species Default landscape palette comes across through it under GameData\\Terrain and the Spectrum font atlas and the order icons under GameData\\Textures, Core's TextureFile reads them, and SpeciesLineage.md §4 records them as taken"
      - "The provenance ADR lists every Species-derived file in the tree with its source path, states that the owner accepts the provenance risk including distribution to other players in M3 and inside mods (OpenQuestions.md R5), and repeats what is excluded regardless"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Tools/MakePlaceholderModels.py GameData/Models
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
    parallel_safe: true
    status: done
    notes: >
      ADR-010 is the provenance ADR, with a table naming every Species-derived file in the tree,
      the Species path it came from and the tool that converted it. The exclusions - the
      soundtrack, the branding, the narration - are enforced in Tools/ImportSounds.py as a refusal
      rather than left to a reader's memory, because that is the one part of the decision that
      must not fail quietly.
      THE COLOUR KEY IS NOT MAGENTA EVERYWHERE, which SpeciesLineage.md 4's opening line said and
      C4 found to be wrong when converting. Magenta is the SPRITES' key; the font is white glyphs
      on black so its key is black; the order icons are glyphs on an opaque dark-blue field that is
      part of the icon and take no key at all. Measured before converting; the line is corrected.
      ImportShp.py reads all 106 Species shapes, both encodings, and the one that failed first -
      BattleCannonBase.shp - turned out to be markers with no geometry at all, which is a real file
      and not a broken one: a set of mount points for other shapes. It now comes across as a model
      with no geometry and says so.
      ImportSounds.py copies nothing, as the acceptance says, and it found that a SOUNDNAME is
      often a GROUP rather than a file: 0lasers is not Sounds/0lasers.wav, it is Laser1 through
      Laser11. 163 names resolve to 54 files exactly and 41 groups covering 316 more, which the
      tool reports as guesses rather than as matches. Which alternative an event wants is
      m2-skirmish/T9's to decide.
      ImportObj.py takes the team-colour slot from a material's DISSOLVE of zero rather than from a
      name: zero opacity is the one thing an artist can set in any tool and that no real surface
      wants, where a naming convention breaks on a rename. Tools/Fixtures/MarkerCube.obj is the
      committed round trip, and its output loads in the real C++ loader.
      ImportTextures.py writes the DDS header and pixels itself rather than taking a dependency for
      ninety bytes. Core's TextureFile reads both files it produced.
      The plan's file list names the models as ChassisLight.json and so on; C2 generated them as
      ChassisLightI.json, because a model id may not be the id of the row that names it and the
      mark is part of the row's. The list is stale rather than unmet.
      MakePlaceholderModels.py now takes its output directory positionally as well, because this
      task's own verify line spells it that way and a tool whose documented invocation fails is a
      tool nobody runs.

  - id: S1
    title: Sim world objects and seats
    intent: >
      Devices, structures, projectiles, features and wrecks exist as records in slot maps with
      an ObjectId that never aliases (TechnicalDesign.md §4.3), and a seat holds what a
      commander owns, so that every later system has state to act on and the hash and snapshot
      cover all of it.
    project: Sim
    depends_on: [C1]
    blocked_by:
      - m0-foundation/T15
      - m0-foundation/T17
    files:
      - Sim/ObjectId.h
      - Sim/Device.h
      - Sim/Structure.h
      - Sim/Projectile.h
      - Sim/Feature.h
      - Sim/Wreck.h
      - Sim/World.h
      - Sim/World.cpp
      - Sim/Seat.h
      - Sim/Seat.cpp
      - Sim/StateHash.cpp
      - Sim/Snapshot.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Tests/SimTests/WorldTests.cpp
      - Tests/SimTests/SnapshotTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - ObjectId is a std::uint32_t creation counter with a kind tag, issued by World in creation order, and World resolves it through the slot map's generation-checked handle; a stale id resolves to nothing
      - Each record holds only simulation fields in the units of TechnicalDesign.md §4.1 and refers to other objects by ObjectId, never by pointer or index
      - "Seat holds power in hundredths, the stockpile cap, the research state, the designs, the device and structure counts against the caps, the fog grid and the ghost store (S9 fills them), the victory state and the AI flag"
      - The state hash covers every field of every record and every seat, and the snapshot round-trips a populated world
      - "Tests: creation order and id monotonicity, resolution of a removed id, hash sensitivity to a single field, snapshot equality on a populated world"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done

  - id: S2
    title: Sim orders - the twenty kinds, validation with reason codes, application at tick start
    intent: >
      Orders are the only input (TechnicalDesign.md §4.7) and the whole of the trust model: the
      framework validates every order against what the seat owns, sees and can afford and drops
      what fails with a reason, so that every later system adds its kinds' rules to one place.
    project: Sim
    depends_on: [S1]
    files:
      - Sim/Order.h
      - Sim/OrderValidation.h
      - Sim/OrderValidation.cpp
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Tests/SimTests/OrderValidationTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - Order.h documents the operand layout of every one of the twenty kinds
      - "OrderValidation returns an accepted order or a RejectReason (NotOwned, NotVisible, CannotAfford, AtCap, InvalidTarget, InvalidPlacement, NotResearched, NoCommandPost, Malformed); stage 1 applies accepted orders in seat order then arrival order and records the rejections per seat for Net to report"
      - "Move, AttackMove, Stop, SetStance, Group, Chat and Surrender are applied by this task; every other kind is validated for ownership and shape here and applied by the task that owns its system, which extends OrderValidation"
      - "An Attack on a target the seat cannot see becomes an AttackMove to the target's last known position from the ghost store, or is rejected NotVisible when there is none"
      - "Tests: each reason fires on a crafted order, an accepted Move changes the device's order, arrival order within a seat is preserved, and the rejection list is part of the hash"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done

  - id: S3
    title: Sim economy - deposits, extractors served within 48 cells, the stockpile and its cap, cost and refund, the army caps
    intent: >
      Power comes from the ground through the chain GameDesign.md §4 describes, with the brakes
      the review added, so that a turtled base cannot bank an army and generators are the
      target at this game's distances.
    project: Sim
    depends_on: [S2, C2, C5]
    files:
      - Sim/Economy.h
      - Sim/Economy.cpp
      - Sim/Deposit.h
      - Sim/Deposit.cpp
      - Sim/MatchSettings.h
      - Sim/OrderValidation.h
      - Sim/OrderValidation.cpp
      - Sim/Sim.h
      - Sim/Sim.cpp
      - Sim/Snapshot.h
      - Sim/Snapshot.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - OutpostCommander/App.cpp
      - Tests/SimTests/EconomyTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - Deposits come from the landscape definition and are simulation objects an extractor is built on and nowhere else
      - "A generator serves the four nearest unserved extractors within 48 cells, recomputed when a generator or extractor is completed or destroyed, ties broken by ascending id; an extractor beyond every generator's reach produces nothing; a served extractor yields 5 power per second and a command post 1, credited per tick in hundredths"
      - The stockpile is capped at 1,000 plus 500 per completed generator; cost is drawn when construction or production begins; cancelling refunds the unbuilt share and any refund above the cap is lost; cancelled research refunds nothing; demolition refunds half
      - "The device cap (100, 200 or 300 from MatchSettings) and the structure cap of 300 are enforced: a factory at the cap pauses, a placement at the cap is rejected AtCap"
      - "Tests reproduce the worked numbers: income over 20 ticks, the fifth extractor unserved, a generator at 49 cells serving nothing, the cap at two generators, a refund clipped by the cap, the factory pause"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      The device cap had nowhere to come from: GameDesign.md 4 calls it a lobby setting and
      MatchSettings had no field for it, so DeviceCapLevel and DEVICE_CAPS are added beside
      PowerLevel and the snapshot is at version 7. The structure cap is STRUCTURE_CAP, one number
      for every match, because the design gives one.
      A deposit is NOT an object in World. Nothing ever refers to one - an extractor knows the cell
      it stands on and that is the whole relationship - so an id would buy a sixth ObjectKind and a
      sixth map for a record that never changes and is never pointed at. Sim/Deposit.h is the index
      instead, rebuilt from the definition wherever a definition arrives, which is CreateLandscape
      and Snapshot::Read; a test pins that a restored match still has its deposits and goes on
      earning, because forgetting the second of those two would have been silent.
      The service assignment is recomputed every tick rather than on completion and destruction as
      the acceptance says. It satisfies the acceptance strictly (the answer is right after any such
      event) and removes the class of bug where a system that changes a structure forgets to mark
      the cache; the cost is bounded by the structure cap at around 20,000 squared-distance tests a
      seat a tick, and a stale assignment two hosts agree on is a wrong economy rather than a
      desync, which is the harder of the two to find. Caching wants a profile that asks for it.
      The counts on the Seat are filled by one walk of the world in stage 2 rather than by running
      totals. Seat.h says they are kept rather than counted "because the cap is tested on every
      production tick"; the field stays and the test stays a subscript, but nothing has to remember
      to decrement.
      GameDesign.md 4's cap collides with its own lobby: PowerLevel::High starts a commander at
      2,500 against a base cap of 1,000. The cap is on what ACCUMULATES - the section's own words
      are "income accumulates into a stockpile capped at" and "any refund that would exceed the cap
      is lost" - so a stockpile already over its cap is left alone and simply stops growing. The
      alternative, clipping the balance, deletes three fifths of a lobby setting on the first tick.
      Raised for the owner rather than settled here.
      S2 left two checks deliberately loose for this task and both are exact now: a placement is
      priced by its row's costHundredths rather than by "the stockpile is empty", and an extractor
      is refused anywhere but a deposit. OrderContext gained the content tree and the deposit field
      for them, both optional, so a suite that cares about ownership still needs neither.

  - id: S4
    title: Sim structures - placement, plans, construction by builders, flatten, modules, demolition, wrecks
    intent: >
      Structures are placed by the commander and built by builders under the rules of
      GameDesign.md §5, with the terrain flattened under the footprint at construction start,
      so that bases grow the way the design says and the pathfinder sees only what stands.
    project: Sim
    depends_on: [S3]
    files:
      - Sim/Placement.h
      - Sim/Placement.cpp
      - Sim/Construction.h
      - Sim/Construction.cpp
      - Sim/Plan.h
      - Sim/Structure.h
      - Sim/Order.h
      - Sim/OrderValidation.cpp
      - Sim/Visibility.h
      - Sim/Visibility.cpp
      - Sim/StateHash.cpp
      - Sim/Snapshot.h
      - Sim/Snapshot.cpp
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - GameData/Structures.json
      - Tests/SimTests/PlacementTests.cpp
      - Tests/SimTests/ConstructionTests.cpp
      - Tests/SimTests/EconomyTests.cpp
      - Tests/SimTests/VisibilityTests.cpp
      - Tests/SimTests/WorldTests.cpp
      - Tests/SimTests/SnapshotTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - "Placement accepts a footprint where no structure, plan under construction, feature or water cell lies under it, the slope across it is under 25 percent, the seat has explored every cell, and an extractor is on a deposit; PlaceStructure, CancelStructure, Demolish and BuildModule are validated and applied"
      - A placed plan costs nothing, obstructs nothing and counts against a limit of 64 per seat; construction begins when a builder is within its range, at which moment the cost is drawn, the terrain under the footprint is flattened to its mean height as a HeightDelta, and the obstruction grid is marked
      - Progress advances by the sum of the attending builders' rates; hit points are proportional to progress; a structure under construction can be destroyed; a complete structure enters service and the economy recomputes
      - Modules are built by builders onto standing structures after their research and apply their effects (generator +2 extractors, factory -25 percent per module to two, lab -30 percent)
      - A destroyed structure becomes a wreck that obstructs nothing and expires after a stated number of ticks; demolition refunds half
      - "Tests: every placement rule with a synthetic landscape, the plan limit, flatten writes a delta and the samples change, two builders halve the time, destruction mid-construction, the module effects"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE SLOPE RULE WAS BUILT THE OTHER WAY FIRST AND THE MEASUREMENT TURNED IT ROUND. "The slope
      across the footprint is under 25%" reads as the relief over how far the footprint reaches, and
      that is what was written; Landscape::Cell's slopePercent - the steepest step between two
      adjacent samples - was rejected in a comment as "much harsher". Measured over the 795 dry
      three-by-three sites of the built-in Small landscape, the average gradient refuses 4 of them
      and the steepest cell refuses 31, so the harshness was 3.4% of the map rather than a wall, and
      the objection was arithmetic nobody had done. Two things then decide it: 25 is the same number
      and the same unit GameData\Components.json gives Wheels for maxSlopePercent, which is read per
      cell; and what the steepest cell refuses and the average waves through is a footprint
      straddling a CLIFF EDGE, eight gentle cells and one vertical one, which is exactly the site a
      factory must not stand on. PlacementTests pins the reading with a case the other one accepts.
      THE FLATTEN NO LONGER BLACKS OUT THE MAP. S9 dropped every stamp and every fog grid on a
      height delta and left the narrowing to this task, which is the one that actually flattens - and
      it flattens every time a structure goes up, so the explored history of every commander would
      have gone with it. Visibility::InvalidateRegion un-counts only the discs that REACH the
      changed cells, before the heights move; a disc that does not reach them cannot have a cell
      changed by them, so it is exact and not an approximation. VisibilityTests now pins both halves.
      THE BUILD ACCUMULATOR IS EFFORT AND NOT A PERCENTAGE. Structure::buildProgressHundredths
      became buildEffortHundredths because a percentage per tick does not divide: a hundred percent
      over a 1,200-tick factory is 8.33 hundredths a tick, and a truncated percentage reaches 9,600
      and stops. The builders' own contribution accumulates exactly, and the percentage the hit
      points and the refund want is a function of it. Snapshot version 9 carries it and the two
      module fields.
      HOW A BUILD TIME AND A BUILD RATE MEET. The tables state a time per structure (GameDesign.md
      §5, "over the build time") and a rate per builder (§6, ten power a second), and neither says
      what a faster builder does to a stated time. A structure needs buildTimeTicks times the
      REFERENCE rate of effort and each builder puts in its own a tick, so today's only builder
      module is the reference and a structure takes exactly the time its row states. Stated but not
      corrected: the rows' build times are about 1.5 times what cost-divided-by-ten-power-a-second
      would give, four of six at exactly 1.5, and S5's acceptance builds DEVICES by that other rule -
      so structures and devices genuinely build at different paces in the tables as authored.
      A builder is not ordered to build. There is no build order in the order table, so a device
      carrying a builder module attends every plan and site of its own seat within that module's
      range, which makes moving a builder the way a commander directs one.
      One content fix: GameData\Structures.json gave LabModule 25 percent where GameDesign.md §5 and
      this task's acceptance both say 30. The table was wrong, not the design.
      Module effects: the generator's +2 was already live (S3 reads it where it serves), and the
      factory's and the lab's have no consumer until S5 and S6 - ModuleTimeReductionPercent is here
      so that when they arrive they read it from one place rather than two.

  - id: S5
    title: Sim devices and production - designs, derived statistics, factories, spawning, ranks
    intent: >
      A device is a design of chassis, drive and modules whose statistics derive from the parts
      (GameDesign.md §6, pillar 1), built by a factory from the seat's designs, so that the game
      the pillars describe exists in the simulation.
    project: Sim
    depends_on: [S3]
    files:
      - Sim/Design.h
      - Sim/Production.h
      - Sim/Production.cpp
      - Sim/Device.h
      - Sim/Seat.h
      - Sim/OrderValidation.cpp
      - Sim/Snapshot.h
      - Sim/Snapshot.cpp
      - Sim/StateHash.cpp
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Content/DesignStats.h
      - Content/DesignStats.cpp
      - Design/OpenQuestions.md
      - Tests/SimTests/ProductionTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
      - Tests/ContentTests/DesignStatsTests.cpp
    acceptance:
      - "A Design is per seat: a name, a chassis, a drive and modules the seat has researched, saved by SaveDesign and validated against research; its statistics come from Content/DesignStats and are stored on the device at spawn with the class-upgrade percentages of the seat applied"
      - SetProduction and CancelProduction manage a factory's queue; build time is cost divided by 10 per second less the factory's module reduction; a factory whose seat is at the device cap pauses; a completed device spawns at the factory's exit cell with the seat's default stances
      - Experience and rank fields exist with eight ranks and the per-rank accuracy and damage percentages the design proposes, applied by S10
      - "Tests: the worked examples from the tables (a light on wheels with a machine gun, a heavy on tracks with a cannon), the queue and cancel refund, the cap pause, the spawn position, that an upgrade researched later changes fielded devices"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      TWO TYPES CALLED Outpost::DeviceDesign, AND THIS IS THE TASK THAT HAD TO SEE BOTH. Content's
      named its parts by id for the design screen and for saving between matches; Sim's named them
      by row index because a design is hashed and sent every tick. Neither file included the other,
      so the collision sat there from C1 and S1 - Sim/Visibility.cpp even has a comment about the
      two - until this task needed one translation unit to derive a seat's design from Content's
      tables. Content's is now DesignRecipe. RecipeFor is the one conversion, so the simulation and
      the design screen answer with one implementation and ProductionTests reaches the design's own
      two worked numbers through the seat's index path.
      THE ACCEPTANCE CONTRADICTS ITSELF ABOUT STORED STATISTICS AND THE SECOND HALF WINS. It asks for
      the statistics "stored on the device at spawn" and then for "an upgrade researched later
      changes fielded devices"; a stored maximum satisfies the first and breaks the second, and
      Sim/Device.h had already written the rule the other way. What is stored is the CURRENT hit
      points, because damage is state; everything else - the maximum included - is derived from the
      design and the seat's upgrades wherever it is wanted, so a retroactive upgrade reaches the
      field without walking it. S6's "hit points keeping their fraction" is then the one rescale it
      does have to do. Seat gained a ClassUpgrades for S6 to fill and S5 to read.
      A DEVICE IS PRICED AND TIMED BY ITS PARTS AND A STRUCTURE BY ITS ROW, which is two rules and
      the tables as authored: the acceptance says a device's build time is "cost divided by 10 per
      second" and GameDesign.md §5 says a structure is built "over the build time" its row states.
      Both are implemented as written and S4's notes carry the 1.5x gap between them.
      The device cap was checked on SetProduction and no longer is: GameDesign.md §4's cap is on what
      stands in the field and the acceptance says a factory at it PAUSES, so a commander may queue
      against a cap and the factory waits. Better play than a refusal, and it is what stage 4 does.
      THE RANK NUMBERS ARE THIS TASK'S PROPOSAL AND OpenQuestions.md Q22 IS NOW OPEN. GameDesign.md
      §8 gives the count and the word "small" and no figures, and the acceptance asks for "the
      percentages the design proposes". Eight ranks, thresholds doubling to 160 weighted kills,
      accuracy and damage reaching 24 percent - argued in the header and in the question, measured
      against nothing, which is why it is a question rather than a decision. C3's script cannot
      settle it, because it compares at equal power spent and a rank is not bought with power.
      Snapshot version 10 carries the upgrades and the queues.

  - id: S6
    title: Sim research - labs in parallel, unlocks and retroactive class upgrades, auto-research
    intent: >
      Research unlocks parts and improves classes (GameDesign.md §7), retroactively for what is
      already in the field, so that the tree drives the match and the AI can spend on it.
    project: Sim
    depends_on: [S3]
    files:
      - Sim/Research.h
      - Sim/Research.cpp
      - Sim/Seat.h
      - Sim/MatchSettings.h
      - Sim/OrderValidation.cpp
      - Sim/Economy.h
      - Sim/Economy.cpp
      - Sim/Snapshot.h
      - Sim/Snapshot.cpp
      - Sim/StateHash.cpp
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Content/DesignStats.h
      - Tests/SimTests/ResearchTests.cpp
      - Tests/SimTests/SnapshotTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - SetResearch starts an item in an idle lab when its prerequisites are complete and the seat can pay, drawing the cost at the start; each lab researches one item; the time is the item's less the lab's module reduction; CancelResearch refunds nothing; a destroyed lab loses the progress
      - "On completion an unlock marks the component or structure available to the seat, or an upgrade adds its percentage to the seat's class value and every fielded object of that class is updated, hit points keeping their fraction"
      - Auto-research, a MatchSettings option per seat, has each idle lab pick the cheapest available item at the start of the tick
      - "Tests: prerequisite enforcement, three labs in parallel, the module reduction, a retroactive armour upgrade on a damaged device, auto-research picking order"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      PROGRESS BELONGS TO A LAB AND NOT TO A SEAT. "Each lab researches one item" and "a destroyed
      lab loses the progress" are both rules, and a list that does not say WHOSE progress it is can
      enforce neither, so ResearchProgress gained the lab. The countdown stays in that record rather
      than on the structure - which is where a factory's is - because a lab has one item and a
      factory has a queue, so for a factory the entry cannot say which countdown is its.
      TWO UPGRADE EFFECTS HAD NOWHERE TO GO. ResearchEffect has ExtractorRate and StructureHitPoints
      and ClassUpgrades had a slot for neither, so they are two more fields on it rather than two
      more places an upgrade can live: one home for "what research has done to this commander" is
      what makes a derivation trustworthy. Economy reads the first where an extractor yields;
      Research reads the second where a structure's maximum is wanted.
      THE UNLOCK EFFECT DOES NOTHING, AND THAT IS NOT AN OMISSION. A row says what unlocks it and an
      item says what it unlocks; the simulation reads the first (UnlockedFor), so an item joining
      researchComplete IS the unlock. The other direction is what the research panel lists and what
      ContentValidator checks, and a second mechanism would be a second thing to keep in step.
      WHAT AN UPGRADE WALKS THE WORLD FOR is one thing only: the current hit points. Everything
      derived is read from the seat's upgrades wherever it is wanted (S5), so a percentage landing
      there already reaches every fielded device; what has to be rescaled is the damage a device is
      carrying, because a device at half health must still be at half health when its maximum grows.
      Maximums are taken before and after and the fraction is multiplied through.
      Auto-research is a per-seat lobby option, so SeatSettings gained a bool and the snapshot is at
      version 11. It fills every idle lab at the start of the tick with the cheapest available item,
      ties broken by the lower row index so two hosts pick the same pair.
      SnapshotTests now furnishes its seats AFTER the ticks rather than before: stages 2, 3 and 4
      own several of those fields now, and a fixture that filled them first was measuring what the
      tick left rather than what the stream carries. Its research entries were being erased by stage
      3, which made one assertion read an empty vector.

  - id: S7
    title: Sim pathing - cluster graph, hierarchical A*, budgeted planning and refinement
    intent: >
      The system that decides whether a large landscape works (TechnicalDesign.md §4.5) exists
      in the shape the design gives, deterministic because its budget is in nodes, so that M1
      measures it on Small and M3 on Large without a rewrite.
    project: Sim
    depends_on: [S1]
    files:
      - Sim/ClusterGraph.h
      - Sim/ClusterGraph.cpp
      - Sim/PathPlanner.h
      - Sim/PathPlanner.cpp
      - Sim/Path.h
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Tests/SimTests/PathingTests.cpp
      - Tests/SimTests/Fixtures/Landscape/README.md
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - The landscape is divided into 16x16-cell clusters; per drive class a cell is passable by its slope class against the drive's maximum and by the water bit against the drive's water flag, and obstructed cells are impassable to every class
      - Each cluster's boundary crossings are nodes and the paths between them inside the cluster are edges computed lazily per drive class and invalidated when the obstruction grid changes inside the cluster
      - "RequestPath(device, destination) plans over the cluster graph with A* in integer costs, refines the first clusters to a cell path, and refines further as the device advances; planning is amortised with a per-tick budget in nodes expanded, so the result of a tick depends only on the state"
      - "Tests: reachability agrees with a flood fill on the twenty Small landscapes of the fixture hashes for every drive class; the same request in two Sims yields the same path; the budget bounds the nodes expanded per tick; placing a structure invalidates exactly the clusters it touches; nodes expanded per request are logged for the ADR the flow-field question will need"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE ABSTRACTION IS COMPONENTS, NOT ENTRANCES, and the flood-fill test is why. The acceptance
      asks for boundary crossings as nodes, which is the textbook hierarchical abstraction; it was
      built that way twice and it is NOT EXACT, which the test caught both times. One node in the
      middle of a run reports unreachable where a flood fill walks through, when a wall inside the
      cluster separates the middle cell from the rest of the run (fixture seed 2, drive 0). Making
      the whole run interchangeable fixes that and breaks the other way: a run that spans a wall
      inside its own cluster then merges two places a drive cannot get between, and the graph
      reports a route through the wall (fixture seed 9, drive 4; then a wall at x=20 routed
      through at zero cost). A node is now a CONNECTED COMPONENT of a cluster's passable cells,
      which cannot have either fault because a component is by definition what a flood fill
      reaches. Reachability is exact by construction. Agreed with the flood fill on 4,785 requests
      across the twenty fixtures and all six drive classes.
      WHAT THAT GIVES UP is the abstract cost: a component is a region and has no one length, so
      the search costs the octile distance between representative cells, an estimate. The
      refinement is exact - a cell search inside each cluster - so a route is optimal in the small
      and approximate in the large. TechnicalDesign.md 12 leaves hierarchical A* against flow
      fields to be settled by measurement, and this is one of the numbers that weighs.
      MEASURED on the session's Linux harness: 38 full Small-landscape requests in 5.1 ms, 17
      components expanded a request over 64 components, 77 cell searches.
      INVALIDATION IS THE WHOLE GRAPH, not the clusters a cell touches, which is coarser than the
      acceptance asks. A wall across a cluster splits a component, and a split changes which
      components a NEIGHBOURING cluster's are joined to, so the blast radius is not local the way
      an entrance's was. It is one flood fill of 256 cells per cluster per class in use, on an
      event that happens when a structure is placed rather than every tick, and the test pins that
      a write leaving the grid as it was rebuilds nothing.
      NOTHING HERE IS IN THE SNAPSHOT, and that leaves a question for S8. A path is a pure function
      of the graph and the request, so a reloaded host that re-requested would compute the same
      one. What it would not reproduce is the TICK the path arrives on, because the budget starts
      again - and if a device's movement depends on when its path arrives, that is a divergence.
      S8 decides: either the pending queue joins the snapshot, or a device re-requests on load and
      the arrival tick is made not to matter. Written down rather than discovered there.
      The budget is in NODES and not in milliseconds, because a budget in time makes the result of
      a tick depend on how fast the machine is, which is a desynchronisation dressed as an
      optimisation.

  - id: S8
    title: Sim movement and steering
    intent: >
      Devices follow their paths at the speed their parts derive across the terrain their drive
      can take, steering around each other in integer arithmetic, so that the crossing times the
      design is sized by are what the slice measures.
    project: Sim
    depends_on: [S7, S5]
    files:
      - Sim/Movement.h
      - Sim/Movement.cpp
      - Sim/Steering.h
      - Sim/Steering.cpp
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Tests/SimTests/MovementTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - A device advances along its path by its speed in subunits per tick, scaled by the terrain factor of its drive on the cell it is on, turns its heading toward the path at a rate in binary angle per tick, and sits at the heightfield's height
      - Separation steering between devices within a radius in integer arithmetic, with a deterministic tie-break by id; a device that cannot make progress for a stated number of ticks re-plans
      - "Move arrives and stops; AttackMove and Patrol advance until S10 interrupts them; Guard returns to its position; Stop clears the path"
      - "Tests: a light on wheels with a machine gun crosses a flat 8,192-unit Small landscape in about 1,575 ticks; a tracked device refuses a 45 percent slope and a wheeled one a 30 percent slope; two devices meeting head-on pass; two Sims move identically"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      Sim/Movement.h and .cpp walk the routes, Sim/Steering.h and .cpp keep devices apart, and
      stage 6 is planning then the walk then the separation, in that order.
      WHAT SLOPE COSTS was a hole in the design and not a number to look up: GameDesign.md 2 says
      slope costs movement and 6 says a drive "sets speed as a function of terrain", and the drive
      table has a speed factor, a max slope, a water flag and a hit-point factor and no terrain
      column at all. Put to the owner as OpenQuestions.md Q23 and answered 2026-09-18: full speed
      on the flat falling LINEARLY to half at the drive's OWN maximum, impassable beyond. Scaling
      to the drive's own limit is the point of the answer - tracks keep 75 percent of their speed
      on a 20 percent slope where wheels keep 60 - so the max slope column buys handling on ground
      both can cross and not only reach on ground one cannot. GameDesign.md 6 records it.
      MEASURED, and it is the number the design is sized by: a light on wheels with a machine gun
      crosses a flat Small landscape, 8,128 world units between the middles of the end cells, in
      1,563 ticks against the 1,563 its 1,331 subunits a tick allow. It cost two things to get
      there. The first is that a tick's distance is spent ACROSS legs: stopping short at a cell's
      middle and losing the remainder costs a twentieth of the speed on cell-sized legs, which is a
      crossing time that no longer matches what the design screen printed. The second is the row
      the test crosses on - a cluster's own first row, where the graph's doorways sit; half a
      cluster higher the same drive takes 1,804 ticks, all of it two diagonals to the doorway and
      back, which is S7's abstraction and is S7's suite to measure.
      SEPARATION IS AWAY-PLUS-A-QUARTER-TURN, not away. A purely radial push between two devices
      meeting head-on is exactly a brake, and they grind to a halt facing each other; turning the
      push a quarter turn - always the same way round - is antisymmetric in a second sense, so each
      takes the side the other does not and they pass. Measured: they close to 1,278 subunits and
      pass 357 subunits either side of the line they met on. The id tie-break is needed for exactly
      one case, two devices at the same point, where there is no direction to push along.
      Bucketed by cell rather than pair by pair: the radius is a quarter of a cell, and the
      eight-seat device cap is 2,400, which pair by pair is 2.9 million tests a tick.
      THE SNAPSHOT QUESTION S7 LEFT is answered the first way: the planning queue joins the
      snapshot, carrying each request and the route it has reached but never the search's working.
      A request still being searched is replayed for the nodes it had already spent, which
      Path::nodesExpanded already records, so a Frontier-sized open set never goes on the wire and
      the tick a route lands on is the tick it would have landed on. Version 12, with a device's
      patrol anchor, how far along its route it has walked and how long it has been stuck.
      TWO BUGS THIS TASK EXPOSED RATHER THAN CAUSED, both of which crash or silently break a
      reloaded match the moment a device moves. The snapshot never rebuilt the obstruction grid or
      the cluster graph, so a reloaded match's buildings were walked straight through - obstruction
      is derived from the standing structures and is in neither the landscape's definition nor its
      deltas, so Construction gained MarkStandingObstructions and the read calls it. And a Sim was
      not safely copyable at all: the cluster graph holds a pointer to the landscape and the
      planner a pointer to the graph, so the Sim that Snapshot::Read hands back planned against the
      landscape of the Sim it was moved out of. It segfaulted on the first tick after a reload.
      Sim's members are now a private base and its copy and move re-aim the two pointers; a
      hand-written member list would have put the next member added in the wrong place in silence.
      ALSO ADDED: Neuron::AngleOf, the inverse of Sin and Cos, found by bisecting the SAME table
      those two read rather than by a polynomial - a device turns to what it says and then drives
      along Sin and Cos of it, and an inverse that disagreed by a unit is a device that turns back
      and forth for ever. Measured round trip: exact, zero units, over the whole turn.
      NOT DONE, and it is S10's: a destroyed device must have its request cancelled. Nothing
      removes a device yet; PathPlanner::Cancel is what S10 calls when something does, and
      Sim/Movement.h says so where the next reader will look.

  - id: S9
    title: Sim visibility - fog grids with viewer counts, the budgeted refresh, height-extended sight, occlusion, the ghost store
    intent: >
      Fog of war is computed on the host and is also the replication filter (TechnicalDesign.md
      §4.6), so this is the system that makes the fog real and the one whose cost the design
      names first; it is built budgeted and deterministic from the start.
    project: Sim
    depends_on: [S1]
    files:
      - Sim/Visibility.h
      - Sim/Visibility.cpp
      - Sim/FogGrid.h
      - Sim/FogGrid.cpp
      - Sim/GhostStore.h
      - Sim/GhostStore.cpp
      - Sim/Seat.h
      - Sim/Snapshot.cpp
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Tests/SimTests/VisibilityTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - "FogGrid per seat holds a viewer count byte and a two-bit state per cell (unexplored, explored, visible); a cell is visible while its count is positive and explored forever after"
      - "A viewer's disc is its sight radius in cells plus one cell per 32 world units it stands above the target cell, each cell tested by an integer walk along the heightfield from the viewer, blocked by a sample above the line of sight; a moved viewer's old disc is un-counted before its new one is counted"
      - The refresh budget is 200 viewers per tick, moved viewers first, then the oldest refresh, so every viewer refreshes at least once per second; allied seats share vision by summing counts into a shared grid or by union at read, decided and tested
      - "GhostStore per seat records the last-seen state of every structure the seat has ever seen, updated when a structure leaves visibility and when it is seen again; the fog grid and the ghost store are in the snapshot, the grid run-length encoded"
      - "Tests: a ridge between viewer and target blocks; a hill extends the radius; the budget bounds the reads per tick; un-seeing on move; ghost recording and update; the reads per refresh are logged for the tick-cost measurement"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      MEASURED, AND IT IS THE FINDING. A full refresh of 200 viewers at a 20-cell radius costs
      4,215,706 cell-height reads, 21,078 a viewer, and took 140 ms on the session's Linux harness
      - nearly three times the whole 50 ms tick. The cause was Visibility::CellHeight rescanning a
      cell's 25 samples on every query, so Landscape::Cell now carries highestSample, derived where
      the slope and the water bit are derived, and the same refresh takes 16.4 ms. That is still a
      third of the tick for one system and the figure to beat; the next easy win is the scan box,
      which is twice the sight radius on each side because height may extend the radius, and so
      scans 6,561 cells to light about 1,257 on ground where no viewer is raised at all. G3's
      measurement on the owner's hardware decides whether it is worth taking.
      THE VIEWER COUNT IS A uint16, NOT THE BYTE THE ACCEPTANCE ASKED FOR. Allied seats share one
      grid and an alliance may be all eight seats at 200 devices and 300 structures each, so 4,000
      viewers can hold one cell at a chokepoint. A byte saturating there could never be decremented
      back to zero and the cell would stay lit for the rest of the match - a fog that wrongly
      reveals is the worst failure this system has, and the second byte buys it away outright. The
      snapshot's run encoder is templated on the value type for it.
      ALLIED VISION IS SHARED BY WRITING, NOT BY UNION AT READ, which the acceptance left open. A
      viewer stamps its disc into every seat of its own alliance. Union at read was rejected
      because explored is history: a commander would otherwise not see ground their ally scouted an
      hour ago unless the history were unioned too, and unioning history on every read costs more
      than writing it once.
      STAMPS ARE NEW STATE and they are the heart of it. A cell is visible while its count is
      positive, so a viewer that moves must un-count exactly the disc it counted, and the only way
      to un-count exactly is to remember - the cell, and the radius, since a sight upgrade changes
      it. They are in the hash and in the snapshot at version 8.
      A HEIGHT DELTA DROPS THE WHOLE FOG. A stamped disc was worked out against the heights as they
      were; un-counting it against new ones takes viewers off cells that were never counted. So
      Sim::FlattenTerrain resets visibility and the budget refills it over the next ticks. The cost
      is that the explored history goes with it, which a test pins as a known consequence rather
      than a surprise; narrowing the reset to the cells the delta touches is work for S4, which is
      what will actually be calling it.
      Seat.h's fogViewers/fogState and its ghost vector became FogGrid and GhostStore, so the
      invariants - two arrays of one size, ghosts ascending by id - live with the data instead of
      with every caller. Seat.cpp is gone; SizeFog is FogGrid::Resize.

  - id: S10
    title: Sim combat - targeting, stances, hit rolls, projectiles and splash, the damage formula, destruction, experience, retreat
    intent: >
      The damage model of GameDesign.md §8 exists exactly as written, with direct fire decided at
      the shot and indirect fire at the impact, so that the matrix the balance script checks is
      the matrix the simulation applies.
    project: Sim
    depends_on: [S8, S9, S4]
    files:
      - Sim/Targeting.h
      - Sim/Targeting.cpp
      - Sim/Weapons.h
      - Sim/Weapons.cpp
      - Sim/Projectiles.cpp
      - Sim/Damage.h
      - Sim/Damage.cpp
      - Sim/Experience.h
      - Sim/Retreat.cpp
      - Sim/OrderValidation.cpp
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Tests/SimTests/DamageTests.cpp
      - Tests/SimTests/CombatTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - "A weapon acquires a target its seat can see within its long range by the stance and target-priority rules, fires at its rate, and rolls a hit from the simulation Random against the short or long hit chance plus rank and research percentages; direct fire resolves at the shot and emits a projectile event for the client; a Tower takes a target-priority stance only"
      - "Indirect fire (the mortar) needs a spotter with the target in its own sight, has a minimum range, launches a Projectile that lands at the predicted cell after its flight ticks and damages everything within its splash radius at impact, so a moving target walks out from under it"
      - "Damage is scaled = damage x modifier[weaponClass][targetClass] / 100 and dealt = max(scaled - armour x armorFactor[weaponClass] / 100, scaled / 3), against kinetic or thermal armour per weapon class, structures using their strength class; a device or structure at zero hit points is destroyed, a structure leaving a wreck, and the killer gains experience toward its eight ranks"
      - "Retreat: a device under its retreat threshold breaks off and returns to the nearest repair bay or repair device only if one is within 60 cells, else holds and fights; M1 has neither structure nor module, so the rule is tested with a stub repair point and the order kind ReturnToRepair is applied"
      - "Tests: the damage formula against hand-computed values (a cannon against tracks with 25 armour deals 47; the minimum third; the laser's half armour and artillery's zero via the table), the spotter rule, an indirect miss on a moving target, rank accrual, retreat only within 60 cells, two Sims fight identically"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      Stages 8, 9 and 10 of the tick are no longer empty. Sim/Targeting acquires and fires,
      Sim/Weapons holds what a weapon is and flies the shells, Sim/Damage applies what the two
      decided, Sim/Retreat decides who leaves, and Sim/Experience says what a kill is worth.
      DAMAGE IS A QUEUE AND NOT A WRITE AT THE SHOT, which is the one structural decision here.
      Two shooters that both fire at one device in a tick both fired while it was alive, because
      both fired before either shot landed; a write at the shot makes the second shooter's tick
      depend on which of the two the walk reached first. Stages 8 and 9 queue, stage 10 applies,
      and the order of the walk stops mattering. The queue is scratch - filled and emptied inside
      one Advance - so it is neither hashed nor carried.
      ONE VISIBILITY TEST IS BOTH THE SIGHT RULE AND THE SPOTTER RULE. GameDesign.md 8 asks that a
      target be visible to someone on the commander's side, and that indirect fire "additionally"
      need a spotter. The fog grid of S9 is already the union of every viewer of the alliance, so
      the two are one subscript. What makes them bite differently is the tables: a cannon reaches
      16 cells on a chassis that sees 20, so a direct shooter always sees its own target; a mortar
      reaches 28 and sees 20, so it cannot fire at its longest range unless something else of its
      commander's is looking. The test that proves it is the one that takes the spotter away.
      THE OPTIMAL RANGE STANCE HAD TO BE DEFINED AND NOT JUST IMPLEMENTED, and the mortar is why.
      Optimal means "hold fire until the short band", and the mortar's short range IS its minimum
      range - both six cells - so the first rule written made it unable to fire at all. A weapon
      with no short band to close into opens at its long range under both stances: "engage at
      optimal range" cannot mean "engage inside the range you may not fire within". Caught by the
      spotter test, not by reading.
      THE TARGET PRIORITY IS THIS TASK'S AND IT IS NOW OpenQuestions.md Q24, OPEN. GameDesign.md 8
      says structures "take a target-priority stance only" and never says what the priorities are,
      and there is no such axis in the four the order set carries. The rule shipped is one for
      every shooter: keep the target you have while it is alive, visible and in range - a weapon
      that re-chose every tick would spray a crowd and kill nothing - and otherwise the nearest,
      devices before structures, ties to the lower id. Whether a commander may CHOOSE a priority
      is the open half, and it is an axis, an operand and a panel control, so it is m2-skirmish's.
      SPLASH HITS ENEMIES AND NOT THE SHOOTER'S OWN SIDE. The acceptance says an impact "damages
      everything within its splash radius", which reads as area-rather-than-target; GameDesign.md
      nowhere names friendly fire, and an area weapon is not where one arrives by implication. One
      condition in Sim/Weapons.cpp if the owner wants it the other way, and it is called out there.
      A SHELL CARRIES WHAT IT WAS FIRED WITH rather than looking it up when it lands: the chance
      and the damage are on the Projectile, so a shot fired by a device that dies in the two
      seconds it is in the air still lands as it was fired. Snapshot version 13, which also brings
      a structure's weapon reload - a field of its own and not a second use of the production
      countdown, which a mod that armed a factory would otherwise have the two share.
      MEASURED: two scouts four cells apart settle it on tick 156, against the ~160 the tables
      allow (100 hit points, 4 damage a hit through 5 armour, 80 percent every five ticks); eight
      scouts in a line fight down to four in 400 ticks, and two Sims agree on every one of them.
      SimTests is 134 methods and green; the snapshot of the three-seat fixture is 1,689 bytes.
      TWO INHERITED TESTS HAD TO CHANGE, both because a placeholder became real. SimTests asserted
      "stage 8 draws once a tick", which was the unconditional draw S1 put there to exercise the
      stream from M0; stage 8 now draws only when it has a hit to roll for, so the test asserts
      that instead and that Sim::Roll is the one door to the stream. And SnapshotTests' fixture
      gave its projectile nine ticks of flight and then ran fifty ticks, so stage 9 landed it and
      the hash test dereferenced a shell that was gone - the same class of fault as the furnishing
      S6 had to move after the ticks. It carries nine hundred now, and the fixture covers the two
      fields version 13 added.
      THE MORTAR TEST IS WHY S7's ABSTRACTION IS WORTH KNOWING ABOUT. The first version had the
      target run north half a cluster from a doorway, so its route detoured ten cells toward the
      mortar; a shell fired over fourteen cells leads its target by less than the splash and
      connects, and the test read as "the miss rule is broken" when the miss rule was fine. The run
      is along a cluster's own first column now, where the doorways are, and the test asserts the
      device really did run straight - so the day the route changes, it says so rather than
      quietly measuring something else.
      ALSO DONE HERE: the duty S8 wrote down for this task - a destroyed device's planner request
      is cancelled, so a job does not outlive its device and take budget from the ones still alive.
      Attack and ReturnToRepair now walk, which needed Sim/Movement.cpp: what makes them different
      from a Move is only who sets the destination, so the walk needs no case of its own.
      NOT DONE, and it is the content's rather than this task's: M1 ships neither a repair bay nor
      a repair module, so RepairPointNear always answers no on shipped content and every device
      holds and fights. The rule is written and tested against a fixture that defines both, which
      is what the acceptance asks for. The repairing itself, and the walk back to the guard
      position afterwards, belong to the task that ships the bay.

  - id: S11
    title: Sim victory and match flow - annihilation, the survival clock, surrender
    intent: >
      A match ends by the conditions of GameDesign.md §2 and the seats know it, so that the
      slice has an ending and the AI test has a termination.
    project: Sim
    depends_on: [S4, S5]
    files:
      - Sim/Victory.h
      - Sim/Victory.cpp
      - Sim/Seat.h
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Tests/SimTests/VictoryTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - Annihilation eliminates a seat when it has no structure and no device carrying a builder module; the last side with an uneliminated seat wins; Survival ends at the clock with the side that extracted the most power winning; Surrender eliminates the seat
      - Victory state per seat is Playing, Won, Lost or Eliminated, checked at stage 12, and the match stops advancing once decided
      - "Tests: annihilation with a surviving builder, the last-side rule with two allied seats, survival at the clock, surrender"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      Stage 12 is Sim/Victory.{h,cpp}. GameDesign.md §2 gives each condition a sentence and stage 12
      needs a rule, so §2 now carries the six readings this task had to take; the two that were
      real decisions rather than restatements are these.

      SURVIVAL IS DECIDED ON WHAT WAS EXTRACTED, NOT ON WHAT IS HELD. "The side that extracted the
      most power wins" cannot be answered by the stockpile: a commander who spent what he dug would
      lose to one who sat on it, and the stockpile cap throws income away, so two economies that
      differ by a factor of three can end the clock at the same number. Seat::extractedHundredths is
      a running total that stage 2 adds the served extractors' output to before the cap and before
      the command post's trickle - the command post generates, it does not extract. 64 bits, because
      it only ever grows.

      A SEAT THAT NEVER HELD A BASE CANNOT BE ANNIHILATED. The rule as the design states it -
      neither a structure nor a builder - is true of a seat the lobby has just created, so without a
      latch a match would end before its base level was placed, and so would every suite that
      advances a tick over a world it has not filled. Seat::everHeldBase is that latch, set by the
      same walk that tests the rule. It is also what keeps a combat test of two scouts from ending
      as a double annihilation: neither seat ever held a structure or a builder, so neither is out.

      WHAT WENT IN THE OTHER DIRECTION. Seat::defeated is gone rather than joined: the victory state
      is the same fact spelled better, and Seat.h's own header warns against a second field that can
      disagree with the first. Seat::Defeated() is the question spelled out, and it means Eliminated
      - out during play - which is not Lost, which is being on the board at the end and not winning.
      A decided match no longer advances at all: Sim::Advance returns at once, so the tick, the
      stream, the world and the hash stay as the deciding tick left them.

      Dominance still falls through to the annihilation rule. It needs 60% of a landscape's deposits
      held for ten continuous minutes and ships with the first Large landscapes (GameDesign.md §2),
      so a match set to it is played rather than refused.

      Snapshot version 14 and ADR-003's table with it: a seat is 157 bytes and the three-seat
      fixture 1,716. Measured: SimTests 143 passed, 0 failed; VictoryTests is 9 of them.

  - id: S12
    title: Sim scripted AI seat
    intent: >
      The scripted opponent of the slice is the planner of TechnicalDesign.md §7 with one
      hard-coded behaviour list, so that it is the M2 AI's skeleton rather than a throwaway, and
      an AI-versus-AI match from a seed is the test that exercises every system at once.
    project: Sim
    depends_on: [S6, S10, S11]
    files:
      - Sim/AiSeat.h
      - Sim/AiSeat.cpp
      - Sim/AiBlackboard.h
      - Sim/AiBlackboard.cpp
      - Sim/AiDesigner.h
      - Sim/AiDesigner.cpp
      - Sim/Sim.cpp
      - Sim/Sim.vcxproj
      - Sim/Sim.vcxproj.filters
      - Tests/SimTests/AiTests.cpp
      - Tests/SimTests/SimTests.vcxproj
      - Tests/SimTests/SimTests.vcxproj.filters
    acceptance:
      - "The AI seat observes through its own fog grid and ghost store into a blackboard: known enemy structures, a threat map at cluster resolution, its economy and army composition; it runs at stage 11 within a budget and emits orders for tick t plus its delay through the ordinary queue"
      - "The behaviour list is fixed for M1: claim the nearest deposits with extractors and a generator within 48 cells, build a factory and a lab, research by auto-research, design by AiDesigner (expected damage per power against the seen enemy composition through the damage matrix, best design per role), produce to a composition, attack-move with a group when its strength exceeds a threshold, rebuild what is lost"
      - No wall time, no float, no thread; the AI's decisions are a function of the state and the tick
      - "Tests: AI against AI on GameData/Landscapes/Slice.json from a fixed seed reproduces hash for hash in two Sims; within 12,000 ticks both seats have a served extractor, a factory and a lab and have fielded a device, and first contact has occurred; a 36,000-tick run reaches a decided victory state, run in a test that CI can afford (its duration is recorded in the notes)"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      Sim/AiBlackboard observes, Sim/AiDesigner scores and Sim/AiSeat decides, at stage 11. The
      blackboard is entirely DERIVED and rebuilt every decision, so the AI adds nothing to the state
      hash and nothing to the snapshot - a blackboard that were state would be a second copy of the
      world two hosts could disagree about. No wall time, no float, no thread; two Sims from one
      seed agree hash for hash over 3,000 ticks of a real match, which is the test that would catch
      any of the three.

      THREE ORDERINGS IN THE BEHAVIOUR LIST COST A RUN EACH TO FIND, and they are the substance of
      this task rather than the code around them. (1) A GENERATOR GOES UP BEFORE THE SECOND
      EXTRACTOR: GameDesign.md §4 says an extractor earns nothing until a generator reaches it, and
      the first version claimed four deposits first - four extractors' worth of power spent on an
      income of nothing, and a match where neither commander ever extracted a single unit. (2)
      NOTHING NEW IS PLACED while there are more unfinished sites than builders to work them, with a
      slack of one: without the rule one truck is spread over six plans and finishes none, and
      without the slack a site no builder can reach - a deposit on a summit - stops the commander
      building anything else for the rest of the match. (3) THE ATTACK BEHAVIOUR IS EVALUATED EVERY
      DECISION rather than taking its turn: idle fighters are not a resource the base is competing
      for, and behind the queue it never ran at all, because a factory with something to produce
      answers first every decision for the whole match.

      TWO THINGS THE SIMULATION MADE THE AI LEARN. A deposit it has never SEEN cannot be built on -
      CheckPlacement refuses an unexplored footprint - so a builder walks to it first, which is a
      scouting behaviour the acceptance did not ask for and the placement rule requires. And a group
      is sent FOUR CELLS SHORT of what it attacks: the cell a commander starts on is where his
      command post stands, a structure's cells are impassable, and a route into the middle of a base
      comes back unreachable, so an army ordered at one stands where it was built. A group and not
      the whole army, too: the planner's budget is 2,000 nodes a tick shared across every request,
      so thirty cross-map searches at once each get seventy nodes and none of them finishes.

      NOTHING IN THE TREE OWNS BASE-LEVEL PLACEMENT YET. GameDesign.md §2's "nothing but a builder
      and a command post" is placed by the TEST here, because m1-vertical-slice/G1 is the task that
      starts a match. The test also has to put the builder on ground its drive can stand on: a
      device dropped on a cliff has no component in the cluster graph, every route it is given comes
      back unreachable, and it stands there for the whole match - which looked like an AI fault for
      three runs and was a placement one.

      MEASURED, on GameData/Landscapes/Slice.json from seed 20260919 with the real GameData tables:
      first contact on tick 6,003; at 12,000 ticks both commanders hold a served extractor, a
      generator, a factory, a lab and devices they built themselves, and both have completed
      research. The 36,000-tick run reaches a decided victory state with 20,318 and 40,533 power
      extracted. DURATION, as the acceptance asks: 14 s for the 12,000-tick run and 54 s for the
      36,000-tick one, on the Linux harness at -O0; CI's MSVC Debug build is the number to watch,
      and the suite's step was 2m45s before this task.

      The behaviour list and the blackboard's fields are documented in the header comment of
      AiSeat.h, not in a prose file: project directories are flat (AGENTS.md §2).

      Measured: SimTests 148 passed, 0 failed, of which AiTests is 5.

  - id: N1
    title: Net wire records and encodings
    intent: >
      The wire form of every replicated thing (TechnicalDesign.md §5.3) exists as fixed-width
      integer aggregates with pinned byte layouts, including the fog delta the client needs to
      draw its own commander's fog, so that host and replica share records and nothing else.
    project: Net
    depends_on: [S1]
    blocked_by:
      - m0-foundation/T10
      - m0-foundation/T14
    files:
      - Net/Records.h
      - Net/Records.cpp
      - Net/Messages.h
      - Net/Messages.cpp
      - Net/Net.vcxproj
      - Net/Net.vcxproj.filters
      - Tests/NetTests/RecordsTests.cpp
      - Tests/NetTests/NetTests.vcxproj
      - Tests/NetTests/NetTests.vcxproj.filters
    acceptance:
      - "DeviceState, StructureState, WreckState, FeatureState, SeatState, Event, DesignState, FogDelta (runs of cells with their new fog state, for the commander's own grid) and OrderMessage are plain aggregates of fixed-width integers; a position on the wire is quantised to a quarter of a world unit and, in a change record, sent as a delta against the baseline under a field mask; a device change record is at most 14 bytes plus its mask by static_assert"
      - "Messages: Join, JoinAccepted (seat, settings, landscape definition, tick), JoinRefused (reason), Frame (sequence, baseline sequence, created, changed, removed, events, fog deltas), Fragment (frame sequence, index, count, bytes), Ack, Orders (sequenced), Heartbeat"
      - Every record writes and reads through Core's byte stream, and NetTests pins the exact bytes of one instance of each so a layout change is a visible diff, plus round trips and truncation cases
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      Net/Records.h is the wire form of a device, a structure, a wreck, a feature, a seat, a design,
      an event and a run of fog, and Net/Messages.h the eight datagrams they travel in. Positions
      quantise to a quarter of a world unit by FLOOR division, so that the step is uniform across
      zero rather than double-width at it, and come back as the middle of the quarter they named, so
      the round trip errs by half a step either way instead of a whole step downwards.

      A DEVICE'S ORDER AND ITS FOUR STANCES ARE ONE BYTE, and that is what makes TechnicalDesign.md
      §5.7's fourteen-byte changed record fit: seven orders times three fire, two range, three
      retreat and two movement stances is 252 of the 256 values a byte has, where the same five as
      bit fields need nine bits and would cost a second byte on every device that turned round. The
      four unused values are refused on read. Fourteen is then exactly the id, the six-byte position
      delta, the heading, the hit points and that byte, which a static_assert pins. The fields NOT
      in the mask - design, seat, rank and target - are the ones that rarely change, and a device
      whose rank or target changed is re-sent whole rather than widening every frame's worst case.

      A NAME COLLISION THAT HAD TO BE SETTLED. TechnicalDesign.md §5.3 names the wire record
      StructureState and Sim already had an enumeration of that name; one namespace cannot hold
      both. The simulation's is now StructurePhase, which is the better name for what it is, and the
      wire keeps the name the design gives it. A mechanical rename over 22 files, no behaviour
      changed, SimTests unchanged at 143 passing.

      THE WIRE DOES NOT SHARE THE SNAPSHOT'S WRITER, deliberately. Net writes the settings and the
      landscape definition itself: they are two streams with two versions, so a snapshot that gained
      a field would otherwise break every client of a host that can save, and they carry different
      payloads - a snapshot carries the flatten deltas and the wire must not, because the terrain
      under an unscouted base is not public (§5.2).

      Measured: NetTests 14 passed, 0 failed. Every record's bytes are pinned by hand from the
      layout rather than recorded from a run, every record round-trips, and every prefix shorter
      than a whole record is refused - 211 truncation cases across the nine.

  - id: N2
    title: Net host endpoint - interest sets, delta frames against acked baselines, history, fragmentation, join and rejoin; the network ADR
    intent: >
      The host publishes to each client exactly its commander's interest set (TechnicalDesign.md
      §5.2 to §5.4), encoded against what the client acknowledged, so that the fog of war is
      enforced by the host and a lost datagram costs nothing but a larger next frame. The
      interest test is a security property and is written here.
    project: Net
    depends_on: [N1, S9]
    files:
      - Net/Host.h
      - Net/Host.cpp
      - Net/Interest.h
      - Net/Interest.cpp
      - Net/FrameEncoder.h
      - Net/FrameEncoder.cpp
      - Net/ClientHistory.h
      - Net/Fragmenter.h
      - Net/Fragmenter.cpp
      - Net/Net.vcxproj
      - Net/Net.vcxproj.filters
      - Tests/NetTests/HostTests.cpp
      - Tests/NetTests/InterestTests.cpp
      - Tests/NetTests/FragmenterTests.cpp
      - Tests/NetTests/NetTests.vcxproj
      - Tests/NetTests/NetTests.vcxproj.filters
      - Design/ADR/README.md
      - Design/ADR/ADR-<next>-replication-protocol.md
      - Design/TechnicalDesign.md
    acceptance:
      - "Interest per client per publish, from the seat's fog grid: every object in a visible cell of the seat or an ally, every object the seat owns, ghosts from the ghost store when a structure leaves or re-enters visibility, the seat's own SeatState and fog deltas, and at join the landscape definition and settings; flatten deltas travel only inside a structure's record or ghost"
      - Host publishes a frame per client every second tick, delta-encoded against the newest frame the client acknowledged, keeps 32 frames of history per client, sends a full frame when the baseline has fallen out of history or at join, and splits any frame over 1,200 bytes of payload into numbered fragments
      - "Join, rejoin with the same token, leave and timeout per §5.4, with the seat handed to AI control after the grace period MatchSettings gives; ReceiveOrders() hands validated-shape orders to Sim's queue for the next tick"
      - "The interest test: in a scripted match over LoopbackTransport, for every frame to every client, no record names an object outside the seat's visibility, own objects or ghost store, and no fog delta names another seat's cells"
      - "The conversation tests over loopback: join and full frame, deltas against an acked baseline, a lost frame recovered by the next delta, a full frame after history exhaustion, fragmentation and reassembly of a large frame, a slow client dropped to AI"
      - The network ADR records the model as decided, the publish rate, history length, quantisation and fragment size, the leak posture of §5.2, and the bytes per frame measured in the tests at 100 and 600 visible objects
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      Net/Host is the host end, Net/Interest the filter it publishes through, Net/FrameEncoder what
      turns a simulation and an interest set into one client's frame, Net/ClientHistory the 32 it
      keeps, and Net/Fragmenter what splits a frame too large for a datagram. ADR-012 records the
      model and every number, measured.

      THE INTEREST SET IS THE SECURITY BOUNDARY AND THE ENCODER IS SHAPED AROUND IT. The encoder
      reads the simulation only for ids the interest set names, so the only way a record of an
      unseen object could reach a frame is through a set that named it - which is what makes the
      guarantee testable rather than reviewable. InterestTests plays a scripted match with two
      commanders who never see each other and asserts that neither was told of a seat, a device, a
      structure or a seat state but his own, and that the fog each was sent equals HIS OWN grid cell
      for cell rather than the alliance's union.

      ONE THING §5.3 DOES NOT HAVE AND THE PROTOCOL NEEDED. The acknowledgement rides every datagram
      the client sends, and a frame the client DISCARDS now forces another one. Without it: the
      client applies frame N, its acknowledgement is lost, the host goes on encoding against N-1,
      the client goes on discarding what arrives because the baseline is not what it holds, and the
      two talk past each other until the next heartbeat twenty ticks later. A discarded frame is
      evidence the host's idea of this client is stale, so it is exactly what should make the client
      say so again. Found by a test that dropped a third of the link, not by reading.

      MEASURED, and it is what ADR-012 quotes: a full frame at 100 visible objects is 3,653 bytes
      and a delta with half of them moving 637 (6 KB/s at 10 Hz); at 600 objects, 19,153 and 3,387
      (33 KB/s). A fifth under TechnicalDesign.md §5.7's arithmetic, because a device that did not
      move sends nothing and a changed one sends only what its mask names. The 32-frame history is
      therefore 600 KB a client at 600 objects, which is the figure to reopen the history length
      against.

      MatchSettings gains rejoinGraceTicks, which the acceptance asks for and the lobby had no field
      for; GameDesign.md §10 asks for a grace period and gives no number, so the default is two
      minutes. Snapshot version 15 carries it.

      Measured: NetTests 49 passed, 0 failed; SimTests still 143.
      A DEFECT OF THIS TASK WAS FOUND AND FIXED IN m1-vertical-slice/G1a ON 2026-09-19. The fog was
      the one field encoded against a rolling per-client grid that advanced on every ENCODE rather
      than against the baseline the client ACKNOWLEDGED, so every frame Net/Client.cpp dropped -
      which is every frame published while an acknowledgement is in flight, not a lossy-link case -
      lost its fog runs for good and the commander's map diverged for the rest of the match. It made
      the first capture of a real match come back entirely black. FrameRecord now carries the runs
      its frame sent and ClientView::fog advances only on acknowledgement; G1a's notes carry the
      whole account, and ConvergenceTests now compares the fog cell for cell rather than by size.

  - id: N3
    title: Net client endpoint - frames in, acknowledgements, the reliable order stream
    intent: >
      The client end of the conversation (TechnicalDesign.md §5.5) receives frames and
      fragments, acknowledges what it applied, and delivers orders reliably through loss, so
      that Replica has frames and the host has orders with nothing else in between.
    project: Net
    depends_on: [N1]
    files:
      - Net/Client.h
      - Net/Client.cpp
      - Net/Reassembler.h
      - Net/Reassembler.cpp
      - Net/ReliableStream.h
      - Net/ReliableStream.cpp
      - Net/Net.vcxproj
      - Net/Net.vcxproj.filters
      - Tests/NetTests/ClientTests.cpp
      - Tests/NetTests/ReliableStreamTests.cpp
      - Tests/NetTests/NetTests.vcxproj
      - Tests/NetTests/NetTests.vcxproj.filters
    acceptance:
      - Client joins through the transport, reassembles fragments and discards a frame with one missing, hands complete frames to a sink in sequence order and acknowledges the newest applied sequence in every datagram it sends
      - "ReliableStream carries orders with sequence numbers, cumulative acknowledgements in every datagram, and resend after a round-trip estimate; duplicates are dropped and delivery is in order; the host side of the stream lives in N2's Host and is exercised here"
      - "Tests over loopback with seeded loss, duplication and reorder: every order arrives once and in order; a frame with a lost fragment is skipped and the next delta recovers; the round-trip estimate converges"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      Net/Client is the client end, Net/Reassembler puts a fragmented frame back together and
      Net/ReliableStream carries the orders. Both halves of the stream are one class, because a
      conversation has a sending and a receiving half at both ends and the acknowledgement one half
      produces is the one the other consumes; N2's Host holds the other instance.

      TWO THINGS THE DESIGN DID NOT SPELL OUT AND THE CODE HAD TO. Karn's rule: an order that was
      SENT TWICE measures no round trip, because there is no telling which copy the acknowledgement
      answers, and taking the last send would measure a trip that never happened - a lossy link
      otherwise talks itself into an estimate far shorter than the link, resends more, and gets
      lossier. And the estimate is held in EIGHTHS of a tick: in whole ticks a smoothed average
      over eight samples cannot move at all for a sample within eight of the estimate, so it would
      sit on its initial guess for ever. Measured: it walks from the initial 8 down to 5 on a link
      of 4.

      WHEN A PARTIAL FRAME IS GIVEN UP ON. §5.3 says a frame with a fragment missing is discarded,
      and the question it leaves is when. Two answers, both here: a third frame arriving while two
      are part-way through displaces the older, and a frame that COMPLETES discards every partial
      older than itself - the host encodes its next frame against what this one acknowledged, so
      the missing piece would complete a frame nothing is a delta from any more. No timer, and
      nothing to tune.

      A frame is applied when it is full or when its baseline is exactly what this client last
      applied; anything else is dropped and the unmoved acknowledgement is what tells the host. The
      Join is the one message with no acknowledgement of its own, so it is the one repeated on a
      schedule of its own; everything else rides the frame acknowledgement.

      Measured: NetTests 28 passed, 0 failed. Sixty orders over a link losing a twelfth, duplicating
      a twentieth and reordering a tenth arrive exactly once and in order; twenty orders over the
      real LoopbackTransport dropping a fifth take 47 datagrams and 12 resends.

  - id: R1
    title: Replica world and interpolation, with the convergence tests
    intent: >
      The client's world model (TechnicalDesign.md §2, §5) applies frames into a replica that is
      a faithful reading of the host's interest set and interpolates it 100 milliseconds behind,
      so that the client draws what the host said and a test proves it converges with and
      without loss.
    project: Replica
    depends_on: [N3]
    files:
      - Replica/Replica.h
      - Replica/Replica.cpp
      - Replica/ReplicaObject.h
      - Replica/Interpolation.h
      - Replica/Interpolation.cpp
      - Replica/DesignStore.h
      - Replica/Replica.vcxproj
      - Replica/Replica.vcxproj.filters
      - Tests/ReplicaTests/ConvergenceTests.cpp
      - Tests/ReplicaTests/InterpolationTests.cpp
      - Tests/ReplicaTests/ReplicaTests.vcxproj
      - Tests/ReplicaTests/ReplicaTests.vcxproj.filters
      # Deleted: the placeholder suite, which the definition of done says the first real test in a
      # project replaces (ImplementationPlan.md §4).
      - Tests/ReplicaTests/SuiteSmoke.cpp
    acceptance:
      - Replica applies created, changed and removed records, keeps ghosts for structures, holds the seat's own state, designs by DesignState, and its commander's fog grid from fog deltas
      - Interpolation keeps the last two frames per object and evaluates positions and headings at a time 100 milliseconds behind the newest frame, which is the first float conversion and the only one, never outside the segment between the two frames
      - "Convergence test: a real Sim, N2's Host and N3's Client over LoopbackTransport in ReplicaTests; at every acknowledged frame, with and without seeded loss, the replica equals the host's interest set for that seat object for object"
      - "Tests also cover: a ghost persists after the structure leaves visibility and updates when seen again; interpolation stays on the segment; a rejoin rebuilds the replica from a full frame"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE REPLICA STARTS ITSELF FROM THE CLIENT, and that is the finding of this task rather than a
      convenience. The first version took the seat and the landscape size through a Begin() the
      caller made after joining - and the convergence test came back with an EMPTY replica. Net's
      Client reads the JoinAccepted and delivers the first full frame inside the SAME Advance
      (Net/Client.cpp), so there is no moment between the two to call anything in: Begin ran after
      the first frame had been applied and cleared it, and every frame after that was a delta the
      replica had no baseline for. G1a would have hit the identical trap with no signal. The
      constructor now takes the Client and Apply adopts the seat and the landscape on every full
      frame, which also makes a rejoin into a different seat fall out rather than need a rule.
      A DEVICE THAT SENT NO CHANGE DID NOT MOVE, which the protocol says by omission: an empty
      change is never sent. Without accounting for that, a device standing still for a hundred ticks
      and then moving hands Evaluate a segment spanning the whole still period and is dragged across
      it in the one frame that ends it. Apply stamps the held position at the last frame THIS
      REPLICA APPLIED before pushing the new sample - not a guess, and not an assumption about the
      publish rate either, only what this replica observed.
      A GHOST NEEDS NO SECOND COLLECTION. The host sends a remembered structure in the same list as
      a standing one, at its last-seen state with no hit points (Net/FrameEncoder.cpp's WireGhost),
      and Net/Interest.h guarantees an id is never in both. So the id never leaves the replica and
      the flag is one comparison in the line that takes the record.
      MUTATION TESTING FOUND TWO HOLES IN THE FIRST SUITE, and they are the reason it has seven
      tests and not five. With device removals ignored the suite still passed - nothing in it ever
      died. With every position delta thrown away it still passed - nothing in it ever moved. The
      removal test and the walking test exist because of those two runs, and each carries the
      assertion that would fail again.
      WHAT IS ASSERTED WHILE SOMETHING MOVES IS NOT FULL CONVERGENCE. A replica is one publish
      behind a moving world - that is what interpolating 100 ms back is for - so a replica equal to
      the simulation mid-stride would mean the client had guessed ahead, which Interpolation.h
      refuses to do. The walking test holds the object set and the following, and makes the full
      claim once the world stops.
      THE SIMULATION WAS RIGHT AND THE TEST WAS WRONG, twice. Razing a seat's only structure
      annihilates it, S11 ends the match and Sim::Advance stops on m_finished, so the frames the
      test was waiting for never came; the removal test now gives the seat two. And the first
      settling loop stopped the simulation and pumped the network alone, which the host answers by
      republishing until acknowledged - true, but a state the game is never in, and it took the
      sequence numbers twenty publishes past a tick that never changed. Settling is ordinary ticks
      now.
      Verified in the session on Linux: CheckTaskDag, CheckFormat and CheckProjectFiles clean;
      -fsyntax-only over Replica.cpp and Interpolation.cpp; and the whole of ConvergenceTests built
      against the real Sim, Net's Host and Client and the LoopbackTransport and RUN - seven tests,
      zero failures, including the seeded-loss run at 250 per mille. The MSVC build, clang-tidy and
      InterpolationTests under vstest are CI's.
      AND CI HAS NOW RUN THEM. clang-tidy 22.1.8 found two things clang-tidy 18 was not asked about
      in this session, both fixed in the head this note sits on: Replica's constructor was noexcept
      while default-constructing four maps, three vectors and a DesignStore, every one of which
      allocates (bugprone-exception-escape), and the heading wrap cast through std::int8_t
      (bugprone-signed-char-misuse). The heading step is arithmetic on the unsigned difference now,
      and the replacement was checked against the original over all 65,536 heading pairs with no
      mismatch rather than reasoned about.
      DONE ON THE EVIDENCE OF RUN 35435306312 (head 530d206): the MSVC build, vstest over every test
      DLL, and clang-tidy 22.1.8 over all 162 translation units all ran. The two translation units
      that failed that run were m1/K3's - Tests/ClientTests/UiLayoutTests.cpp and Client/UiPanel.cpp
      - and clang-tidy reached them only because the build and the tests before it had passed, which
      is what makes Replica's half of that run evidence rather than an assumption.

  - id: R2
    title: Replica render view, model composition at markers, and picking
    intent: >
      The replica becomes a plain list of what to draw (TechnicalDesign.md §6.3) with a device
      composed from its chassis, drive and module models at their markers, plus the picking that
      turns a click into an object, so that the executable holds no logic worth a test.
    project: Replica
    depends_on: [R1, C4, C6]
    blocked_by:
      - m0-foundation/T20
    # THE FILE LIST REACHES OUT OF Replica\ ON PURPOSE. The acceptance below asks RenderView to
    # carry a selection flag, construction progress, a colour INDEX rather than a packed colour and
    # a wreck-or-projectile distinction (Design/Interface.md §11 row 2), and RenderView is Core's.
    # Client/GeometryPass.cpp packs teamColor straight into the instance buffer and
    # Client/ModelBuffers.h mirrors the field, so the index has to be resolved against C6's
    # commander colours somewhere, and the geometry pass is where the palette is known once per
    # match rather than once per instance. This is a three-layer change, not a Replica-only one.
    files:
      - Core/RenderView.h
      - Client/GeometryPass.h
      - Client/GeometryPass.cpp
      - Client/ModelBuffers.h
      - Client/Client.vcxproj
      - Replica/RenderViewBuilder.h
      - Replica/RenderViewBuilder.cpp
      - Replica/ModelComposer.h
      - Replica/ModelComposer.cpp
      - Replica/Interpolation.cpp
      - Client/Shaders/GeometryVS.hlsl
      - Replica/Picking.h
      - Replica/Picking.cpp
      - Replica/Replica.vcxproj
      - Replica/Replica.vcxproj.filters
      - Tests/ReplicaTests/RenderViewTests.cpp
      - Core/FixedPoint.h
      - Tests/ReplicaTests/PickingTests.cpp
      - Tests/ReplicaTests/ReplicaTests.vcxproj
      - Tests/ReplicaTests/ReplicaTests.vcxproj.filters
    acceptance:
      - "RenderViewBuilder fills Core's RenderView each frame: for every device and structure the replica holds, one instance per model part with its interpolated transform, the seat's colour index, a rank badge, a selection flag and, for a structure under construction, its progress; wrecks as one instance each; the terrain chunks the structures applied this frame flattened; the commander's fog grid for the fog pass and the minimap"
      - "FEATURES AND PROJECTILES ARE NOT THIS TASK'S, and the line above says so rather than leaving them implied. This acceptance asked for both before anybody checked whether the content existed: Sim/Feature.h's `design` indexes a feature table Content does not have, and no row anywhere says what a shot looks like. C7 and C8 own them, each carrying its own RenderViewBuilder change, and each depends on this task rather than blocking it - so the four sources that have data ship now and neither gap is lost"
      - ModelComposer places a drive model at each of the chassis's MarkerDrive markers and a module model at each MarkerMount, carrying the module's MarkerMuzzle forward, so a device is a tree of three model kinds and the model count is a sum, not a product
      - Picking finds the nearest replica object under a screen ray and the objects inside a screen rectangle, as pure functions over the replica and the camera matrices
      - "Core/RenderView.h carries what Design/Interface.md §11 row 2 asks of it: a selection flag, construction progress, a commander colour INDEX in place of the packed teamColor, and a wreck-or-projectile distinction; GeometryPass takes C6's eight commander colours once and resolves the index, so the palette is a table rather than a number in a vertex buffer, and Interface.md §11 row 2 is struck"
      - "Tests: the composition of a heavy with two modules yields the expected instance list; a construction site scales; a ray picks the nearer of two overlapping devices; a rectangle selects only own devices"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      DONE SO FAR: Core/RenderView.h carries everything Design/Interface.md §11 row 2 asks of it -
      RenderInstanceKind, a commander colour INDEX in place of the packed teamColor, buildPercent,
      selected and a per-row draw scale - and Client/GeometryPass takes C6's eight colours once per
      match and resolves the index. Replica/ModelComposer composes a device from its chassis, drive
      and module models at the chassis's MarkerDrive and MarkerMount markers, carries each module's
      MarkerMuzzle forward through two frames, and composes a structure with its modules. Verified
      on Linux: 22 ReplicaTests (14 new), the whole convergence probe still green, clang-tidy 18
      clean over ModelComposer, Interpolation and RenderViewTests.
      THE KIND IS NOT A DRAWING SWITCH. TechnicalDesign.md §6.2's pass table draws "every device,
      structure, feature and wreck" with "team colour substituted", so GeometryPass does not read
      RenderInstanceKind at all; it is what picking and the minimap read, a wreck being something
      drawn that nothing selects.
      THE ROTATION IS Client/Shaders/GeometryVS.hlsl's Turn WRITTEN AGAIN and has to be: the
      chassis's own vertices are turned by that function, so a marker on the chassis has to land
      where they do. Heading zero looks along +z and grows toward +x. A sign the other way round
      mirrors every drive and every mount about the device and reads as a different model rather
      than as a defect, which is why a case pins it by hand-worked numbers rather than by
      recomputing the implementation.
      THREE CONVERSIONS HAD THREE HOMES AND NOW HAVE ONE. Client/ModelBuffers.cpp kept a WorldUnits,
      Replica/Interpolation.cpp a RADIANS_PER_HEADING_SUBSTEP, and the composer was about to write a
      third of each; both are in Core/RenderView.h now, beside the aggregate whose fields they
      produce, with the standing rule that nothing in Sim may call either of them.
      TWO GAPS IN THIS TASK'S OWN FILE LIST, found by reading rather than by guessing.
      (1) Client/Shaders/GeometryVS.hlsl HAS NO SCALE INPUT AT ALL - its instance attributes are
      origin, heading and teamColor - while every component and structure row carries
      modelScaleHundredths ("so that one model serves two rows at two sizes") and this task's own
      acceptance says a construction site scales. So GeometryInstance and the vertex shader both
      have to change, and the shader was not in the file list; it is now.
      (2) Content's ModelMarker carries a pitchBinaryAngle that RenderInstance cannot express, so a
      part on a pitched marker is drawn level. Every M1 marker is level - a drive sits on the ground
      and a mount faces out - so nothing is wrong on screen yet, but the content already records
      something the render path throws away. Raising it rather than adding a pitch nobody asked for.
      PICKING IS DONE: a world ray from an authored screen point, the nearest object under it, and
      the devices inside a screen rectangle, all as pure functions. §5's rules rather than this
      task's opinion - a click takes the nearest object of any seat because an enemy one is an
      inspection, a rectangle takes DEVICES OF ONE SEAT because "a rectangle never selects
      structures and never selects another commander's anything", and a wreck, a feature and a
      projectile are picked by nothing. Refusing a ray that starts on a panel stays the caller's;
      the interface already knows where its panels are.
      REPLICA HAS NO MATRIX TYPE AND MAY NOT HAVE ONE - it includes no platform header ever and
      CheckProjectFiles refuses one - so the camera arrives as sixteen floats, row major, row vector
      times matrix, which is what XMStoreFloat4x4 gives and what GeometryVS.hlsl's mul computes. The
      unprojection is here rather than in the executable BECAUSE that is where it can be tested,
      which is this task's whole intent.
      THE FIXTURE CAMERA WAS WRONG IN A WAY THAT LOOKED RIGHT. Its projection and its inverse agreed
      with each other and described a camera looking the other way; four cases failed and not one of
      them said which. The z row is written out with the reversed depth of ADR-005 spelled beside
      it now - near at 1, far at 0.
      MUTATION TESTING, TWELVE TRIED. Two survived and both were worth the finding. The rectangle
      case moved both axes at once, so a box that had lost its right-edge test entirely still
      excluded the device by its bottom edge; each of the four edges has its own case now. And the
      behind-the-ray early-out turned out NOT to be load-bearing - the distance comes out negative
      anyway and the caller drops it - so the comment says it is an early-out rather than a guard,
      which is what it had claimed to be.
      RenderViewBuilder IS BLOCKED ON CONTENT THAT DOES NOT EXIST, for two of the five things this
      task's acceptance asks it to fill. Found by reading before writing, and recorded rather than
      invented: a feature table and a projectile's appearance are content-schema decisions, and the
      last three of those became C6 and N4 rather than being guessed at inside a task.
      (a) FEATURES HAVE NO TABLE. Sim/Feature.h's `design` is documented "Row index in the feature
      table"; Content/ContentTree.h has no feature table and GameData ships no Features.json. So a
      feature arrives over the wire naming a row of nothing, and the builder cannot resolve its
      model. Nothing places features in a match yet either, so M1 may simply not need them - but the
      acceptance says "every replica object".
      (b) A PROJECTILE HAS NO APPEARANCE. TechnicalDesign.md §5.3 is clear that "projectiles and
      effects inside the set are sent as short-lived events rather than objects", which is what the
      acceptance means by "from events" - but no row anywhere says what a shot LOOKS like.
      ModuleDesc carries a model whose MarkerMuzzle is where the shot leaves, and no projectile
      model, no tracer, no lifetime. There is nothing to draw.
      THE THIRD ONE RESOLVED ITSELF AND IS WORTH RECORDING AS SETTLED. changedChunks looked blocked
      too - the replica holds no terrain heights and Net/Records.h sends none - until the reason
      turned up in that file: "a snapshot carries the landscape's flatten deltas and the wire must
      not, because the terrain under an unscouted base is not public (§5.2)". It is a fog boundary,
      not an omission. The client generates the landscape from the definition it was given at the
      join and flattens it under the structures it can SEE, which is exactly why StructureState
      carries `y`, "the flattened height under the footprint". So the builder computes the changed
      chunks from the structures it applied this frame, and the terrain a commander has not scouted
      stays unflattened on his machine, which is the point.
      RenderViewBuilder IS WRITTEN for the four sources that have data: devices at the pose
      Interpolation gives, structures at their footprint's CENTRE (a model is authored about its own
      origin, and a structure whose origin sat on a corner would draw a footprint away from where
      the simulation says it is), wrecks as one instance each - a device's wreck is its CHASSIS and
      not its whole tree, because the drives and the modules came apart with everything else - and
      the fog straight through, the replica's own bytes without a table.
      IT REMEMBERS WHAT IT HAS FLATTENED, which is the one piece of state in it. The replica says
      what structures there ARE and not which arrived this frame, and re-flattening every structure
      every frame would rebuild every chunk of a base on every frame of the match; the builder keeps
      what it last flattened and reports the difference, including for a structure that is GONE,
      because a razed building leaves its ground as it left it. ForgetTerrain() exists for the full
      frame a rejoin brings.
      A GHOST IS DRAWN AND NOT PICKED. It is what the commander remembers of a building he cannot
      see now, so a click on one would select something that may not be there; §5's inspection is
      for what is visible. The fog pass darkens it like everything else.
      TESTING IT NEEDED A DECISION AND THE DECISION IS RECORDED. Build() takes a Replica, which
      needs a Net::Client, which needs a host to have sent a JoinAccepted - the whole stack, which
      is what ConvergenceTests already stands up and what RenderViewTests deliberately does not. So
      the chunk arithmetic, which is the only part that is wrong by one chunk at an edge rather than
      wrong by a crash, is a FREE function and has eight cases; the walk itself is covered by the
      stack, and a full-stack case for it belongs beside ConvergenceTests rather than here.
      MUTATION TESTING FOUND THE SUITE WEAK AND THEN FOUND THE CODE REDUNDANT. Four of six survived
      the first round. One was a real gap - every case moved x and none moved y, so taking the minus
      one off the y line alone failed nothing - and another needed a footprint at a chunk's FIRST
      cell to show at all. The other two survived because the code is genuinely redundant: the clamp
      to the landscape's last cell and the off-landscape refusal both fall out of the loop bound, so
      the comment says which guards are load-bearing and which are there to keep the rule visible.
      THE DRAW SCALE REACHES THE SHADER, which was the gap this task's file list did not name:
      GeometryVS.hlsl's instance attributes were origin, heading and teamColor, and every content
      row carries a modelScaleHundredths. GeometryInstance gains three floats and the vertex shader
      multiplies before it turns - the scale is in the model's own axes, so a row drawn at another
      size has to be scaled where its vertices are and not after they are placed in the world.
      A SITE RISES, AND THAT IS A READING RATHER THAN A RULING. The design says a structure under
      construction "is a thing on the landscape" and that the panel shows it a barBuild bar; neither
      says what it looks like in the world, and this task's acceptance says only "a construction
      site scales". Rising is what "scales" most naturally means for a building going up, and
      scaling all three axes would read as a small finished building rather than an unfinished one.
      The combination is Core/RenderView.h's InstanceScale rather than two lines of HLSL, BECAUSE
      THAT MAKES IT A UNIT TEST: it is two multiplies either way, and a shader cannot be run on a
      machine with no GPU.
      THE NORMAL IS NOT SCALED and that is stated rather than hidden. For a non-uniform scale the
      correct transform is the inverse transpose, which would buy correct lighting on a building for
      the seconds it is going up and cost an inverse transpose per instance on every instance in the
      frame for the whole match. A construction site is lit a little flat; nothing else is wrong.
      THE INSTANCE BUFFER'S OFFSETS ARE STATIC_ASSERTED beside the struct now. The input layout is
      written from them and nothing checks the two agree: a mismatched element reads whatever lies
      at that byte and DRAWS it, so the failure is a model at the wrong size rather than a
      diagnostic. A member inserted above `scale` fails the build instead of the frame.
      THE WALK IS COVERED FROM THE STACK, beside ConvergenceTests, which is where the fixture that
      can build one lives: a match is run to convergence and the render view built from it, then
      the instances, the pick candidates, the selection, the fog and the chunk list are asserted
      against what the host actually put there. The convergence fixture's rows gained models for it
      - a row naming no model composes nothing, and a render view that came back empty would pass a
      case that only counted.
      MUTATION TESTING FOUND THREE HOLES IN THAT ONE CASE, each of them a branch no match in the
      file reached: a GHOST as a pick candidate (this match makes none, so the rule now sits in the
      ghost case where there is one), ForgetTerrain, and a RAZED structure's chunks. All eight
      mutations are caught now.
      AND THE PROBE HARNESS WAS LYING. Its runner listed the cases by name, so the render-view case
      simply did not run and the suite still reported every test passing - the same shape of fault
      as the stale binary the Client harness had. It is generated from the file's own TEST_METHODs
      now, so a case that is added cannot be silently skipped.
      DONE: the four acceptance lines hold, Design/Interface.md §11 row 2 is struck, and the two
      things this task asked for that have no content behind them are C7 and C8 with rows of their
      own. What CI has verified: runs 112 (picking), 114 (the builder) and 115 (the draw scale and
      the shader), the last of which is the only part this session could not compile.

  - id: K1
    title: Client geometry pass - model buffers, instancing, team colour unlit, per-face normals
    intent: >
      Devices, structures, features, wrecks and shells are drawn from the render view in the
      Species look (GameDesign.md §11 point 1; TechnicalDesign.md §6.4), with the team-colour
      slot written unlit as pillar 3 demands, so that a fight reads from the far end of the map.
    project: Client
    depends_on: [C1]
    blocked_by:
      - m0-foundation/T20
    files:
      - Client/ModelBuffers.h
      - Client/ModelBuffers.cpp
      - Client/GeometryPass.h
      - Client/GeometryPass.cpp
      - Client/Shaders/GeometryVS.hlsl
      - Client/Shaders/GeometryPS.hlsl
      - Client/Client.vcxproj
      - Client/Client.vcxproj.filters
      - Tests/ClientTests/ModelBuffersTests.cpp
      - Tests/ClientTests/ClientTests.vcxproj
      - Tests/ClientTests/ClientTests.vcxproj.filters
      - Design/ADR/ADR-011-model-normals.md
      - Design/ADR/README.md
      - Design/TechnicalDesign.md
    acceptance:
      - ModelBuffers loads a ModelDesc into vertex and index buffers with one colour per vertex and a team-colour flag per vertex where the model's slot colour was; the per-face normal comes from screen-space derivatives in the pixel shader or from split vertices, and the ADR this task writes decides which with a captured frame of a placeholder model
      - GeometryPass draws every instance of the render view with one instance buffer per model per frame, the seat colour substituted for flagged vertices and those vertices written unlit, lit otherwise by the two directional lights of the terrain pass
      - The unit test covers the loader's vertex splitting or flagging on a small model; the capture job's frames show composed placeholder devices once G2 lands
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      ADR-011 is the decision TechnicalDesign.md 6.4 deferred: the loader splits and bakes, it does
      not take the normal from the pixel shader's derivatives. The argument that settled it is not
      the cost - it is that the terrain's way of fixing the derivative's sign, "if the normal points
      down, flip it", is the ground knowing it faces up, and a model has no such rule. The cost is
      arithmetic either way: over the twenty-two models in GameData\Models the colour alone, which a
      derivative renderer still has to split on because a flat attribute comes from the triangle's
      first vertex, costs 232 vertices against the descriptions' 176; keying the normal in as well
      costs 528, which is 14.8 kB for the whole set. Measured through LoadContent and BuildModelMesh
      rather than estimated. The outward normal turned out to be cross(c - a, b - a) and not the
      textbook cross(b - a, c - a): the placeholder generator winds a front face clockwise seen from
      outside, which is what Direct3D calls front by default, and all 264 triangles agree; getting it
      backwards lights every model from inside and the symptom says nothing, so a test pins all six
      faces of a box and ModelMesh::inwardFacingTriangles counts the disagreements for authored
      models later. What this task does NOT have is the captured frame its acceptance asks for -
      G2 composes devices into the capture and R2 fills the render view, so no frame with a model in
      it exists yet. ADR-011 says so in its own measurements section and names what would overturn
      it. The pass is written and registered but nothing calls it: App.cpp is G2's file, not this
      one's. The acceptance's "one instance buffer per model per frame" is implemented as one buffer
      per frame holding a contiguous run per model, one draw per run - the same batching with one
      allocation instead of a resource per model per frame.
      CI run 76 was red on a link error this task caused and the session's Linux harness cannot see:
      ModelBuffers.obj is the first Client object a ClientTests test names that touches Direct3D, and
      a named symbol drags its whole object in, so the suite suddenly needed the libraries the
      executable links and linked none of them - unresolved WINRT_IMPL_RoOriginateLanguageException,
      which is winrt::check_hresult's originate. ClientTests.vcxproj now carries the same
      d3d12.lib;dxgi.lib;runtimeobject.lib OutpostCommander does. K2 would have hit it again for
      D3D12SerializeRootSignature.

  - id: K2
    title: Client fog pass
    intent: >
      Unexplored cells are black and explored ones dimmed from the commander's fog grid as the
      replica knows it (TechnicalDesign.md §6.2), so that what the player sees is what the host
      sent and nothing more.
    project: Client
    depends_on: []
    blocked_by:
      - m0-foundation/T20
    files:
      - Client/FogPass.h
      - Client/FogPass.cpp
      - Client/Shaders/FogVS.hlsl
      - Client/Shaders/FogPS.hlsl
      - Client/SceneTarget.h
      - Client/SceneTarget.cpp
      - Client/Client.vcxproj
      - Client/Client.vcxproj.filters
      - Core/RenderView.h
      - Tests/ClientTests/FogPassTests.cpp
      - Tests/ClientTests/ClientTests.vcxproj
      - Tests/ClientTests/ClientTests.vcxproj.filters
      - Design/TechnicalDesign.md
    acceptance:
      - A per-cell fog texture is updated from the render view's fog grid only for rows that changed, and a full-screen pass darkens explored cells and blacks unexplored ones after the geometry pass and before the UI
      - The capture job's frames show the three states once G2 lands
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE RENDER VIEW HAD NO FOG GRID TO READ. The acceptance says "from the render view's fog grid"
      and Core/RenderView.h carried instances and changed chunks and nothing else, so this task added
      FogShade and FogView to it; R2 fills them, and Interface.md 7 already promised the minimap the
      same grid. The changed rows are part of the view rather than something the client diffs out:
      every device that moves changes fog, so "did anything change" is true every frame and only
      "which rows" is worth having, and the producer knows it for free. Same bargain as changedChunks.
      HOW A PIXEL FINDS ITS CELL. The pass reads the depth buffer and runs it back through the
      inverse view projection, because what the fog darkens is whatever was DRAWN at a pixel - ground,
      device, structure - and the depth buffer is where that already is. The darkening is the blend
      state, source ZERO and destination SRC_COLOR, so the colour target is never read as a texture
      while it is a render target, which is not legal. A depth of zero is the reversed depth's far
      value and means nothing was drawn, so the background keeps the clear colour instead of going
      black. SceneTarget gained Bind, BindColorOnly and Depth(), because the depth buffer cannot be a
      texture and a bound depth target at once; its own contract already said whoever moves a target's
      state moves it back. Two depth views are declared and one is null - a Texture2DMS view of a
      one-sample resource is not legal and neither is the reverse, and the scene target falls back to
      one sample on an adapter without multisampling. The pass runs once per pixel and takes sample
      0's depth, so a silhouette crossing a cell boundary gets one answer for four samples: one pixel
      along one line, against four times the work to fix it. The shade for explored is 0.5, which is
      Interface.md 7's "half brightness" rather than a number picked here. The grid is fetched by
      integer coordinate with no sampler, because interpolating between two fog states would invent a
      third. FogPassTests covers the one part of this that a test can reach without a device, which is
      which rows a frame copies; getting that wrong leaves a row showing last frame's fog, and a
      player reads that as vision they do not have. Nothing calls the pass yet - App.cpp is G2's.

  - id: K3
    title: Client UI toolkit - panels, widgets, the Spectrum font atlas, the UI pass, router integration
    intent: >
      The interface of Design/Interface.md needs panels, widgets and text drawn 1:1 at the
      authored resolution with the Species font (TechnicalDesign.md §6.4), consuming input
      UI-first through the router of M0, so that the game's panels are assembled rather than
      hand-drawn.
    project: Client
    depends_on: [D1, C4, C6]
    blocked_by:
      - m0-foundation/T19
      - m0-foundation/T20
    files:
      - Client/UiPanel.h
      - Client/UiPanel.cpp
      - Client/UiWidgets.h
      - Client/UiWidgets.cpp
      - Client/UiLayout.h
      - Client/UiLayout.cpp
      - Client/BitmapFont.h
      - Client/BitmapFont.cpp
      - Client/UiPass.h
      - Client/UiPass.cpp
      - Client/UiDraw.h
      - Client/UiDraw.cpp
      - Client/UiInputSink.h
      - Client/UiInputSink.cpp
      - Client/Shaders/UiVS.hlsl
      - Client/Shaders/UiPS.hlsl
      - Client/Client.vcxproj
      - Client/Client.vcxproj.filters
      - Tests/ClientTests/UiLayoutTests.cpp
      - Tests/ClientTests/BitmapFontTests.cpp
      - Tests/ClientTests/UiInputSinkTests.cpp
      - Tests/ClientTests/UiDrawTests.cpp
      - Tests/ClientTests/ScaleModeTests.cpp
      - Tests/ClientTests/ClientTests.vcxproj
      - Tests/ClientTests/ClientTests.vcxproj.filters
    acceptance:
      - "Panels are fixed rectangles at the authored resolution holding widgets: label, button, toggle, list, progress bar, icon, text field taking WM_CHAR characters; flat rectangles with one-pixel borders in the chrome palette, which is READ FROM C6's GameData/Interface.json and never spelled as a constant in Client - Design/Interface.md §3 makes the look of a panel data, and a toolkit that hard-codes it leaves nobody owning the move"
      - BitmapFont loads the Spectrum atlas from its DDS through Core's TextureFile (16 by 14 cells from ASCII 32, glyph advance 0.6 of the size per SpeciesCanvas.md §4) and lays out text as quads at 1:1; UiPass draws panels, text and icons orthographically, alpha blended, after the fog pass
      - UiInputSink is the router's first sink; a widget that acts on an event consumes it, and a panel under the cursor consumes clicks so they never reach selection
      - "Tests: layout arithmetic, text measurement, the consume rule with stub events"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      The toolkit's rules are documented in the header comment of UiPanel.h, not in a prose
      file: project directories are flat (AGENTS.md §2).
      THE ACCEPTANCE AND THE DESIGN DISAGREED ABOUT THE FONT, and the design won. This task's
      acceptance quotes SpeciesCanvas.md §4's "glyph advance 0.6 of the size"; Design/Interface.md
      §3 rules 16 pixels a glyph box with a 16-pixel advance and states that a line of n characters
      is exactly 16n, "which is the arithmetic every layout in this document uses", and §12 records
      it as a ruling the owner's merge accepted. 0.6 of 16 is 9.6 - a glyph on a fraction of a pixel,
      resampled - which is the one thing ADR-004's authored resolution and pillar 3 exist to
      prevent. Species scaled its whole interface to the window and this game does not, so the
      acceptance line is Species's rule quoted before §3 ruled against it. The implementation
      follows §3 and a test pins the 16 by name so that a return to 0.6 fails rather than blurs.
      THE AUTHORED RESOLUTION MOVED FILES. AUTHORED_WIDTH_PIXELS and AUTHORED_HEIGHT_PIXELS were in
      SceneTarget.h, which cannot be included without Direct3D; the whole interface layout is
      authored against them, so they are in ScaleMode.h now and SceneTarget.h includes it. That is
      what makes the layout, the font and the consume rule testable on a machine with no GPU, which
      is the difference between this task's first three pieces being verified and being guessed at.
      UiLayout NAMES NO PANEL, against the obvious reading of "the M1 panels". Minimap, Selection
      and Command are game concepts and an engine that knew them would be in the wrong layer
      (AGENTS.md R9); §2's table of rectangles is K4's. What is here is the arithmetic any panel
      needs.
      A THIRD TEST FILE. The file list names UiLayoutTests and BitmapFontTests; the consume rule is
      neither layout nor text, so it is UiInputSinkTests. ScaleModeTests grows the round trip
      Design/Interface.md §11 row 1 asks for.
      A TEST THAT CRASHED RATHER THAN FAILED, found by running the suite: BitmapFontTests reads the
      shipped atlas from the tree, and the case that pokes its width field indexed a vector that was
      empty when the file could not be read. It asserts and RETURNS now - the return is not belt and
      braces, because a harness that only counted a failure would carry straight on into the same
      read.
      Verified in the session on Linux by BUILDING AND RUNNING all four test files against the real
      sources: 41 tests, zero failures, including the shipped SpectrumFont.dds at 256x224, the §2
      client-area and column arithmetic, AuthoredFromClient's round trip at all three scale modes
      (lossless at Exact and Integer, within one authored pixel at Bilinear) and the whole consume
      rule with stub events. With the atlas removed the suite fails cleanly rather than crashing.
      AND THE GUARD ITSELF CAME BACK RED. Assert::Fail is [[noreturn]] under the framework, so the
      `return` after it is unreachable code - MSVC's C4702, fatal under /warnaserror. The guard is
      Assert::IsFalse followed by a reachable return now, which stops the read under a harness that
      only counts AND satisfies a compiler that knows Fail never comes back. The session's Linux
      harness was the thing at fault rather than the code: its stub counted a failure and carried
      on, where the framework throws. It marks Fail [[noreturn]], throws on a failed assertion and
      builds with -Wunreachable-code now, and reproduces C4702 on the old guard - so this class of
      finding is caught here rather than in CI next time.
      AND CLANG-TIDY FOUND TWO MORE, which is the third round CI caught and the session did not.
      The five function-local constexpr rectangles in UiLayoutTests were camelCase, where
      .clang-tidy sets ConstexprVariableCase to UPPER_CASE (R3) and every other function-local
      constexpr in Tests already is; they are LEFT, RIGHT, AREA and BAR now. And OnPointerUp's
      switch had TextField and the three readouts both ending in a bare break, which
      bugprone-branch-clone reads as one branch written twice. MERGING THEM WOULD HAVE BEEN A LIE:
      a text field is interactive and a bar is not, and a reader meeting the four kinds under one
      label would conclude the opposite. They say different things now - the readouts clear
      result.widget, for the reason the empty space under a list's last row does.
      THE REAL FIX WAS TO THE SESSION, NOT THE CODE: clang-tidy runs on Linux after all, over the
      same stripped sources the test harness builds, against this repository's own .clang-tidy with
      ExcludeHeaderFilterRegex dropped (clang-tidy 18 does not know that key; the CI pin is 22.1.8).
      It reproduced both findings before the fix and 217 checks come back clean over every file of
      R1 and K3 after it. 18 is not 22 and will not catch everything 22 does, but "clang-tidy cannot
      be run here" was wrong, and acting on it cost three CI rounds.
      UiPass AND A FILE THE LIST DID NOT NAME. The declared files are UiPass.h/.cpp and the two
      shaders; what is written is those four plus Client/UiDraw.h and Client/UiDraw.cpp, and the
      split is the same one that moved AUTHORED_WIDTH_PIXELS out of SceneTarget.h. UiPass.h includes
      d3d12.h, so everything declared beside the pass can only be exercised where that header is;
      turning a panel into quads is rectangles, insets, thresholds and colour choices, which is
      arithmetic that comes out wrong by one pixel rather than by a crash. UiDraw holds it, has no
      Direct3D, and is 16 cases of UiDrawTests. UiPass is left with the upload, the pipeline and one
      DrawInstanced. UiPanel.h's "UiPass.h turns it into quads" was updated to say UiDraw.h.
      ONE DRAW CALL, BECAUSE ALPHA IS NOT COMMUTATIVE. Both atlases are bound at once and the pixel
      shader branches on a flat integer the vertex carries, rather than batching by texture: a batch
      reorders the quads, and reordering them paints a button's caption underneath the button. The
      branch is uniform across a quad and costs a comparison.
      TWO FIELDS ADDED TO UiWidget. A List had no row captions at all - `maximum` says how many rows
      it has and `text` is one string - so a list was a widget kind the toolkit could not draw;
      `rows` is them, with `maximum` still the authority the hit test and the row rectangles are
      built on. And a ProgressBar had no way to say whether it was a build bar or a health bar,
      which §3 needs to pick between barBuild, barHealth and barHealthLow; `barStyle` says.
      §3 LEAVES A BAND UNNAMED and UiDraw.cpp states its reading rather than hiding it: the table
      gives barHealth "at or over half" and barHealthLow "under a quarter" and nothing between, so
      the quarter is the threshold and the band above it takes the ordinary colour.
      THE ICON SHEET DOES NOT YET MATCH THE RULE IT IS DRAWN BY, and this is already tracked rather
      than new: §3 rules an icon "a single-channel mask tinted at draw time, never a coloured
      bitmap", while GameData\Textures\Icons.dds is the eight 64x64 Species order banners C4
      imported unkeyed - 468 colours, alpha 255 everywhere - which ADR-010 says stand "until the
      icon atlas is rebuilt from this game's own art" and §11 row 7 assigns to C4 as a blocker on
      K4. UiPass implements §3 (a 32x32 cell, the red channel as the mask) and IconAtlas loads any
      sheet that divides into 32x32 cells, so the placeholder loads as an 8x4 grid of quarter
      banners and an icon draws as a tinted square until the sheet is re-authored. Nothing here
      hard-codes the placeholder's geometry. The font needs no such note: it is a two-colour cutout,
      white on transparent, so its alpha IS its coverage, which is why the two kinds read different
      channels and UiPS.hlsl says so.
      MUTATION TESTED, AND IT FOUND TWO HOLES. With Uppercased() removed from the title the suite
      still passed, because the case pinned the FIRST glyph of "Selection" and that letter is
      already a capital; it pins the second now, where 'e' becomes 'E'. With an icon's tint fixed at
      bodyText it still passed, because nothing covered §3's "dimText where it is not available".
      Both are cases now, and the other eleven mutations tried - the bar drawn over its own border,
      a collapsed gradient, overlapping border corners, a half instead of a quarter, a uv edge, a
      dropped triangle, an unchecked cell index, a selected row's caption, a down button's caption -
      were all caught before the fix.
      AND THE LINUX HARNESS WAS WRONG AGAIN, in a way that mattered less than the last one but was
      worth fixing: it built every test file into one translation unit, which invented a clash
      between two files' file-local SELECTION_PANEL that MSVC cannot have, and its compile step was
      piped through grep so a failed build reported the previous binary's run as a pass. Each test
      file is its own translation unit now, as it is under MSVC, and the binary is deleted before
      the build so a stale one cannot answer for it.
      Verified in the session on Linux: 59 tests, zero failures, over five test files; clang-tidy 18
      clean over UiDraw, UiPanel, UiWidgets and UiDrawTests with this repository's own configuration;
      CheckFormat, CheckProjectFiles and CheckTaskDag clean.
      WHAT IS NOT SHOWN HERE. UiPass.cpp, UiVS.hlsl and UiPS.hlsl are Direct3D and HLSL: this
      session can compile neither, and nothing constructs a UiPass yet - K4 builds §2's panels and
      G2's capture is the first frame that would carry one. So the pipeline state, the root
      signature, the blend and the two shaders are checked by CI's build and by nothing else until
      then, and the acceptance line "UiPass draws panels, text and icons" is shown in G2's frames
      rather than here. That is a deliberate statement of what is and is not evidence, not a request
      to take it on trust.
      GREEN ON RUN 35437308010 (head ebf94b1), which is what makes this done: the MSVC build with
      /warnaserror took UiPass.cpp and both shaders, dxc took UiVS.hlsl and UiPS.hlsl, vstest ran
      the 16 new ClientTests cases with the rest, the WARP capture and the content validation
      passed, and clang-tidy 22.1.8 came back clean over every translation unit. The three files
      this session could not compile compiled first time, which the local clang-tidy sweep and the
      check of every d3dx12.h helper call against the vendored header are the reason for - that
      check found the one real defect, <iterator> for std::begin and std::size.

  - id: G1a
    title: OutpostCommander application - the client loop and the host thread over loopback
    intent: >
      The slice runs as the two loops of TechnicalDesign.md §3 in one process: a host thread
      with Sim and Net's Host over loopback, and the client loop with Net's Client, the replica,
      the render view and the passes, so that single-player is the same code path as a match
      over the network from the first playable build (owner, 2026-09-17). This half is the one
      the capture can witness on its own, which is why it is worked and reviewed on its own.
    project: OutpostCommander
    depends_on: [R2, N2, K1, K2, S12]
    files:
      - Core/LoopbackTransport.h
      - Core/LoopbackTransport.cpp
      - Core/TickPacer.h
      - Core/Core.vcxproj
      - Tests/CoreTests/TickPacerTests.cpp
      - Tests/CoreTests/CoreTests.vcxproj
      - OutpostCommander/LocalHost.h
      - OutpostCommander/LocalHost.cpp
      - Replica/RenderViewBuilder.h
      - Replica/RenderViewBuilder.cpp
      - OutpostCommander/App.h
      - OutpostCommander/App.cpp
      - OutpostCommander/LocalHost.h
      - OutpostCommander/LocalHost.cpp
      - OutpostCommander/Match.h
      - OutpostCommander/Match.cpp
      - OutpostCommander/Main.cpp
      - OutpostCommander/OutpostCommander.vcxproj
      - OutpostCommander/OutpostCommander.vcxproj.filters
    acceptance:
      - "LocalHost runs Sim, Net's Host and the AI seat on its own thread driven by wall time, mapping wall time to ticks in exactly one place, slowing the match clock rather than skipping when it falls behind; it talks to the client only through LoopbackTransport"
      - "The client loop is the seven steps of §3: pump, drain the network, apply frames, interpolate 100 milliseconds behind, build the render view, route input, draw and present; it has no simulation clock"
      - Starting the executable starts a match on GameData/Landscapes/Slice.json with the human in seat 0 and the scripted AI in seat 1 at the lobby defaults, until M2's lobby exists
      - "THIS TASK'S OWN CAPTURE PROVES IT, NOT G2'S. The CI capture step runs the executable over a real match and its log names what was drawn - the landscape's extent, the frame's instance and draw counts, and the camera each frame was taken from - and Tools/DescribeCapture.py reads the frames back as per-band statistics in the same job. A frame that carries this commander's base is a frame whose instance count is non-zero over a landscape that arrived; that is observable in the job log without G2. THE LINE SAID 'the capture of G2' UNTIL 2026-09-19 AND THAT WAS A CYCLE: G2 depends_on G1a, so a G1a whose acceptance waits on G2's artefact can never be closed and takes K6, G1b, G2, K4, K7 and G3 down with it - six of the nine tasks left in the milestone, none of them blocked by any code. The AI-versus-AI observer capture stays G2's, where it was always scoped"
      - "A base and not two: the frames are drawn from one commander's replica and the opposite base is unexplored ground (TechnicalDesign.md §5.2). This line said two until G1a was written; G2's own acceptance already says 'a base', singular, and that is the wording that governs"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      SPLIT OUT OF G1 ON 2026-09-19 (owner), with G1b. G1 was thirteen files and the whole
      application - the host thread, the wall-time-to-tick mapping, match start, camera, selection,
      order input, the pause menu, the end-of-match overlay and a measured click-to-move - with five
      dependencies and everything downstream of it, and ImplementationPlan.md §2 says no session can
      build or run it: only CI sees it. The split makes G2's capture the witness for this half
      rather than for the whole, so the capture and G1b proceed at the same time instead of one
      behind the other, and it halves the largest single diff of the milestone.
      THE TRANSPORT WAS NOT THREAD SAFE AND THIS TASK IS WHERE THAT BITES (owner ruled 2026-09-19,
      on being shown it). This acceptance says LocalHost runs on its own thread and talks to the
      client "only through LoopbackTransport", and TechnicalDesign.md §3 says the same - "a host
      thread in the same process talking to the client through the loopback transport". But
      Core/LoopbackTransport had no mutex and no atomic anywhere: two deques, the counters and the
      fault Random, all touched by both ends, and an end's Poll reaches into the OTHER end's queue
      through Carry. That is a data race of the kind that survives every test and corrupts a shipped
      build. One coarse lock over the whole object now, because an end never touches only itself.
      PROVED RATHER THAN ASSERTED. A probe runs a host thread and a client thread over one transport
      under ThreadSanitizer: 32 data races reported with the lock taken out, and none with it in.
      The probe is not committed (ImplementationPlan.md §2), and TSan came from g++ 13 because the
      clang-18 runtime is not installed here.
      Faults() and Statistics() return BY VALUE now. A const reference into state another thread may
      be writing is a race the caller cannot see; both are a handful of integers, and no caller bound
      a reference to either. Host() stays unlocked and says why: it reads the one element the
      constructor put there, which nothing moves and nothing erases. Connect() locks, because it
      grows the vector every End holds a reference into.
      THE DETERMINISM PROMISE IS UNCHANGED WHERE IT IS MADE. "The same seed and the same traffic lose
      the same datagrams" is a promise to the tests, which drive both ends from one thread in a fixed
      order; two threads interleave their Sends and no lock makes that repeatable. Local play runs
      with no faults at all.
      LocalHost IS WRITTEN: the host thread, started explicitly rather than by the constructor -
      a thread started there can reach a member the constructor has not finished writing - stopped
      and joined by Stop(), which the destructor calls so a match that throws on its way up leaves
      no thread behind. Pass(elapsed) is public so the capture path can run a match as fast as it
      can rather than in real time, and so a test can step it exactly.
      THE AI SEAT IS SIM'S. Sim::Advance runs it at stage 11 (AdvanceAiSeats), so a host that ticks
      the simulation has already run every AI commander; this acceptance names the AI seat because
      the host thread is what makes it run at all, not because the host drives it.
      THE PACING IS IN Core AND NOT IN THE EXECUTABLE, which is TechnicalDesign.md §2's rule rather
      than a preference: "code in an executable cannot be linked into a test DLL, so anything in one
      that deserves a test is code that belongs in a library". Turning wall time into ticks is the
      arithmetic that is wrong only on a machine that stutters, months later, unreproducibly - so it
      is Core/TickPacer.h with ten cases in CoreTests, and what stays in OutpostCommander is the
      thread and the wiring, which have nothing to get wrong. Every one of seven mutations is
      caught, and the guard against a zero step is caught as a DIVIDE BY ZERO rather than a failed
      assertion, which is as load-bearing as a guard gets.
      IT SLOWS AND IT DOES NOT SKIP, which is the rule the whole file exists for: when the debt runs
      past the ceiling what is thrown away is WALL TIME, never a tick. Every tick the match owes is
      still run, later than real time asked for; the match takes longer in real seconds and is
      otherwise identical. A skipped tick would be a different match, and on a simulation whose hash
      both ends compare that is not something anybody recovers from. §3 also asks that a slowed host
      TELL its clients and that the lobby show it; M1 has no lobby (this task's own acceptance says
      "until M2's lobby exists"), so it is counted in SlowedTicks() and BehindTicks() and left for
      whoever grows one.
      NO WINDOWS HEADER ANYWHERE IN IT, which makes it the one part of G1a that is not CI's alone:
      Sim, Net, Core and a thread. It builds and runs outside CI, and it did before it was pushed.
      Match IS WRITTEN: the host thread on one side, this commander's client and replica on the
      other, and the loopback between them. Of the client loop's seven steps it runs 2 to 5 - drain
      the network, apply frames, interpolate, build the render view - and App will run 1, 6 and 7,
      the window's messages, the input routing and the drawing, because those need a window and a
      device and these do not. So Match, like LocalHost, has no Windows or Direct3D header in it and
      is not CI's alone. Client/TerrainChunk.h's CHUNK_CELLS is PASSED IN rather than included for
      exactly that reason, which is the same choice RenderViewBuilder already makes.
      THE CLIENT HAS NO SIMULATION CLOCK AND STILL NEEDS A CLOCK. §3 says the client "has no
      simulation clock; it has the replica's timeline", and Net's Client takes a tick for heartbeats
      and timeouts. That is a LIVENESS clock, and it has to come from wall time rather than from the
      replica: a host that stopped sending stops advancing the replica's tick, which is exactly the
      moment a timeout has to fire. It is Core's TickPacer again, allowed to run every tick it owes,
      because a liveness counter that ran four at a time would make a timeout late by however long
      the frame hitched.
      RUN END TO END, WHICH NOTHING IN THIS MILESTONE HAD BEEN. A probe stands up a Match on the
      real GameData and the real Slice landscape and runs it for a second and a bit: the host thread
      reached tick 24 in 1.2 seconds (20 Hz, to the tick), the client made 147 passes, the join was
      accepted, the replica adopted the landscape and the render view carried the fog, with nothing
      behind and no wall time given away. Not committed (ImplementationPlan.md §2).
      AND THE SAME MATCH IS CLEAN UNDER ThreadSanitizer: zero data races with a real host thread and
      a real client thread over the loopback, which is the lock added earlier in this task verified
      where it actually matters rather than in a synthetic probe.
      THE CLIENT GENERATES ITS OWN LANDSCAPE, AND THIS IS WHERE THAT BOUNDARY IS EASIEST TO LOSE.
      Match holds a Landscape of its own, built from the definition the host sent with the join, and
      NOT the host's - whose own is flattened under every structure in the match, including the
      bases this commander has never scouted. TechnicalDesign.md §5.2: "the terrain under an
      unscouted base is not public". In a local match the host's Landscape is one member away and
      reading it would be a wallhack that shipped, so the client generates and App draws that.
      It arrives with the join, so it is empty until the client is Playing (TerrainReady() says),
      and it is generated AFTER Client::Advance rather than before, because Advance is what reads
      the JoinAccepted. App therefore cannot build its terrain, water and fog passes at startup the
      way M0 did: they wait for the landscape, which is a state M0's loop did not have.
      APP IS WRITTEN AND IT IS THE ONE PART OF THIS TASK CI ALONE CAN COMPILE. It loads GameData,
      finds Slice by its file stem in the content tree - M0's hard-coded recipe is gone and a
      missing landscape is refused rather than substituted for - holds a Match, and draws the render
      view. The passes that need a landscape are built when it arrives and not at startup; the
      frames before it does present the cleared target, which is the Species sky, and the log says
      once if the join has still not landed after ten seconds, because a window of nothing but sky
      is otherwise indistinguishable from a window that hung. ModelBuffers and the geometry pass are
      built at startup, since models and commander colours are content and need no join.
      THE CAPTURE WAITS FOR THE JOIN BEFORE IT COUNTS FRAME 0, and the window deliberately does not.
      A window shows sky for a frame or two and nobody minds; a capture whose frame 0 is the cleared
      target has written a BLACK BMP as its first artefact, and those files are the only look anyone
      gets at this game. The wait is bounded and a join that never lands is reported.
      TWO MISTAKES CAUGHT IN THE WRITING, BOTH WORTH RECORDING. The terrain pass's constants address
      is set INSIDE Draw, from the slot that Draw wrote, so hoisting it above the call to satisfy
      R16 would have bound the previous frame's constants - the hoist of FrameOf next to it is real
      and that one was not. And the probe's first check of the command post's height compared a wire
      value against a subunit one and failed by a factor of 64; the code was right and the check was
      wrong, which is the failure a probe exists to make loud.
      THE BASE LEVEL WAS NOBODY'S AND IS NOW THIS TASK'S. Tests/SimTests/AiTests.cpp has carried the
      note since S12: "NOTHING IN THE TREE OWNS BASE-LEVEL PLACEMENT YET - it is the application's
      (m1-vertical-slice/G1), and until G1 lands a match is set up by whoever starts one". So
      OutpostCommander/StartingBase places GameDesign.md §2's Nothing level - a command post and a
      builder - before the host thread starts, because a base placed on a running simulation is a
      structure that appeared out of nothing in a frame somebody was watching. Small and Established
      are refused rather than silently reduced: a commander who chose an established base and got a
      builder would read it as the game losing his settings, and the lobby that can ask for them is
      M2's. The builder's design is the AI designer's choice of builder even for the human seat,
      which is not a shortcut - it is the same truck the scripted opponent starts with, so neither
      commander begins a chassis ahead.
      IT FLATTENS AND THE TEST FIXTURE NEVER DID. Sim/Construction.cpp levels the ground under a
      footprint the moment construction begins and writes the mean into Structure::y; AiTests skips
      that step, so every command post in that suite stands at y = 0, which is sea level. No test
      there draws, so nothing noticed. StartingBase runs the same FlattenDelta and the same mean,
      and the probe confirms the post at 84 world units against ground the client generated at 86 -
      the footprint's mean against the point under its centre, which is exactly the difference there
      should be.
      PROVED END TO END AGAINST THE REAL CONTENT: the commander sees exactly his own command post
      and his own builder and nothing else, because the opposite base is unexplored - §5.2 working
      rather than asserted - and the render view has four instances to draw. ThreadSanitizer finds
      no race in a match that now has objects to publish every tick, which the empty world of the
      earlier run could not have exercised.
      A CLIENT CANNOT BE SEATED ON AN AI SEAT, found here and recorded against G2. Net/Host.cpp's
      FreeSeat hands a joining client only a seat whose kind is Human, so the all-AI lobby G2's
      acceptance asks for refuses the join with NoSeat and leaves no replica to draw. Tried, not
      reasoned about: six failures and an empty view. The capture therefore watches an unattended
      human seat whose base stands still while the AI opposite it plays, and G2 decides between a
      scripted order stream and an observer connection in Net.
      AND THE FLATTEN NEVER REACHES THE CLIENT AT ALL, which became K6 by the owner's ruling on
      2026-09-19. changedChunks has no consumer, Match never calls Landscape::ApplyDelta, and
      TerrainPass cannot re-mesh a chunk; K6 carries the whole finding. Sequenced before G2, so that
      the frames G2 uploads do not show bases half-buried in hillsides.
      AND THE FIRST CAPTURE OF A REAL MATCH CAME BACK 100% BLACK ON A GREEN BUILD, which turned out
      to be a Net defect that had been in the tree since N2. DescribeCapture said it plainly - "mean
      colour (0, 0, 0); near-black 100.0%" for every frame - and the job passed anyway, because the
      capture's exit code reports debug-layer messages and nothing else. M0's frames showed the
      island because M0 had no fog pass; M1's multiplies every pixel by FOG_SHADES[shade], and an
      all-unexplored grid multiplies the whole frame by zero. So the fog pass was right and the fog
      was wrong.
      NARROWED BY PROBES RATHER THAN BY READING. The host's own grid: 166 cells visible from tick 1,
      so Sim and the base placement were fine. The replica's: all 16,384 unexplored, so the copy into
      the render view was fine too. The whole stack single-threaded, printing both sides every pass:
      the replica had the right fog at passes 3 and 4 and lost it at pass 5, permanently.
      THE DEFECT. Every list in a frame is the difference between what the commander can see NOW and
      the baseline he ACKNOWLEDGED, so a frame he drops costs nothing - the next one carries the
      same difference again. The fog was the one field encoded against a rolling per-client grid
      that advanced on every ENCODE. And Net/Client.cpp drops any delta whose baseline is not
      exactly the frame it last applied, which is not a lossy-link case: it happens whenever a
      publish outruns an acknowledgement, which at a 10 Hz publish rate is most of them. Every
      dropped frame took its fog runs with it and those cells were never sent again.
      THE FIX GIVES THE FOG THE SAME SEMANTICS AS EVERYTHING ELSE. FrameRecord carries the runs its
      frame sent; ClientView::fog is now the grid the client is KNOWN to hold, advanced only when a
      baseline it acknowledged is folded in; and a full frame encodes against an empty grid, because
      the replica clears everything it holds on one. Runs per record rather than a grid per record,
      so the memory is ADR-008's O(cells) a client and not O(cells x history).
      THE TEST HOLE WAS THE REAL FINDING. ConvergenceTests is the suite whose whole purpose is "the
      replica holds exactly what the host would send", and the only fog it compared was its SIZE -
      which an all-black grid passes. AssertConverged now compares it cell for cell, which put the
      failure count at 12,070 over six cases before the fix and 0 after, and a case named for the
      bug walks the publishes one at a time so a failure says which one lost the map.
      MUTATION TESTING FOUND THE FIX HALF-TESTED. Three ways to break it: advancing the grid on
      encode again, never folding the acknowledged baseline, and diffing a full frame against what
      the client had. The first and third went red; the SECOND SURVIVED, because a host that never
      folds re-sends the whole grid every publish and is still correct - the replica agrees with it
      every time. That is a bandwidth defect a correctness suite cannot see, so the case also
      asserts that a settled match publishes no fog runs at all, which kills it.
      ONE EDGE LEFT AS IT IS, DELIBERATELY. A minimised window blocks in WaitMessage while the host
      thread keeps ticking, so the liveness clock falls behind and, past Match's thousand-tick debt
      ceiling, wall time is written off. Pausing the local host is G1b's Escape menu and that is the
      right place to fix it; spinning the loop instead would burn a core to avoid a case a pause
      handles properly.

      CLOSED ON 2026-09-19 AGAINST CI RUN 130 (head 19f6788, Debug|x64 green: build, 0 warnings as
      errors, every suite, clang-tidy over 178 translation units, OutpostHost --validate, and the
      capture). The capture step's log is the evidence, line for line: "content: 3 chassis, 6
      structures, 30 research items"; "palette: ...\\GameData\\Terrain\\LandscapeDefault.dds" - the
      authored palette and not the built-in gradient, which is the half of C2's last acceptance line
      this task can settle on its own; "terrain: 16 chunks at four strides, 386624 vertices, 664912
      triangles built"; "capture: the landscape arrived - 513 samples a side, 128 cells, highest
      130" - so the join was accepted, the definition crossed the loopback and the client generated
      its own terrain from it; and three frames each "4 instances in 4 draws, 0 naming no model" at
      host ticks 24, 245 and 448, ending "300 frames, 0 debug-layer messages, debug layer on, host
      tick 638". Four instances is exactly this commander's command post and builder composed at
      their markers, with the opposite base unexplored - which is the second acceptance line
      observed rather than argued. DescribeCapture read the frames back in the same job: frame 0 at
      95.9% near-black over a descending orbit that starts above the fog, frames 100 and 200 at
      57.9% with a mean colour of (20, 77, 65), which is the landscape lit and fogged rather than
      the black screen this task's own fog defect produced two runs earlier.
      WHAT THIS CAPTURE DOES NOT SHOW, AND WHY IT IS STILL G2'S. The instance count is 4 at tick 24
      and 4 at tick 638 - nothing is built, nothing moves, no shot is fired - because seat 0 is an
      unattended human seat and the only commander playing is behind the fog. Frames 100 and 200 are
      deliberately the same vantage (ADR-005's fog pair) and DescribeCapture returns the same
      statistics for both to every digit it prints, so the artefact carries two pictures and not
      three. That is the whole of G2's brief and none of this task's: G1a owed a real match rendered
      from a real replica, and that is what the log shows.
      AND THE PACING IS REAL TIME, WHICH G2 INHERITS AS A NUMBER RATHER THAN A HOPE. --warp selects
      the WARP adapter and does not warp the clock; RunCapture advances the match by measured wall
      time and says so in its own comment. Run 130 went from host tick 24 to 638 in 30.7 seconds of
      wall time: 20.0 ticks a second, which is TICKS_PER_SECOND exactly and no faster. G2's "6000
      ticks" is therefore 5 minutes of simulation before WARP has drawn a pixel, against G2's own
      five-minute ceiling, so the tick-driven pacing LocalHost::Pass(elapsed) was made public for is
      a precondition of that acceptance line rather than a refinement of it.

  - id: S14
    title: Two scripted commanders never fire a shot, and the capture cannot show combat until they do
    intent: >
      Measured on the slice landscape at the lobby OutpostCommander runs, seed 1, both seats
      scripted: over 20,000 ticks - three times the 6,000 G2's capture is specified at - the two
      commanders build 115 devices between them, spend 99,734 device-ticks HOLDING AN ACQUIRED
      TARGET, and fire ZERO shots. Found while measuring how long G2's capture would take; the
      producer m1-vertical-slice/C9 added is what made it visible at all, because until then a
      shot left no trace anybody could count.
    project: Sim
    depends_on: [S10, S12, C9]
    files:
      - Sim/AiSeat.h
      - Sim/AiSeat.cpp
      - Sim/Targeting.cpp
      - Tests/SimTests/AiTests.cpp
      - Tests/SimTests/CombatTests.cpp
      - Design/GameDesign.md
    acceptance:
      - "THE MEASUREMENT IS REPRODUCED FIRST, as a committed case: a scripted match on the slice landscape fires at least one shot inside the tick count G2's capture runs for. Today it fires none in twenty thousand, and Sim::Shots() is what counts them"
      - "BOTH HALVES OF THE CAUSE ARE ADDRESSED OR ONE IS RULED OUT OF SCOPE, and they are independent - either alone leaves the match silent. (a) THE GROUP DOES NOT ARRIVE: AiSeat's Attack sends a group to APPROACH_CELLS (4) short of the enemy's start cell and the nearest attacker ends 12 cells from it, so the order is given and not carried out - a route refused, a budget spent, or an arrival that is not one. 14 devices were ever put on an AttackMove and NONE was still on one at the end. (b) NOTHING CLOSES ONCE IT IS IDLE: a machine gun under the default Optimal range stance opens at 8 cells and acquires at 12, and Sim/Targeting.cpp closes on a target only for an Attack order or an AttackMove whose stance pursues - so a device that has arrived, gone to Stop, and can SEE an enemy at 10 cells stands there for the rest of the match"
      - "WHICHEVER WAY IT IS FIXED, GameDesign.md §8's reading of the range stances is written down: 'optimal range' either means 'close to the short band and engage there', which makes (b) a defect, or it means 'do not fire outside the short band' and nothing more, which makes it the AI's job to order the closing. The design says neither today and the code takes the second reading silently"
      - The measured figures are recorded in the notes, before and after, so the next reader can tell a fix from a coincidence
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE MEASUREMENTS, so that a fix can be told from a coincidence. Slice landscape, seed 1, both
      seats SeatKind::Ai, the starting base OutpostCommander/StartingBase.h places, run with no
      renderer and no client:
        tick  2,000 - 2 devices, 12 structures
        tick  6,000 - 11 devices, 16 structures
        tick 12,000 - 55 devices
        tick 20,000 - 115 devices, 0 shots
      First target acquisition on tick 8,139, which agrees with S13's "first contact fell on tick
      8,047 in every one of sixteen seeds". The closest any two enemies ever came, devices and
      structures alike, was 10 CELLS on tick 9,401. The machine gun the AI designs and builds is
      8 cells short and 12 long; both commanders' devices carry it.
      WHY IT WAS INVISIBLE UNTIL NOW. The AI suite asserts that the scripted commander PLAYS - it
      builds, it expands, it sends groups - and nothing asserted that a match ever resolves. Two
      commanders that build 115 devices and never fight look exactly like two commanders playing
      well, in every figure the suite prints.
      WHAT THIS BLOCKS. G2's capture is specified at 6,000 ticks and its frames are the only look
      anybody gets at this game; m1-vertical-slice/C8's moved acceptance line asks one of them to
      show a shot in flight. At 6,000 ticks there is not even an acquisition yet, let alone a shot.
      G3 is the owner's run of a match to a decided end, and a match where neither side can hurt
      the other has no end.
      NOT FIXED HERE BECAUSE THE FIX IS A RULING. Making a device close on what it can see but may
      not hit changes what a range stance MEANS, and that is GameDesign.md's to say rather than
      something to decide inside Targeting.cpp.
      HALF (a) IS FIXED AND MEASURED; HALF (b) IS STILL THE OWNER'S. The root cause of (a) was
      pinned rather than guessed: the group was ordered on tick 5,623 to cell (38,90), which is
      inside the enemy's own buildings and impassable to everybody; the planner answered
      Unreachable one tick later, Sim/Movement.cpp fell the devices back to Stop, and the seat
      re-issued the same four orders every ten ticks for the rest of the match. APPROACH_CELLS of
      four was enough for a bare start cell and is not enough for a base that has grown by the time
      a commander has four idle fighters. AiSeat's Attack now rings outward from that cell for one
      the group's own drive class can stand on (ATTACK_APPROACH_RINGS, sixteen).
      BEFORE AND AFTER, same landscape, same lobby, seed 1:
        before - 115 devices, 0 shots in 20,000 ticks, closest approach 10 cells
        after  - first acquisition tick 6,908, FIRST SHOT TICK 6,980, 5,184 shots in 20,000
        and over five seeds the first shot fell on tick 6,980 in every one; the landscape decides
        this far more than the seed does, which is what S13 found for first contact too
      A DECIDED MATCH NOW TAKES 12,746 TICKS, where the victory case used to reach its end on the
      survival clock. That is the acceptance G3 asks for arriving by conquest instead.
      THE FIRST ATTEMPT AT THE FIX WAS WRONG AND THE HARNESS SAID SO. It required the destination
      to be in the same ClusterGraph COMPONENT as the group. Components are per CLUSTER - a flood
      fill over at most 256 cells - so two cells of one landmass sixty apart have different numbers,
      every attack was refused, and both armies stayed within four cells of home for twenty
      thousand ticks. Reachability is a search and the planner is what does it; what this function
      answers is only whether the ground is standable, which is the case that was actually broken.
      WHAT ASSERTS IT. The 12,000-tick acceptance match already in AiTests now counts shots - the
      tick loop was already paid for - and asserts a first shot inside 9,000 ticks and more than
      500 shots. Both bounds are needed: with the destination left broken the defenders still
      stumble into each other, measured at tick 11,758 and 56 shots, so "a shot was fired at all"
      passes a build where the attack never happens. And a focused case asserts the invariant
      directly - every AttackMove the commander issues names a cell its own drive class can stand
      on - which is the rule and not the symptom.
      ONE EXISTING ASSERTION WAS CORRECTED RATHER THAN WEAKENED. The acceptance case read its
      structure counts once, at tick 12,000, which was a true reading only while nothing could be
      destroyed. With combat working the loser has lost its factory and its generator well before
      then, which is the game working. Each condition is latched when it is first true now, exactly
      as the seed sweep beside it already did.
      FOUR MUTATIONS KILLED: sending the group to the raw cell as before; no rings at all; starting
      the search at the enemy's start rather than short of it; and, caught by a mutation that
      SURVIVED, a redundant Passable test beside the component test - ComponentAt answers
      NO_COMPONENT exactly where Passable answers false, so the pair was one question asked twice
      and is now one test.
      HALF (b) IS RULED AND FIXED. The owner read "optimal range" the wide way (2026-09-20): it
      means fight at your best range, so a device holding a target no weapon of its own can reach
      walks into the short band - but only when there is no commander's order to outrank. Stop is
      the absence of an order and may be replaced; Move, AttackMove given by a commander, Attack,
      Patrol, Guard and ReturnToRepair are not, and HoldPosition holds whatever the range says.
      GameDesign.md §8 now carries the ruling in two paragraphs rather than leaving it to be read
      off the code.
      WHAT IT TOOK, and the first attempt that did nothing. Targeting.cpp already knew the opening
      range - the reload gate computes it - but the same expression is now named once, OpeningRange,
      so the gate and the closing rule cannot drift apart; a weapon whose short range is its minimum
      still answers longRange, which is the mortar case the paragraph above it already ruled.
      Setting only the destination changed NOTHING, measured: Sim/Movement.cpp's Walk returns
      immediately for PrimaryOrder::Stop, so a device given somewhere to be and no order to go
      there stands exactly where it stood. The device gives itself the ORDER as well, and
      AttackMove rather than Attack because Arrive turns an AttackMove back into Stop - it takes
      the post and stops, instead of chasing whatever it sees next across the landscape.
      MEASURED, on the same slice landscape and lobby:
        a gunner with no order, 10 cells from an enemy it can see - closes to 5 cells, fires, and
        is on AttackMove while it walks; back on Stop once the target is dead
        the same gunner under HoldPosition - does not move, stays on Stop, fires nothing
        a gunner already inside the short band - does not move and stays on Stop; the rule is
        "cannot reach it", not "would rather be closer"
        a gunner carrying a commander's Move order - is still on Move every tick it holds a target
        a decided match - 12,214 ticks, where half (a) alone left it at 12,746
      FOUR MORE MUTATIONS KILLED, and the harness lied once before they were: M1 hold position no
      longer holds; M2 it closes on something it can already hit; M3 Attack rather than AttackMove;
      M4 a commander's order is hijacked too. All four SURVIVED the first sweep, and three of those
      survivals were the harness rather than the tests - the restore put the source back but left
      the archive holding the last mutant, so the probes were reading a build nobody had written.
      Rebuilt from the restored source, M1 fell to one case, M2 and M3 to two. M4 survived honestly:
      nothing asserted that an order is not overwritten, only that one was not GIVEN. The case that
      kills it - 88 failures - asserts per tick rather than at the end, because 200 ticks let the
      device arrive and revert to Stop on its own, and it asserts only while a target is held: a
      target is dropped past the twelve-cell weapon reach and not the twenty-cell sight, so "it can
      see the enemy the whole way" was a false premise and failed twice before it was corrected.

  - id: S15
    title: A scripted commander seated without auto-research can never finish a match it has won
    intent: >
      Sim/AiSeat.h carries no research behaviour on purpose - S6 gives every idle lab an
      auto-research flag on the seat, and the header says in as many words that a scripted seat is
      set up with it on. OutpostCommander/App.cpp's lobby did not set it, so the commander the
      GAME ships was locked out of the whole research table, and therefore out of every weapon but
      the machine gun. A machine gun deals EXACTLY NOTHING to a command post: 8 damage taken to 2
      by the anti-light matrix's 30 percent against Hard, then all of it by the post's 20 kinetic
      armour, with a floor of a third of 2 that is nought in whole points. Measured on the slice
      landscape at seed 1: fifty simulated minutes, the enemy army swept from the field, 153
      machine guns standing round an enemy command post at full health, and a match that could not
      end. Found on 2026-09-20 while checking whether G3's acceptance - "plays to a decided end" -
      was reachable at all.
    project: OutpostCommander
    depends_on: [S12, G1a]
    files:
      - OutpostCommander/App.cpp
      - Sim/AiSeat.h
      - Sim/MatchSettings.h
      - Tests/SimTests/CombatTests.cpp
      - Design/TechnicalDesign.md
      - .github/workflows/build.yml
    acceptance:
      - The lobby the game ships seats every scripted commander with auto-research on, which is the contract Sim/AiSeat.h states
      - An annihilation match between two scripted commanders on the slice landscape reaches a decided state, measured on the seed the game plays and on others
      - CI refuses a capture in which the watched commander researched nothing, because the capture is the only run in the tree that plays the game's own lobby rather than a fixture's
    parallel_safe: true
    status: done
    notes: >
      Done 2026-09-20. ONE LINE OF LOBBY, and the whole of the difficulty was finding it. The
      defect sat between a library's documented contract and the single caller that had to honour
      it, which is a seam no unit test of either side can see: Tests/SimTests/AiTests.cpp's fixture
      sets the flag, so every suite ran a commander that researches while the executable ran one
      that does not, and every suite passed for as long as the defect existed.
      MEASURED BEFORE AND AFTER, on the annihilation lobby the game ships (both seats scripted,
      the probe harness of ImplementationPlan.md 2). Before: seed 1 unfinished at 60,000 ticks with
      seat 0 reduced to three untouched buildings and seat 1 holding 153 machine guns. After: seed
      1 decided in 12,874, and of seven seeds sampled six decide between 11,673 and 34,264 ticks.
      The seventh, seed 7, is S16 and a different defect.
      THE GUARD IS THE CAPTURE AND NOT A UNIT TEST, for the reason the defect existed: the capture
      is the one thing that plays the game's lobby. It now logs how many items the watched
      commander has researched - the observed seat's own mask, read off the replica the way a
      client reads anything - and the job throws when that is zero.
      One case was added to CombatTests rather than a new suite: a device chasing another DEVICE
      sets its destination to the quarry itself, which is the behaviour S16 must not change when it
      teaches a device to hold off from a BUILDING. It kills the mutation that drops that
      distinction.

  - id: S16
    title: A device closing on a structure asks for a route into it, and stands there asking for ever
    intent: >
      A device that chases a target sets its destination to the target's own position
      (Sim/Targeting.cpp's chasing block). A STRUCTURE'S CELLS ARE IMPASSABLE, so the planner
      answers Unreachable, Sim/Movement.cpp stands the device down to Stop, and S14's closing rule
      gives it the order again on the very next tick - for ever. Measured on the slice landscape at
      seed 7 with S15 fixed: 80,000 ticks, the winning commander holding 78 CANNONS - which destroy
      buildings perfectly well - with 63 of them holding the enemy command post as their target and
      NOT ONE of them firing. It is not the scripted commander's defect: a human's own Attack-move
      or Attack on a building walks into the same wall, and Design/Interface.md 6 makes Attack the
      default right-click order on an enemy structure.
    project: Sim
    depends_on: [S14, S15]
    files:
      - Sim/Targeting.cpp
      - Tests/SimTests/CombatTests.cpp
      - Design/TechnicalDesign.md
    acceptance:
      - A device ordered onto an enemy structure walks to standable ground inside its own opening range and fires, rather than asking for a route into the building
      - "The fix holds at ARMY SCALE and not only for one device: an annihilation match on seed 7 reaches a decided state, and the seeds S15 measured do not regress"
      - A device chasing another DEVICE still runs it down rather than holding off, which is what S15's case pins
    parallel_safe: true
    status: todo
    notes: >
      TWO VARIANTS WERE TRIED ON 2026-09-20 AND NEITHER IS THE ANSWER, which is why this is a task
      and not a line. Walking to the edge of the opening range fixed seed 7 (22,858 ticks) and left
      a boundary oscillation a unit case catches: a mover arrives on a CELL, so a device sent to the
      exact edge of its band comes to rest a fraction outside it, cannot fire, gives itself the
      order again, finds it is already there, stands down, and repeats. Backing the stand-off point
      off by one cell cured the oscillation and LOST seed 7 again, at 80,000 ticks. Each variant
      fixed some seeds and cost others.
      WHAT THAT POINTS AT is the planner's shared budget rather than the geometry: 2,000 nodes a
      tick across every request, and eighty devices round one base each asking for a route whenever
      their computed stand-off point moves. Sim/AiSeat.h's ATTACK_GROUP_SIZE comment already records
      the same arithmetic for attack groups. So the fix wants a stand-off point that is STABLE as
      well as standable - S13's StandableNear is the search, and something has to stop a device
      re-requesting a route it has already failed to get, which is the memory Sim/Movement.cpp
      leaves in the destination.
      The measurements above are reproducible with the probe harness; seed 7 is the case to work
      against and the other six are the regression set.

  - id: G1b
    title: Selection, orders, the pointer's two modes and the match overlays
    intent: >
      The operator drives the slice: picking things, giving them orders, and being told the match
      is over. Every order becomes an OrderMessage and nothing in the executable touches Sim,
      which is what makes the panels of K4 a matter of emitting the same messages.
    project: OutpostCommander
    depends_on: [G1a, N4]
    files:
      - Replica/Selection.h
      - Replica/Selection.cpp
      - Replica/PlacementPreview.h
      - Replica/PlacementPreview.cpp
      - Replica/WarningLine.h
      - Replica/OrderInput.h
      - Replica/OrderInput.cpp
      - Replica/Replica.vcxproj
      - Replica/Replica.vcxproj.filters
      - Tests/ReplicaTests/SelectionTests.cpp
      - Tests/ReplicaTests/OrderInputTests.cpp
      - Tests/ReplicaTests/PlacementPreviewTests.cpp
      - Tests/ReplicaTests/WarningLineTests.cpp
      - Sim/Placement.h
      - Sim/Placement.cpp
      - Tests/ReplicaTests/ReplicaTests.vcxproj
      - Tests/ReplicaTests/ReplicaTests.vcxproj.filters
      - OutpostCommander/CameraController.h
      - OutpostCommander/CameraController.cpp
      - OutpostCommander/App.cpp
      - OutpostCommander/OutpostCommander.vcxproj
      - OutpostCommander/OutpostCommander.vcxproj.filters
      - Design/Interface.md
    acceptance:
      - "Selection by click and drag rectangle through Replica's picking; orders by right click (Move, or Attack on an enemy), by hotkey (attack-move, stop, stances) and, until K4's panels, by hotkey plus click for structure placement with the footprint ghost; every order becomes an OrderMessage and nothing in the executable touches Sim"
      - "The pointer has two modes and AIM is the default (Design/Interface.md §4 and §12 ruling 3, rewritten by the owner on 2026-09-19): in aim the mouse turns the camera with no button held, from Client/RawMouse.h's relative counts at 0.005 radians a count, Windows' pointer is hidden, and the ray a click casts goes through the CENTRE of the screen; in point there is an ordinary pointer and the camera does not turn"
      - "Escape swaps the modes, after peeling an armed order and then a modal panel, and the pause menu moves to F10 - a key that both released the mouse and opened a menu would do one of the two by accident every time"
      - "The drag rectangle, the panels, the minimap and the six screen cursors are point mode's alone, and the camera's arrow keys and screen edges with them; everything else works in both, because §5 and §6 are written about the RAY and not about the mouse"
      - "The camera is not held at a height: Client/Camera.h's floor pushes it up out of the terrain and never pulls it down onto it, which is Species's own MIN_GROUND_CLEARANCE of 10 and MAX_HEIGHT of 5000 (SpeciesLook.md §7), and nothing in this task changes it"
      - "The end of the match shows a win or loss overlay; F10 pauses the local host and shows a menu with resume and quit; Alt+F4 quits. IT SAID ESCAPE UNTIL 2026-09-19 and the line above says Escape swaps the pointer's modes - one of the two had to go, and the owner's ruling 3 moved the menu, on the argument that a key which both released the mouse and opened a menu would do one of the two by accident every time"
      - "A rejected order shows N4's reason as one line of warning text for two seconds, per Design/Interface.md §6"
      - A click moves a unit in about 250 milliseconds over loopback, measured and recorded in the notes
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      SELECTION AND ORDERINPUT ARE IN Replica AND NOT IN THE EXECUTABLE, which is a change from the
      file list this task was written with and not a preference. Build/CheckProjectFiles.py refuses
      any project that lists a source outside its own flat directory, and OutpostCommander is an
      Application, so an .exe exports nothing a test DLL can link against: a suite for logic that
      lives in the executable cannot be built at all. The repository's rules therefore say that
      logic worth testing lives in a library, and these two are the most intricate logic in M1 -
      seven rows of a default-order table, a drag threshold, an armed-order state machine. Replica
      is the right library on its own merits: Design/Interface.md §5 calls this code "R2's picking
      functions, as pure functions over the replica and the camera matrices", Picking.h is already
      there, and no layering edge is new, because Replica is built on Core, Content, Sim and Net and
      this needs only Picking and Sim/Order.h. What stays in the executable is what needs a device:
      App's wiring, the overlays and the camera.
      THE CAMERA LOST ITS LETTER KEYS BY THE OWNER'S RULING ON 2026-09-19. Design/Interface.md §7's
      hotkey table and M0's CameraController collided on two: S was Stop as well as backward, and R
      was Attack-move as well as pitch down. §7 is the document that specifies the game's controls
      and the camera's letters were placeholders from SpeciesLook.md §7 that predate any order
      existing, so the orders win. The camera keeps the arrow keys, the screen's edges, the wheel,
      PageUp and PageDown.
      AND IT LOST THE MIDDLE BUTTON TOO, LATER THE SAME DAY. The owner asked for Species's
      arrangement - the mouse connected to the camera the whole time, Escape releasing it - so
      aiming comes off the buttons altogether. What was weighed against it is that an aimed mouse
      has no pointer, and §5's rectangle, §7's panels and §9's minimap all need one; hence two
      modes rather than Species's single one.
      DO NOT PORT SPECIES'S FREE-MOVEMENT CAMERA, which is a trap this task should not walk into.
      Species has no yaw and no pitch: it moves a virtual cursor, ray-casts it onto the terrain,
      rotates the camera's forward vector toward that world point, and then WARPS the operating
      system's pointer back so it stays glued to it, the screen edge's clamp being what makes the
      turn continue (SpeciesLook.md §7.2). Client/Camera.h stores yaw and pitch, Client/RawMouse.h
      already reads relative counts because TechnicalDesign.md §6.5 wanted them unclamped by the
      screen's edge, and a per-frame SetCursorPos is the thing that behaves worst across two
      monitors and a high-DPI display. Species's own editor camera is the delta camera and is what
      this task ports, at its 0.005 radians a pixel.
      SHIFT-QUEUED ORDERS ARE NOT DONE AND ARE NOT SILENTLY DROPPED. Design/Interface.md §6 and §12
      ruling 5 describe them as a client-side convenience the simulation knows nothing about; no
      acceptance line of this task asks for them, so they are not here. They belong with K4's orders
      panel or with a line of their own, and this note is so that the next reader of §6 does not
      look for them in OrderInput and conclude they were forgotten.
      THE CLICK-TO-MOVE MEASUREMENT, taken on a real Match over the real loopback with the host on
      its own thread, from the moment the order is submitted to the moment the device's drawn
      position changes. Three runs at each of three frame intervals, and they barely move:
        16 ms a frame - the client knew at 113 ms, the commander SAW it at 210 ms
         8 ms a frame - the client knew at 106 ms, the commander SAW it at 204 ms
         1 ms a frame - the client knew at 101 ms, the commander SAW it at 203 ms
      IT IS TWO NUMBERS AND NOT ONE, and the acceptance means the second. The client KNOWS at about
      105 ms: the order waits for the next datagram the client sends, the host runs it on the tick
      after it arrives, and the frame carrying the result goes out on the next even tick. The
      commander SEES it a further 100 ms later, which is INTERPOLATION_DELAY_TICKS - two ticks, one
      publish interval, the delay TechnicalDesign.md §3 spends to absorb an interval of loss and
      jitter. 210 ms at sixty frames a second, against the 250 the acceptance allows, and the frame
      rate is worth about 7 ms of it.
      THE REJECTION LINE IS Replica/WarningLine.h, for the reason Selection and OrderInput are
      there: an Application exports nothing a test DLL can link. It watches SeatState's sequence
      rather than its value, which is the whole reason N4 carried three fields - a frame repeats
      the seat's newest refusal until a newer one replaces it, so a line restarted by the record
      would never expire, and two identical refusals would look like one. Six cases; four
      mutations killed: restarting on every frame, never expiring, keeping the line when the seat
      reports no refusal, and comparing the sequence for ORDER rather than for change, which the
      wrap past 65,535 makes wrong.
      THE FOOTPRINT GHOST'S RULE IS Replica/PlacementPreview.h, which K4's construction panel wants
      too - so it is here, where the acceptance that needs it first is, rather than in the task
      that will colour it. It answers "may a structure of this row stand at this cell" from the
      commander's OWN information: the structures he can see, the fog he has explored, the
      landscape he generated from the definition his join carried, and the deposits in it.
      THE GROUND'S RULES ARE SHARED LITERALLY AND NOT COPIED, which is the whole risk this piece
      carried. Sim/Placement.h gains CheckFootprintGround - on the landscape at all, out of the
      water, not too steep - and CheckPlacement now calls it, so the steepest-cell reading of the
      slope that Placement.h spends thirty lines justifying cannot drift between the ghost and the
      order. A case walks all 1,764 three-by-three sites of a rough landscape through a host-side
      and a client-side copy and asserts they agree site for site; measured, the ground takes 777,
      refuses 326 for water and 661 for slope, so all three answers appear and the comparison is
      not two functions that only ever said Accepted.
      WHAT THE GHOST CANNOT KNOW IS §5.2 AND NOT A FAULT. It has no features - their footprints are
      not on the wire - and no buildings the commander has never scouted, so a green ghost means
      "nothing I know of refuses this" and the host still decides; the refusal that comes back is
      what the warning line above is for. A GHOST BLOCKS, because a building he has seen and walked
      away from is still there as far as he knows. A PLAN DOES NOT, which is the simulation's rule.
      THE FAULT ORDER IS THE SIMULATION'S, occupied before water and slope, and a case pins it on a
      site that is both: a preview that tested the ground first would tell a commander a hillside
      was too steep when what is in his way is his own factory. Eight cases, seven mutations
      killed.

      THE APP WIRING LANDED ON 2026-09-20, in OutpostCommander/Operator.h - the commander at the
      keyboard. IT IS IN THE EXECUTABLE BECAUSE THE LAYERING PUTS IT THERE and not by preference:
      it needs Client's authored-pixel arithmetic and Replica's picking and orders, and ADR-001
      makes those two siblings over Core, so nothing but an executable may hold both. Everything
      that COULD be a library already is - Selection, OrderInput, WarningLine and PlacementPreview
      in Replica, PointerMode in Client - and what is left is wiring.
      IT CARRIES NO DIRECT3D AND NO DirectXMath, deliberately: the camera reaches it as a
      PickCamera, which is sixteen floats and its inverse, so a whole match can be stepped through
      it on a Linux container even though the pass that draws its quads cannot be. MEASURED THAT
      WAY, against a real Match over the real loopback:
        aim is the mode a match opens in
        Escape swaps aim and point, both ways
        an armed attack-move is PEELED FIRST: the same press disarms and does NOT swap the mode
        F10 opens the menu, puts the pointer in point mode and freezes the world - measured at tick
        7 and still tick 7 forty passes later - and the client is not lost while it is frozen
        Escape closes the menu and the world runs on
        a left click over a device selects exactly it; a right click 120 pixels away moves it, from
        x 2,144 to x 2,609
        an order naming an object this commander does not own comes back NotOwned and the line
        "Move: that is not yours" is drawn five milliseconds later and gone two seconds after that
      PAUSING FREEZES THE SIMULATION AND NOT THE HOST, which is a correction to the obvious
      implementation rather than a flourish. A host that simply stopped its thread would stop being
      heard from, and Net's Client reports Lost after the liveness timeout - twenty seconds on a
      loopback match - so a menu left open would drop the commander from his own game. LocalHost
      takes the shape a FINISHED Sim already had: the pacer is forgotten and no tick is run, while
      Host::Advance goes on publishing, acknowledging and counting heartbeats.
      THE CAMERA SPLIT IN TWO ALONG THE MODE (§4's table). In point it has the arrows and the
      screen's edges; in aim it has the mouse, with no button held, at Client/PointerMode.h's rate
      and signs - which are there rather than in the controller because an inverted aim compiles
      either way and is noticed by nobody until somebody plays it. The edges are read only in point
      mode: an aimed pointer is hidden and warped nowhere, so "the mouse is at the edge" would be
      true of wherever it was left and the map would scroll on its own.
      THE BUILD HOTKEY EXISTED IN THE TABLE AND DID NOTHING. OrderKeys named 'B' and nothing read
      it, so five of the six structures were unreachable. §7 gives Build ONE key, so until K4's
      panel gives each row a button the key CYCLES: the first press arms row 0, each press after it
      steps on, it wraps, and another armed order resets it. Four cases.
      AND WHAT IS ARMED IS SAID IN WORDS, above the refusal line, because §4's cursor says "Build"
      and not WHICH build - a cycling key with no readout is a key nobody can use. K4's panel is
      what replaces that line with a row of buttons.
      AbilitiesOf WAS PROMISED BY OrderInput.h AND DID NOT EXIST. The header said a selection's two
      abilities were "built from the replica's device and its seat's design once a frame
      (AbilitiesOf below)" and there was no such function, so nothing in the tree could tell a
      gunner from a builder and §6's table had no input. It NAMES WHAT IT READS rather than taking
      the replica, which is PlacementPreview's rule next door: the devices, the structures, the
      design store and the tables. Ten cases, EIGHT MUTATIONS KILLED and two of them only after the
      cases were corrected - a bad row exactly one past the end of the module table rather than 99,
      because a `>` where a `>=` belongs is invisible at 99; and a second call into the same vector,
      because a helper that appended rather than replaced would grow the selection every frame.
      ONE MUTATION SURVIVES AND IS RECORDED RATHER THAN CONTRIVED AROUND: reading modules[size()]
      is undefined behaviour, and on the Linux probe it reads whatever is there and usually answers
      the same. MSVC's Debug standard library bounds-checks operator[], and CI builds Debug, so it
      dies there; a case that pinned it on the probe would be a case pinning one machine's garbage.
      THE FOOTPRINT GHOST LANDED WITH K7'S PASS, which is what it was waiting for: the UI pass
      draws axis-aligned screen rectangles and a projected footprint is a skewed quadrilateral, so
      there was no way to put a quad on the ground until CursorPass existed. It is the same Emit
      the ring uses at a footprint's corners instead of a cursor's, tinted green or red by
      PlacementPreview's answer - the rule and its eight cases were already here, and what this
      adds is the colouring, exactly as K4's acceptance now says.
      IT IS FLAT AND AXIS-ALIGNED AND LIFTED A WORLD UNIT. Flat because the ground under a footprint
      is about to be LEVELLED: a ghost that followed the slope would show the shape the ground has
      now rather than the shape it will have when the structure stands on it. Lifted because a flat
      quad over ground that is not flat can have a corner under a rise its middle clears, which is
      a case no depth bias answers.
      AND THE CLIENT BUILDS ITS OWN DEPOSIT FIELD for it, from the definition its join carried -
      the same bytes the host built its own from, so the two agree. Without one, PlacementPreview
      refuses an extractor nowhere and every extractor ghost would read legal and be refused by the
      host.
      NOT DONE AND NOT SILENTLY DROPPED: §10's chat line, for the reason the shift-queue note above
      gives - no acceptance line of this task asks for it, and this note is so that the next reader
      of §10 does not look for it and conclude it was forgotten.

  - id: C10
    title: The models are wound the way the renderer lights them, and a wrong one fails rather than warns
    intent: >
      ADR-011 bakes one normal per triangle at load and says the outward normal is cross(c - a, b - a),
      the SDK's handedness - "All 264 triangles of the twenty-two shipped models are wound that way,
      with no exception", measured 2026-09-18 with 0 facing inward. The owner replaced all twenty-two
      placeholders with authored models on 2026-09-19 and the measurement was never redone. CI now
      reports 6,356 of 9,632 triangles wound the other way: two thirds of every model's surface is
      lit from inside. It is a WARNING, so the build stays green and the frames stay wrong.
    project: Tools
    depends_on: []
    files:
      - Tools/ImportObj.py
      - GameData/Models
      - Client/ModelBuffers.cpp
      - Tests/ClientTests/ModelBuffersTests.cpp
      - Design/ADR/ADR-011-model-normals.md
    acceptance:
      - "The cause is established before anything is re-imported: Tools/ImportObj.py carries no winding handling at all and OBJ's convention is counter-clockwise seen from outside, which is the OPPOSITE of what Tools/MakePlaceholderModels.py winds and of what D3D12_RASTERIZER_DESC::FrontCounterClockwise = FALSE calls front. Confirmed against the shipped models rather than assumed, because a model hand-edited after import would not be the importer's fault"
      - The importer winds a face the way ADR-011 requires, and says so where it is easy to find; a model already wound correctly survives a re-import unchanged
      - The twenty-two models under GameData\Models are re-imported or corrected, and ModelBuffers reports ZERO inward-facing triangles over the whole set
      - "A non-zero count FAILS rather than warns. It is the one check that catches this class, it was already there, and it was already telling the truth into a log nobody reads - which is why this shipped. A model that legitimately needs a two-sided face is a content decision to take then, not a reason to keep the gate open"
      - "ADR-011's measurement section is re-dated and re-measured against the models actually in the tree, and says what a re-import must preserve. Its vertex and triangle counts move with it: 264 triangles and 176 vertices were the placeholders', and the authored set is 9,632 and 18,802"
      - The capture of G2 shows models lit from outside, which is what makes this worth doing before it rather than after
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      RULED ITS OWN TASK AND SEQUENCED BEFORE G2 BY THE OWNER ON 2026-09-19, on the same argument
      that put K6 there: the frames G2 uploads should be worth accepting.
      HOW IT WAS FOUND, so the shape is recognisable next time. Not by reading the models and not by
      a failing test - by reading a CI log that had been green for a day. "models: 22 built, 18802
      vertices, 9632 triangles" followed by "models: 6356 triangles wound so that their normal
      points inward". ADR-011 had designed the check, written it, and made it a WARNING; the check
      worked perfectly and told nobody. A number in a log is not a gate.
      AND ADR-011 IS WHY THIS MATTERS RATHER THAN BEING COSMETIC. Its own argument for baking the
      normal was that the winding is the only thing that fixes the SIGN - "getting this backwards
      lights every model from inside and the symptom, everything black, does not say why". The
      geometry pass draws both faces, so nothing disappears; what happens instead is that two thirds
      of the surface takes the wrong dot product against ADR-005's two lights.
      DONE 2026-09-19, AND THE FIRST HYPOTHESIS WAS HALF RIGHT IN A WAY THAT MATTERED. This task
      was written saying the importer had flipped the winding. It had - but the evidence offered
      for it, the 6,356 of 9,632 inward count, does not say that and could not have: a uniform flip
      would report ALL of them, and every model came back MIXED, between 55% and 77%. Two wrong
      explanations were measured and discarded before the right one: that the models were
      non-convex and the count was confused by that (re-measuring against each triangle's own
      FRAGMENT centroid made it worse, 6,657), and that the winding was scrambled rather than
      flipped (welding by position and counting directed edges found 13,930 of 14,576 shared edges
      correctly opposed, and fifteen of twenty-two models perfect).
      THE TEST THAT ANSWERED IT IS THE SIGNED VOLUME, which is exact for a closed mesh where a
      centroid test is a proxy: sum the centre of each face dotted with its outward normal. All
      twenty-two models were negative, decisively, every one. So the models WERE uniformly inside
      out and the count was simply a weak instrument - it under-reports, because a thin plate has
      two large faces with the centroid between them and one of them points at the centroid however
      the plate is wound. Authored models are full of plates.
      SO THE FIX IS BOTH HALVES. Tools/ImportObj.py reverses each face, since an OBJ is
      counter-clockwise seen from outside and ADR-011's normal is clockwise from outside; the
      twenty-two models in the tree were flipped to match, which moves no vertex and leaves edge
      consistency untouched at 390 same-direction edges before and after. And the GATE moved: the
      centroid count drops to 3,276 after the flip rather than to zero, so it can neither pass nor
      fail a build and is logged at Info as a diagnostic, while ModelMesh::signedVolumeSubunits is
      the orientation test and a negative one is logged at Error.
      ONE SUBTLETY THAT WOULD HAVE MADE THE GATE WRONG. The volume must use the RAW cross product,
      not ModelBuffers' Direction, which is divided by its greatest common divisor so that coplanar
      triangles share a vertex key. The terms of the sum do not all share a sign - a face below the
      origin contributes negatively however the model is wound - so re-weighting them per triangle
      can move the total across zero. The reduction is right for the key and wrong for the volume,
      and CrossOfFace now spells the rule once for both.
      WHAT IS LEFT AND IS NOT THIS TASK'S: 390 same-direction edges across seven models and 256
      boundary edges across three. That is open or self-intersecting geometry in the authored
      models rather than a winding fault, it does not change which way a face points, and it is the
      owner's to fix in the models if it ever shows.

  - id: G2
    title: OutpostCommander --capture replays a scripted match and CI uploads its frames
    intent: >
      The agent's only view of the game is the capture artefact (OpenQuestions.md R1), so the
      M0 capture grows into a headless AI-versus-AI match whose frames show bases, devices,
      combat and fog, and the CI job runs it on every pull request.
    project: OutpostCommander
    depends_on: [G1a, K6, C9, C10]
    files:
      - OutpostCommander/App.cpp
      - OutpostCommander/Capture.h
      - OutpostCommander/Capture.cpp
      - Net/Messages.h
      - Net/Messages.cpp
      - Net/Host.h
      - Net/Host.cpp
      - Net/Client.h
      - Net/Client.cpp
      - Tests/NetTests/HostTests.cpp
      - Tests/NetTests/ClientTests.cpp
      - .github/workflows/build.yml
    acceptance:
      - "--warp --capture <landscape> <ticks> <directory> runs LocalHost for <ticks> ticks as fast as the simulation allows, holds a client replica of seat 0's view, renders the scene target from a scripted camera path every hundred ticks and writes the BMPs; exits non-zero on any debug-layer message"
      - "AN OBSERVER CONNECTION IN NET, which is how seat 0 comes to be playing at all: 'two AI seats' cannot be had as G1 wrote it, because Net/Host.cpp's FreeSeat gives a joining client only a seat whose kind is Human, so an all-AI lobby refuses the join with NoSeat and leaves no replica to draw from - proved by G1a's probe, six failures and an empty view. SETTLED BY THE OWNER ON 2026-09-19: a client may join as an OBSERVER of a seat that is already AI. It receives that seat's frames through the ordinary interest and fog path, sees exactly what that commander sees and no more, and its orders are refused rather than applied. The rejected alternative was a scripted order stream for the human seat, which means hand-writing gameplay that exists only for the capture, beside an AI that already plays a match to a decided end"
      - "The observer is a wire change and carries its own tests: a join that asks to observe an AI seat is accepted and one that asks to observe a seat a human holds is refused; an observer's orders are refused with a reason and never reach stage 1; an observer sees the fog of the seat it watches and nothing outside it, which is the §5.2 assertion NetTests already makes for a player"
      - "The CI step runs --warp --capture Slice 9000 Captures and uploads the ninety frames; the frames at ticks 2,000, 4,000 and 6,000 show a base, devices in transit and fog. IT SAID 6,000 UNTIL 2026-09-19 and the tick count is raised on a measurement rather than a preference: with S14's first half fixed the two scripted commanders acquire on tick 6,908 and fire on 6,980, so a capture that stopped at 6,000 could not show combat however the camera was pointed. Nine thousand clears it by two thousand ticks, and the cost is thirty more rendered frames - the simulation itself is a second"
      - "AND AT LEAST ONE FRAME SHOWS A SHOT IN FLIGHT, which is m1-vertical-slice/C8's acceptance moved here because this is where the frames are. C8 built the chain and measured it against a real wire; what only a capture can say is that the scripted camera is pointed somewhere a shot happens, and if it is not, the camera path is what changes. S14'S FIRST HALF MADE IT POSSIBLE: before it, two scripted commanders fired no shot at all in 20,000 ticks; after it they acquire on tick 6,908 and fire on 6,980, which is why the line above now runs the capture for 9,000 ticks rather than 6,000. What G2 owes is a camera pointed at the fighting"
      - "The capture's wall time in CI is recorded in the notes, and the tick count is reduced if it exceeds five minutes. MEASURED AHEAD OF IT: 6,000 ticks of this match with no renderer and no client is 1.0 second on the session's Linux container and 20,000 is 3.3, so the simulation is not what the budget will be spent on - the sixty rendered frames on WARP are, and M0's capture of 300 frames took 44 seconds in CI"
      - "C2's last acceptance line is settled here: this is the first capture that runs with GameData mirrored beside the executable, so the frames confirm the terrain is coloured by the biome's LandscapeDefault.dds and not by the built-in gradient, or C2 is reopened"
    verify:
      - python3 Tools/CheckTaskDag.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
    parallel_safe: true
    status: done
    notes: >
      TWO EDGES ADDED ON 2026-09-19 BY THE OWNER'S RULINGS, both for the same reason K6 was put
      ahead of this task: the frames uploaded here are the only look anybody gets at the game, so
      they are worth accepting or they are noise. C9 gives the event channel a producer, without
      which "the frames show a shot in flight" cannot happen at all - nothing in the tree has ever
      constructed a Net::Event. C10 fixes the model winding, without which two thirds of every
      model in those frames is lit from inside.
      AND THE OBSERVER CONNECTION IS RULED, not left to this task to choose. A client may join to
      OBSERVE a seat it does not hold; it sees that seat's fog and gives no orders. The scripted
      order stream was the alternative and was refused: it is scaffolding M3 replaces, and the
      frames would show a commander playing from a canned list rather than the scripted AI playing.
      M3's spectator wants the observer anyway, so it is built once.

      THE OBSERVER CONNECTION LANDED AHEAD OF THE CAPTURE, because it is the half that can be
      tested without a device and the half the capture cannot start without. A Join names a seat to
      WATCH (Net/Messages.h's observeSeat, NO_OBSERVED_SEAT for an ordinary join); the host accepts
      it for a seat no human owns and refuses it otherwise with NoSeat, and the frames that follow
      are the watched seat's own - the same interest set, the same fog, the same events - so a
      spectator cannot see more than the commander it watches and there is no second encoder to
      keep in step with the first.
      THE RULE IS ON THE SEAT'S KIND AND NOT ON WHO IS CONNECTED. A human's seat is still his while
      his rejoin grace runs, so it may not be watched even when nobody is on it. And an observer IS
      NOT THE SEAT: not what FreeSeat hands the next joiner, not what SeatState reports of the
      seat, and its going quiet is not a commander dropping to AI - nothing waits for an observer,
      so it is let go rather than held, which a leak that only shows after a few hundred
      reconnections would otherwise hide.
      ITS ORDERS ARE REFUSED WITH NotOwned, which is what RejectReason already means - "the order
      names an object the seat does not own, or none at all" - and it reaches the client through
      the latch every refusal uses rather than through a second path. The seat's OWN refusals are
      not folded into an observer's latch: a refusal answers the client that sent the order, and a
      watcher sent none of them.
      FOUR CASES AND EIGHT MUTATIONS, ALL KILLED: watching an AI seat and being refused a human's;
      seeing the seat's fog and nothing outside it, with its own order refused and the seat's
      refusal not reported to it; and a watcher that goes silent being let go with the seat it
      watched untouched. The mutations: letting a human's seat be watched, applying an observer's
      orders, reporting the observer as the seat, ignoring the seat it asked for, folding the
      seat's refusals into its latch, dropping it to AI like a player, and never letting it go.
      TWO SURVIVED AT FIRST AND BOTH WERE THE TESTS' FAULT. Folding the seat's refusals was
      invisible until a case made the watched seat produce a real one and asserted the watcher's
      latch had not moved; and letting a quiet observer linger was invisible until Host gained a
      count of the clients it holds, because every lookup already skips one marked gone. A guard in
      FreeSeat against an observer taking a seat was REMOVED rather than kept untested: that loop
      only looks at Human seats and an observer's is never one, so the guard was unreachable, and
      the invariant is written where the guard was.
      THE CAPTURE ITSELF LANDED ON 2026-09-20, and what it took is three pieces rather than one.
      (1) THE TICK PACING. --capture takes a landscape, a TICK COUNT and a directory now; M0's
      frame count advanced the match by however long those frames took on WARP, so the match a run
      captured depended on how busy the runner was and two runs' frames could not be compared.
      Match gained HostPacing: Threaded is the game, Stepped starts no thread and StepHost runs one
      pass handed exactly a tick's worth - which is the case Core/TickPacer.h already described in
      as many words. The client's own clock is handed the same tick rather than wall time, because
      a liveness clock reading wall time while the host runs at warp would have the host hearing
      from its client once every few hundred ticks.
      (2) THE CAMERA PATH, IN Client AND NOT IN THE EXECUTABLE. Client/CapturePath.h is a script of
      vantages by tick - an aim point on the ground, a bearing, a setback, an elevation, a fog mode
      and whether it follows the fighting - interpolated between, plus ActionCenter, which is the
      mean of the projectiles in a view. All of it is arithmetic over a span, so a camera pointed
      at the wrong valley is a failing assertion on any machine rather than ninety BMPs nobody can
      rerun: twelve cases, NINE MUTATIONS KILLED (the walk one vantage late; no interpolation at
      all; the eye on the far side of the aim; an absolute elevation; an aim at a sea level;
      everything drawn counting as the fighting; an empty sky answered with the origin; the fog
      taken from the vantage being travelled toward; and a tick before the script extrapolating
      backwards).
      (3) A SCRIPT BUILT ON THE MEASUREMENT AND NOT ON A GUESS. The vantages are the island in a
      half orbit (ticks 0-800), the commander's own base (1,200-6,000), the ground the two armies
      meet on (6,900-8,400) and his base again (8,700 on), because that is where S14's numbers put
      the match. The last three follow: the armies walk around terrain rather than through it, and
      a quarter of the way along the line between the two starts is 461 world units from where they
      actually met - close enough to frame at this distance, and not close enough to trust.
      MEASURED, headless, on this session's Linux container with Match driven exactly as the
      capture drives it (no device, so the drawing is CI's to prove):
        the observer joined and had a frame after 3 ticks
        91 frames over 9,000 ticks; 5 of them with a shot in the air, all 5 aimed at the fighting
        the whole stepped match - host, client, replica and render view - took 1.9 seconds
        the followed frames aim at cell (59, 80) at tick 7,000, which is where S14 measured the
        first fight to within a cell
      FIVE FRAMES OF NINETY-ONE IS THIN AND IT IS CHECKED RATHER THAN HOPED FOR. A machine gun's
      projectile lives six ticks and the capture samples every hundredth, so most frames of a fight
      fall between shots. The capture counts them and says so in its log, and the CI step FAILS when
      the count is zero - which is G2's own acceptance line, and the signal that the script is what
      has to change. It is asserted in the step and not in the executable because the exit codes
      mean "could not run" and "the debug layer spoke", and an artefact that is uninteresting is
      neither.
      AND ADR-005'S PAIR IS ONE POSE, ONE TICK AND THE TWO FOG MODES. The ADR says every capture
      re-makes its comparison; M0 did that with frames 100 and 200 of an empty island, which were
      identical apart from the fog. They would not be now - five seconds of a real match separate
      them - so the comparison tick is drawn twice instead, frame-00800.bmp and
      frame-00800-desaturated.bmp.
      CI RAN IT AND EVERY FIGURE AGREES WITH THE HEADLESS ONE (run 151, 6af7a3f, windows-latest,
      WARP, the Debug build with the debug layer on):
        92 frames over 9,000 ticks in 85.8 SECONDS, against the five minutes the budget allows
        0 debug-layer messages
        5 frames with a shot in the air - ticks 7,000, 7,100, 7,200, 8,000 and 8,300, carrying
        7, 4, 2, 8 and 3 shots - and all five aimed at the fighting
        545.8 MB of BMPs, 49.6 MB once upload-artifact has zipped them
      The five frames are the SAME five the Linux probe named, with the same shot counts and the
      same camera positions to the unit. That is the determinism the whole design rests on holding
      across two compilers and two optimisation levels, measured rather than assumed.
      THE ACCEPTANCE FRAMES, from the run's own log: tick 2,000 is 9 instances over the base,
      4,000 is 10, 6,000 is 19 as the army forms, 6,900 is 43 on the way out, and 7,000 is 50 with
      seven shots in the air. The opening sweep runs from (4096, 1789, 409) round to
      (4095, 1789, 7782), which is the half orbit, and frame-00800 and frame-00800-desaturated are
      the same pose under the two fog modes - ADR-005's comparison, re-made on this run.
      AND C2'S LAST ACCEPTANCE LINE IS SETTLED HERE, which is what it was waiting for: the run's log
      reads "palette: ...x64\\Debug\\GameData\\Terrain\\LandscapeDefault.dds" and not "the built-in
      gradient", so the terrain in these frames is coloured by the authored biome palette.

  - id: K4
    title: The M1 panels and the minimap per Design/Interface.md
    intent: >
      The operator manages the economy, designs, research, production and construction through
      the fixed panels Design/Interface.md specifies, reading the replica and emitting orders, so
      that the slice is playable without hotkey lore.
    project: OutpostCommander
    depends_on: [K3, G1b]
    files:
      - OutpostCommander/Hud.h
      - OutpostCommander/Hud.cpp
      - OutpostCommander/App.cpp
      - OutpostCommander/Match.h
      - OutpostCommander/Operator.h
      - OutpostCommander/Operator.cpp
      - OutpostCommander/OutpostCommander.vcxproj
      - OutpostCommander/OutpostCommander.vcxproj.filters
      - Client/Minimap.h
      - Client/Minimap.cpp
      - Client/UiPanel.h
      - Client/UiPass.h
      - Client/UiPass.cpp
      - Client/UiDraw.h
      - Client/UiDraw.cpp
      - Client/Shaders/UiPS.hlsl
      - Client/Client.vcxproj
      - Client/Client.vcxproj.filters
      - Replica/OrderInput.h
      - Replica/OrderInput.cpp
      - Replica/Selection.h
      - Replica/Selection.cpp
      - Content/InterfaceDesc.h
      - Net/Records.h
      - Net/Records.cpp
      - Tools/MakeIconAtlas.py
    acceptance:
      - Every panel Interface.md specifies exists with the content and bindings it gives, reads only the replica, and emits only OrderMessages
      - The design panel shows the derived statistics from Content/DesignStats before the commander commits and refuses a design without a command post; the construction panel shows the footprint ghost coloured by placement legality using the same rules as Sim's Placement, evaluated on the replica's copy of the landscape - THE RULE ITSELF IS ALREADY THERE (Replica/PlacementPreview.h, landed under G1b 2026-09-19, which needed it for the hotkey-plus-click ghost), so what this panel adds is the colouring and not the judgement
      - The minimap draws the fog states, structures, selection and the camera frustum from the render view, and a click on it moves the camera
      - A capture with the panels on shows them legible at 1:1
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      Done 2026-09-20. ONE Hud CLASS AND NOT SEVEN PANEL FILES, against the file list this task was
      written with: every panel of §7 to §9 is the same three operations - Reset, read the replica,
      Add widgets - over the same six UiPanels, and seven translation units of that would be seven
      copies of the id scheme, the layout helpers and the lookups into the content tables. What
      COULD be a library already is: Client/Minimap.h holds the map's picture (15 cases, 12
      mutations killed), Client/UiPanel, UiWidgets, UiLayout and UiDraw hold the toolkit,
      Replica/OrderInput and PlacementPreview hold the order rules, and what is left here is the
      reading of one and the filling of the other. Client/MinimapPass was not needed either: the
      map is one more quad kind through K3's UiPass (t2, UiQuadKind::Minimap), which is one pipeline
      rather than two and cannot disagree with the panel it is drawn inside.
      WHAT THE PANELS COULD NOT SAY IS RECORDED RATHER THAN FAKED. Four gaps were found writing
      them against the document line by line and are Design/Interface.md §11 rows 17 to 20: the
      lobby's settings are not on the wire (§7.5 reads this process's own), an InputEvent carries no
      modifier so §8's Ctrl-click cannot be told from a click, nothing in the tree carries a
      design's NAME so every panel that wants one prints the index, and a UiWidget has no tooltip
      text although §4 gives the tooltip three constants. Row 16's production queue is still open,
      so the Produce tab offers the design list and emits SetProduction and draws the queue not at
      all, saying so. Two gaps were CLOSED on the way: the seat's researched set now reaches the
      client as a 64-bit mask (§11's three tabs could not be filtered without it) and Icons.dds now
      exists as 32 named placeholder cells (row 7).
      §9.3 WAS CORRECTED: it said the stockpile cap was not on the wire and it has been since N1,
      and the income it asked the client to derive would have been a second copy of Sim/Economy's
      service assignment. The readout measures it instead - the rise of SeatState::extractedHundredths
      over the ticks between two readings, sampled over a second - which is exact, includes the
      seat's research upgrades, and copies no rule.
      VERIFIED BY THE CAPTURE AND NOT BY THE COMPILER. The capture draws the panels, selects the
      watched commander's devices so that §8's portrait grid and §9.1's rows have content, and steps
      the command panel's tab frame by frame so all five of §7 are drawn over a run; every frame
      logs how many interface quads it drew and how many devices were selected, and CI throws when
      the lowest is under a hundred or nothing was ever selected. Selection::Hold (4 cases),
      OrderInput::Arm (5) and UiPanel::SetTitle (2) are the pieces this task added to tested
      libraries; every mutation of all three was killed.

  - id: K5
    title: Make the frame time measurable - an unlocked present and a readout
    intent: >
      m0-foundation/T22 asked the owner for the frame time at 1920x1080 and got 8 to 16 ms, which
      is the display's refresh and not the renderer's cost: Client/SwapChain.cpp presents with
      Present(1, 0) and nothing turns that off, and the client reports no frame time anywhere, so
      the figure had to come from outside the process. A milestone whose acceptance is a measured
      frame time needs the client able to measure one.
    project: Client
    depends_on: []
    files:
      - Client/SwapChain.h
      - Client/SwapChain.cpp
      - Client/FrameTimer.h
      - Client/FrameTimer.cpp
      - Client/Client.vcxproj
      - Client/Client.vcxproj.filters
      - OutpostCommander/App.cpp
      - Tests/ClientTests/FrameTimerTests.cpp
      - Tests/ClientTests/ClientTests.vcxproj
      - Tests/ClientTests/ClientTests.vcxproj.filters
    acceptance:
      - "SwapChain::Present takes the sync interval rather than hard-coding 1, and --novsync on the command line presents with 0; tearing is allowed only where the swap chain and the output support it, and the flag says so in its help rather than failing"
      - "A FrameTimer holds a window of recent CPU frame times and reports the mean, the median and the 99th percentile; the window is a count and not a duration, so the figures are comparable between runs"
      - "The client puts those figures in the window title, with the triangle and chunk counts the capture already logs, so a frame time can be read off a running build without a vendor overlay"
      - The unit test covers the timer's percentile over a known sequence, including a window not yet full
      - "m0-foundation/T22's notes are amended with the unlocked figure once the owner has run it, and it is that figure a later regression is caught against"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE LAST ACCEPTANCE LINE IS STILL OPEN and is the owner's: T22's notes carry the unlocked
      figure once the owner has run with --novsync, and it is that figure a later regression is
      caught against rather than the 8 to 16 ms band, which was two monitors.
      Tearing is asked for when the swap chain is CREATED, not when it is presented, so it is asked
      for wherever the machine allows it and --novsync decides per frame whether to use it; the
      query lives on IDXGIFactory5 and GraphicsDevice holds an IDXGIFactory4, which the session's
      Linux syntax check caught. ResizeBuffers takes the same flag or it refuses. Where the output
      does not allow tearing, --novsync still presents on the retrace: the log says so and the
      title bar says so, rather than reporting a refresh interval as a frame time a second time.
      The window is a COUNT of frames and not a duration. "The last second" holds 60 samples at 60
      frames a second and 500 at 500, so its 99th percentile means a different thing at each rate
      and two runs cannot be compared; 240 frames always reports the same statistic. The percentile
      is nearest-rank, so it always names a frame that actually happened.
      Three figures rather than one because a hitch is invisible in the mean and in the median and
      is what a player feels: a test pins that one 40 ms frame among 199 of 4 ms moves the
      hundredth percentile and moves the mean by 180 microseconds.
      The suite cannot be run on this session's Linux harness - ClientTests does not link there,
      because Window.cpp needs the real Win32 - so FrameTimerTests was built and run against
      FrameTimer.cpp alone, six passing. CI is the first run of the whole project.

  - id: G3
    title: The owner's run - M1 accepted, and the tick-cost measurements added to the tick ADR
    intent: >
      M1 is done when the owner has played a match to annihilation on a running build
      (GameDesign.md §12), and the tick ADR of M0 gets the measurement it could not have then.
    project: Design
    depends_on: [G2, K4, C3, S12, S15]
    acceptance:
      - The owner plays the slice against the scripted AI to a decided end on hardware, and records the frame time, the perceived order delay and what felt wrong in this task's notes
      - "The tick ADR gains a dated measurement section: the cost of a full tick at the slice's object counts, the visibility refresh's share, pathing's nodes per tick, and the host thread's headroom, measured with the log on the owner's machine and on CI's WARP run"
      - Every M1 line of GameDesign.md §12 is true, and this plan is moved to tasks/Archive/
    status: todo
    notes: >
      Owner's task. An agent prepares the ADR addendum from the capture run's log and the PR
      that archives the plan once the owner reports.
      S15 WAS BLOCKING THE FIRST ACCEPTANCE LINE AND NOBODY KNEW, which is why it is a dependency
      now: "plays to a decided end" was not reachable at all, because the scripted commander was
      seated without auto-research and a machine gun cannot destroy a command post. Measured
      2026-09-20 and fixed; the lobby the game ships is seed 1, which now decides in 12,874 ticks -
      about eleven minutes of play, which is the right length for this acceptance.
      TWO THINGS TO EXPECT ON THE DAY. S16 is open: a device ordered onto a BUILDING asks the
      planner for a route into it and stands still, so a right-click attack on a structure may not
      advance - the default order on an enemy structure is Attack (Design/Interface.md 6), so this
      is reachable in ordinary play. And NOTHING HAS EVER RUN THE WINDOWED BUILD: CI runs --capture
      only, so the panels have been drawn but the whole input path - clicks, the minimap, the pause
      menu, every hotkey - executes for the first time when the owner launches it.

  - id: C5
    title: The content tree in the simulation, and the snapshot's binding to it
    intent: >
      Every simulation system from S3 on reads rows rather than literals, which nothing made
      possible: Sim took MatchSettings and nothing else, and no task owned the wiring
      (OpenQuestions.md Q20, owner 2026-09-18). Sim takes the tree by reference and never writes
      it, and a snapshot carries the digest of the tables it was written against so that reloading
      against different rules is a refusal rather than a divergence nobody notices.
    project: Sim
    depends_on: [C1, S1]
    blocked_by: []
    files:
      - Content/ContentHash.h
      - Content/ContentHash.cpp
      - Content/DamageTable.h
      - Sim/Sim.h
      - Sim/Sim.cpp
      - Sim/Snapshot.h
      - Sim/Snapshot.cpp
      - Design/ADR/ADR-009-content-in-the-simulation.md
      - Tests/ContentTests/ContentHashTests.cpp
      - Tests/SimTests/SnapshotTests.cpp
    acceptance:
      - Sim takes the ContentTree by reference at construction and exposes it; binding a temporary is a compile error rather than a dangling pointer found at run time
      - ContentHash digests every field of every table the simulation reads - components, structures, research, the damage matrix - and nothing the client alone reads, with strings length-prefixed so two rows whose ids run together cannot digest as one
      - A snapshot carries the digest and Snapshot::Read refuses a tree that differs, before it reads a field; the state hash is untouched, because the digest is a precondition rather than per-tick state
      - ContentHashTests moves one field of every table and each mutation moves the digest, so a field added to a row without a line in the digest fails a test rather than diverging in silence
      - ADR-009 records the decision, what it forecloses - the self-describing snapshot that would outlive its tables - and what it cost
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done

  - id: C6
    title: The chrome palette and the eight commander colours as content - GameData/Interface.json and its loader row
    intent: >
      Design/Interface.md §3 specifies the panel palette as a table so that the look of a panel is
      data, and GameDesign.md §11 now names the eight commander colours; neither exists as a file.
      ADR-006 recorded the gap and gave it to C2, and C2 landed done without it - its acceptance
      lines never mentioned it - so K3 would spell the palette as constants and nobody would own
      the move to data, and R2 would have no table to resolve a colour index against.
    project: Content
    depends_on: [C1]
    files:
      - GameData/Interface.json
      - Content/InterfaceDesc.h
      - Content/ContentTree.h
      - Content/ContentLoader.h
      - Content/ContentLoader.cpp
      - Content/ContentValidator.cpp
      - Content/Content.vcxproj
      - Content/Content.vcxproj.filters
      - Tests/ContentTests/InterfaceDescTests.cpp
      - Design/ADR/ADR-006-content-format.md
    acceptance:
      - "GameData/Interface.json carries the seventeen chrome roles of Design/Interface.md §3 with the values that document gives, in 8-bit RGBA, and the eight commander colours of GameDesign.md §11 in seat order"
      - "Content/InterfaceDesc.h is a plain row aggregate (AGENTS.md R8): the seventeen roles as named fields and the commander colours as an array of MAX_SEATS; ContentTree holds one, loaded with the other tables by the same loader"
      - "The loader refuses, with the file and line ContentLoader already gives: a missing role, an unknown role, a channel outside 0 to 255, and a commander list that is not exactly eight long"
      - OutpostHost --validate passes over the tree with the new file, and fails with a located diagnostic when a role is removed from it
      - ADR-006's open point that nothing loads Interface.json is struck, dated, naming this task
      - "ContentTests covers: every role parsed to the value §3 gives, the eight commander colours in seat order, and each of the four refusals"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE FILE LIST WAS WRONG IN BOTH DIRECTIONS. Content/ContentValidator.cpp is NOT touched: the
      validator is for rules that span files and the palette has none - every fault it can have is
      inside its own document, which is the loader's half. Four files it did not name are:
      Tests/ContentTests/BrokenFixtures.h, because LoadContent now requires Interface.json and every
      fixture tree in the suite would otherwise stop loading; the two ContentTests project files; and
      Sim/Sim.cpp, for the static_assert below.
      THE SEAT COUNT IS IN TWO PLACES AND HAS TO BE. Content may not include Sim (AGENTS.md R9), so
      Content/InterfaceDesc.h declares COMMANDER_COLOR_COUNT and Sim/Sim.cpp static_asserts it
      against MAX_SEATS - the first translation unit where both are visible. Without it, raising
      MAX_SEATS would draw the new seats in whatever the array was initialised to.
      THE READER CHECKS FOR MEMBERS IT WAS NOT ASKED FOR, which no other table does. Every other
      table is a list of rows with ids; this one is a fixed set of names, so a misspelled role is
      not a row that fails to resolve but a field left at its default and a panel drawn in a colour
      nobody chose. The second pass over the object is what turns that into a diagnostic.
      THE MEMBER IS CALLED ui AND NOT interface, because the Windows SDK defines `interface` as a
      macro for `struct`. It compiles everywhere except the platform this game is built on.
      Verified in the session on Linux: CheckTaskDag, CheckFormat (18.1.3) and CheckProjectFiles
      clean - the last of which caught three test names spelling 'colour' against R11; a
      -fsyntax-only pass over ContentLoader.cpp and Sim.cpp; and the loader itself built against
      Core/Json.cpp and run over the real GameData, which reads all seventeen roles and all eight
      seats, and over five broken copies, which give:
        missing role      Interface.json(3,13): the member 'accent' is missing
        unknown role      Interface.json(21,16): 'acccent' is not a chrome role of Design/Interface.md 3
        channel 256       Interface.json(11,22): 'accent' is between 0 and 255
        seven commanders  Interface.json(22,17): 'commanders' holds one colour a seat, which is 8
        empty chrome      Interface.json(3,13): the member 'panelFill' is missing
      Line 21 for the unknown role is 4 + CHROME_ROLE_COUNT, which is what the test computes rather
      than counts, so adding a role does not silently move an assertion.
      ONE COLOUR PAIR IS CLOSE AND IS NOT THIS TASK'S TO FIX. Seat 2's yellow is 93 from the chrome
      accent over the three channels, where every other seat is at least 200; seat 2 is Species's
      team 2 kept byte for byte and the accent is Design/Interface.md 3's own, so neither end was
      this table's to move. It is asserted at 93 by name rather than hidden under a low threshold,
      and GameDesign.md 11 records that the accent moves if a running build shows it too weak. The
      three chosen colours were moved from the first draft to get the closest commander pair up to
      145, which is now seat 3 against seat 7 - both Species's - so the limit is the lineage's.
      CI IS GREEN on 5393ba3, run 98, dispatched onto this branch: the workflow triggers on main
      and on pull requests, so a branch that has neither needs workflow_dispatch to be read at all.
      The MSVC build, every suite, clang-tidy, the format job, the plan validation and the capture
      all passed, and OutpostHost --validate passed over the tree with Interface.json in it.

  - id: N4
    title: The order-rejection wire record - a seat's last refusal reaches the commander who sent it
    intent: >
      Sim records why it dropped an order - Sim/Order.h's RejectReason, collected into Sim/Seat.h's
      rejections - and Design/Interface.md §6 draws it as one line of warning text for two seconds.
      SeatState carries no such field: Interface.md §11 gave the record to N1 and N1 landed done
      without it, so the operator gets no answer when an order does nothing and K4 cannot be
      finished.
    project: Net
    depends_on: [N1, N2]
    files:
      - Net/Records.h
      - Net/Records.cpp
      - Net/FrameEncoder.h
      - Net/FrameEncoder.cpp
      - Net/Host.h
      - Net/Host.cpp
      - Tests/NetTests/RecordsTests.cpp
      - Tests/NetTests/HostTests.cpp
      - Design/Interface.md
    acceptance:
      - "SeatState carries the seat's most recent rejection as three FIXED-SIZE fields - a wrapping rejectSequence, the OrderKind and the RejectReason - and not as a list: §6 draws one line and replaces it, and a variable-length field in a record the delta encoder compares by value costs every frame for a display that shows one line at a time"
      - "Host latches each seat's rejections in Advance, on every tick, because Sim clears them at the start of every stage 1 (Sim/Sim.cpp:244) and publishes only on even ticks (Sim/Sim.cpp:599) - reading the vector at publish time drops every odd tick's refusals outright"
      - "The reader refuses an OrderKind at or over ORDER_KIND_COUNT, a RejectReason at or over REJECT_REASON_COUNT, and Accepted as a reason, the way Read(SeatState) already refuses a seat and a victory state"
      - "rejectSequence changes on a repeat of the same rejection, so a commander who asks twice for what he cannot afford sees the line restart rather than appearing not to have been heard"
      - "NetTests covers: the round trip, the three refusals, a rejection raised on an odd tick reaching the next frame, and two identical rejections yielding two different sequences"
      - Design/Interface.md §11 row 3 is struck
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE PROTOCOL VERSION IS BUMPED TO 2 and SEAT_STATE_BYTES from 34 to 38. Records.h's own rule
      is that any layout change in it bumps the version, and nothing has shipped against 1, but the
      bump is not skipped for that: a version bumped only when someone might notice is a version
      nobody can reason about.
      THE FIELDS ARE FIXED-SIZE AND THERE ARE THREE. Design/Interface.md 6 draws one line and
      replaces it, so a list would be compared and sent every frame for a display that shows one at
      a time. The third field is the sequence, and it is the one that earns its place: two identical
      refusals are equal field for field, so without it the line would not restart and a commander
      asking twice for what he cannot afford would read it as not having been heard. It counts
      refusals rather than naming a tick, so the client compares it for inequality and never for
      order, and it steps over 0 when it wraps because 0 is how a seat that has had none says so.
      THE READER REFUSES A REASON THAT DISAGREES WITH ITS SEQUENCE, both ways round: Accepted with
      a sequence, and a real reason without one. A host that sent either has an encoder that is
      wrong, and the alternative is a client drawing a line of warning text with nothing in it.
      THE LATCH IS ON ClientView AND NOT BESIDE THE SIMULATION'S SEAT. A refusal is reported to the
      commander who sent the order: a seat playing under AI has nobody to tell, and a client that
      joins has no business being handed a refusal from before it arrived. Both fall out of the
      latch starting empty with the view rather than needing a rule.
      Verified in the session on Linux: CheckTaskDag, CheckFormat and CheckProjectFiles clean;
      -fsyntax-only over Host.cpp, Records.cpp and FrameEncoder.cpp; and Records.cpp built against
      a stub and run, which prints the 38 bytes the test pins - they match byte for byte - confirms
      the round trip is equal, and confirms all four refusals refuse and that a seat with no refusal
      still reads. The two host tests need the real Sim and are CI's.
      THE HOST TEST RUNS THE SCENARIO AT BOTH PARITIES rather than asserting one. Which tick stage 1
      judges an order on depends on when the reliable stream delivered it, and a test that assumed
      a parity would pass while proving half of what it is for; it asserts instead that across the
      two runs both an odd and an even tick were seen, and fails if they were not.
      CI IS GREEN on 0b9d01e, run 99, dispatched onto this branch: the MSVC build, every suite,
      clang-tidy, the format job, the plan validation and the capture all passed, which is where
      the two host tests and the round-trip tests were actually run.

  - id: C8
    title: What a shot looks like - the projectile's appearance, so that combat is visible
    intent: >
      TechnicalDesign.md §5.3 sends projectiles and effects "as short-lived events rather than
      objects", which is what m1-vertical-slice/R2's acceptance means by "short-lived projectile
      instances from events" - but nothing anywhere says what a shot LOOKS like. ModuleDesc carries
      a model whose MarkerMuzzle is where the shot leaves, and no projectile model, no tracer and no
      lifetime, so there is nothing for the render view to draw. Found while writing
      RenderViewBuilder (R2's notes).
    project: Content
    depends_on: [C1, R2, C9]
    files:
      - Content/ComponentDesc.h
      - Content/ContentLoader.cpp
      - Content/ContentValidator.cpp
      - GameData/Components.json
      - Replica/RenderViewBuilder.h
      - Replica/RenderViewBuilder.cpp
      - Tests/ReplicaTests/ConvergenceTests.cpp
      - Design/Interface.md
      - Design/TechnicalDesign.md
    acceptance:
      - "A weapon row says what its shot looks like and for how long - at the least a model and a lifetime in ticks - validated like every other row, so that a Shot event has something to draw"
      - "SETTLED BY THE OWNER ON 2026-09-19: a shot is a short-lived INSTANCE of a placeholder projectile model, travelling from the firing module's MarkerMuzzle toward its target, drawn by K1's geometry pass like anything else. Core/RenderView.h's RenderInstanceKind::Projectile already exists and the pass already draws instances, so what this task adds is one authored model, a lifetime, and the event-to-instance walk - no new pass and no new shader. The rejected alternative was a tracer beam: no model content, but a world-space line pass that does not exist, which is more renderer work rather than less"
      - "GameData/Models gains the placeholder projectile, authored like the twenty-two C4 already ships, and the weapon row names it"
      - "RenderViewBuilder turns a Shot event into instances of kind Projectile between the muzzle and the target for the stated lifetime, placed from the event's own tick so that it is not drawn a frame early or late, with a ReplicaTests case"
      - "EVIDENCE THIS TASK CAN PRODUCE, which is what this line used to lack. It named G2's capture - a frame G2 writes, using a camera G2 scripts, in a match G2 runs - so it could not be judged here whatever the code did, which is the same circularity G1a's acceptance had. What C8 can show is the chain end to end: a Shot event that crossed a real wire becomes an instance of RenderInstanceKind::Projectile at a measured point between the muzzle and what it was aimed at, and is gone when its lifetime is over. The capture itself is checked in G2, where the frames exist"
      - Design/Interface.md §11's projectile row is struck
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      THE CONTENT HALF IS DONE AND PUSHED; THE REST IS BLOCKED ON C9, WHICH THIS TASK FOUND. A
      weapon row now says what its shot looks like and for how long (ComponentDesc.h's
      projectileModel and projectileLifetimeTicks), the loader reads them, the validator checks the
      model like any other model reference and refuses one half of the pair without the other or a
      projectile on a system module, ModelComposer resolves the projectile once at construction
      beside every other model, and GameData\Components.json gives the three weapons the
      EffectShell the owner already authored - 6 ticks for the machine gun, 12 for the cannon, 36
      for the mortar, which is the mortar's own flight time at long range. Five cases, three
      mutations, all killed.
      WHAT IS NOT DONE IS THE WALK FROM A Shot EVENT TO AN INSTANCE, and it is written and waiting
      rather than guessed at: RenderViewBuilder remembers each shot it has seen (an event is
      replaced by every Apply, so a shot outliving one frame has to be kept, exactly as the flatten
      is), takes the muzzle from the shooter's own composition, and interpolates from muzzle to
      target over the lifetime. It cannot be COMMITTED because it cannot be tested: no Shot event
      exists to drive it.
      BECAUSE THE EVENT CHANNEL HAS NO PRODUCER AND WOULD LOSE MOST OF WHAT IT CARRIED. Measured,
      not read: nothing in the tree ever constructs a Net::Event - not one of the seven kinds, and
      Host::Events() is "filled by the host loop" by nobody, with LocalHost::Pass running
      Sim::Advance and then Host::Advance with nothing in between. And an event injected by hand
      into a real match over the loopback was published, passed the interest filter, and was then
      DROPPED by the client: the frame carrying it was a delta against baseline 16 while the client
      had applied 18. Over one real match: 105 frames applied, 360 skipped - 77% - on a transport
      that loses nothing. State survives that, because every frame re-sends the difference from the
      acknowledged baseline; an event is the one payload a dropped frame destroys. C9 owns both.
      G2'S CAPTURE LINE IS C9'S TO MAKE TRUE. "G2's capture shows a shot in flight in at least one
      frame" cannot hold while three shots in four would be thrown away even once something fired
      one, so this task closes after C9 rather than claiming it.
      WHAT LANDED ONCE C9 UNBLOCKED IT. RenderViewBuilder keeps the shots it has seen fired, for
      the same reason it keeps what it has flattened: Replica::Events() holds the NEWEST frame's
      and is replaced by every Apply, and a shot is drawn for the lifetime its weapon row names,
      which is longer than a frame. The muzzle comes from the shooter's own composition - the
      module's MarkerMuzzle carried through the module's transform and then the device's - taken
      while the device is composed rather than by composing it twice.
      A SHOT WHOSE SHOOTER IS NOT DRAWN IS NOT DRAWN EITHER, which is TechnicalDesign.md §5.2
      holding without a second mechanism: the host does not send the event to a commander who
      cannot see the shooter, and if one arrived anyway the muzzle it would leave from does not
      exist in this view.
      A DEFECT THE CASE FOUND, WHICH IS WHY IT IS NOT IN RenderViewTests. A client draws many
      frames inside one publish interval, and Replica::Events() is not emptied when no frame
      arrives - so the builder took the same events again on every drawn frame and put a second
      shot in the air for each. It takes them once per frame now, keyed on Replica::NewestSequence,
      which moves exactly when Events() is replaced. Nothing short of the whole stack would have
      shown it: a test that fed the builder one view saw one shot.
      MUTATIONS RUN AND KILLED, six: never dropping an expired shot; firing at a fixed tick rather
      than the event's; starting the shot at the device rather than the muzzle; taking a frame's
      events on every build; drawing a shot before its tick; and aiming it at the shooter rather
      than at the point. TWO OF THEM SURVIVED FIRST TIME and the case was what was wrong, not the
      code: the fixture's weapon mount was one world unit off the floor, inside the tolerance the
      interpolated position needs, so "it leaves from the muzzle" was true of a builder that
      started every shot at y = 0. The mount is eight world units up now and the tolerance is a
      tenth. And nothing built at a render time BEFORE the trigger, so the guard against drawing a
      shot backwards down its own flight was never exercised; there is a case for it now.
      THE CAPTURE LINE MOVED TO G2, where the frames are. It named evidence this task could not
      produce - a frame G2 writes with a camera G2 scripts - which is the same circularity G1a's
      acceptance had and is fixed the same way.

  - id: C9
    title: The event channel gets a producer, and events survive the frame that carries them
    intent: >
      TechnicalDesign.md §5.3 sends shots, impacts, deaths, completions, production, research and
      promotions "as short-lived events rather than objects", and Net/Records.h names all seven.
      Nothing in the tree has ever constructed one. Host::Events() is documented as "the host loop
      fills this from what the tick did" and no host loop fills it, so every effect the client could
      draw - a tracer, an explosion, a building finishing - is a wire feature with no producer. And
      an event that WAS produced would usually be thrown away, because the frame carrying it is
      dropped in the ordinary case rather than the lossy one. Found while writing C8, measured
      rather than reasoned about.
    project: Net
    depends_on: [S10, N2]
    files:
      - Sim/Sim.h
      - Sim/Sim.cpp
      - Sim/Targeting.cpp
      - Sim/Weapons.h
      - Net/Host.h
      - Net/Host.cpp
      - Net/Client.h
      - Net/Client.cpp
      - Net/ClientHistory.h
      - Net/FrameEncoder.h
      - Net/FrameEncoder.cpp
      - Net/Messages.h
      - Net/Messages.cpp
      - OutpostCommander/LocalHost.h
      - OutpostCommander/LocalHost.cpp
      - Tests/SimTests/CombatTests.cpp
      - Tests/NetTests/HostTests.cpp
      - Tests/NetTests/RecordsTests.cpp
      - Design/TechnicalDesign.md
      - Design/ADR/ADR-012-replication-protocol.md
    acceptance:
      - "The simulation records what a tick's combat did, in a per-tick scratch of its own kind - Sim may not name a Net type - covering BOTH fire kinds: direct fire leaves no Projectile at all (Sim/Targeting.cpp says so: 'what flies is the client's business and carries no record'), so an event is the only trace it can ever have, and indirect fire creates one and must not be reported twice"
      - The host loop turns that scratch into Net Events between Sim::Advance and Host::Advance, which is the one place it can go and the place Host.h already says it goes; OutpostCommander/LocalHost.cpp is that loop in M1
      - "AN EVENT SURVIVES A FRAME THE CLIENT DROPS, which is the half that is not bookkeeping. Measured on a real match over the loopback: 105 frames applied and 360 skipped, 77%, on a transport that loses nothing - the publish outruns the acknowledgement, which is the ordinary case and not a lossy link. State survives it because every frame re-sends the difference from the acknowledged baseline; an event is the one payload a dropped frame destroys. The fix is the shape G1a already used for the fog: carry a frame's events in its FrameRecord and re-send those the client has not acknowledged, so that a dropped frame costs a delay rather than the event"
      - "A test that measures it rather than asserting it: over a scripted match, every event the host recorded reaches the seat entitled to see it, and the count of events produced equals the count received"
      - A Shot event names enough to draw a shot - at minimum the shooter, the point aimed at and the tick - and m1-vertical-slice/C8's builder walk lands with it, with the ConvergenceTests case that was written for it and could not be committed
      - "SHOT ALONE IN M1, RULED BY THE OWNER ON 2026-09-19. Destroyed, Completed, Produced, Researched and RankedUp have no consumer in M1's client, so producing them now is wire traffic nothing reads; they stay named and unproduced. What this task may NOT do is leave the channel unable to carry them - the producer is a list the host loop walks, so a second kind is a second entry in it and not a second mechanism"
      - TechnicalDesign.md §5.3 records that an event is re-sent until acknowledged, because "a client that missed a frame missed its events" is what the design says today and is what the measurement contradicts
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      WHY THIS WAS INVISIBLE FOR SO LONG, recorded so the next gap of this shape is found earlier.
      Both ends of the channel exist and both are tested. N1 tests that an Event survives a round
      trip byte for byte. N2 tests that a frame carries only the events a commander may see.
      Replica applies them. Every one of those tests supplies its own events, so the suite proves
      the pipe holds water and never asks whether anything is pouring any in. It is the same shape
      as K6 - four tasks each did their own half correctly and the seam between them was nobody's -
      and the same shape as the fog defect G1a found, where the correctness suite passed on an
      all-black map because it only compared the grid's SIZE.
      THE MEASUREMENTS, so that the next reader starts from evidence. Nothing constructs a
      Net::Event anywhere in the tree - grep for the type's construction and the accessor's use.
      LocalHost::Pass runs Sim::Advance and then Host::Advance with nothing in between. An event
      injected by hand into a real match was published and passed the interest filter (source known
      = 1), and was then dropped by Net/Client.cpp's TakeFrame: "seq=19 baseline=16 applied=18".
      Over one real match on the slice landscape, the client applied 105 frames and skipped 360.
      WHAT LANDED, AND THE SECOND DEFECT IT UNCOVERED. The producer is Sim/Weapons.h's SimShot and
      Sim::Shots(), a per-tick scratch like Sim/Damage.h's DamageEvent: written by Targeting's Fire
      BEFORE it branches on the fire kind, so direct and indirect are recorded once each and a
      salvo is one record rather than one a pellet. LocalHost::Pass turns it into Net Events INSIDE
      the tick loop, because a pass that owed four ticks would otherwise report the fourth only.
      THE SURVIVAL HALF IS A QUEUE ON THE HOST AND A COUNT ON THE CLIENT, and the first attempt at
      it was wrong in a way only a test caught. ClientView::pendingEvents holds what a client has
      been sent and not acknowledged, every publish sends the whole queue, and an acknowledgement
      drops what the acknowledged frame carried. FrameRecord::eventsSent was first written as THIS
      FRAME'S COUNT, which is wrong because the acknowledged sequence does not move on every
      publish - the publish outruns the acknowledgement, which is the whole premise of this task -
      so the same record is folded again and again and a per-frame count drops its own events from
      the front every time. Measured: 40 shots over a link dropping a third of its frames, and 15
      arrived. It is a RUNNING TOTAL now, against ClientView::eventsForgotten, so folding the same
      record twice is a difference of zero.
      AND A THIRD: THE SAME EVENT COULD BE DRAWN TWICE. A client that applies two frames before its
      acknowledgement reaches the host is sent both lots of the queue, and one trigger pull becomes
      two muzzle flashes. The frame now carries firstEvent - where its events sit in the stream this
      client has been sent - and the client keeps its own count and takes only the part it has not
      applied. Four bytes a frame, measured against the same match: a full frame at a hundred
      visible objects went from 3,685 to 3,689 and a delta from 641 to 645. ADR-012's table is
      updated. A REJOIN sends none of the queue: the full frame catches the state up, which is what
      a rejoin is for, and a burst of flashes from shots fired while the commander was away is not.
      THE MEASUREMENT THE ACCEPTANCE ASKED FOR: 40 shots produced, 40 received, none twice, over a
      link dropping a third of its frames (NetTests::HostTests::AnEventOutlivesTheFrameThatDropped-
      ItAndReachesItsCommanderExactlyOnce), and over a real match of two hostile devices on the
      slice landscape, 240 shots produced and 240 received with 50% of frames dropped.
      MUTATIONS RUN AND KILLED. Removing the record kills both CombatTests cases; moving it inside
      the direct-fire branch kills the shell case and leaves the direct one passing, which is the
      discrimination that case exists for; never clearing Sim::Shots() kills the scratch assertion;
      sending only this interval's events instead of the queue takes the loopback match from 240 to
      122; never folding an acknowledgement takes it to 18,002, which is why the probe's condition
      is equality and not "at least". SURVIVING, and recorded rather than hidden: a mutation that
      recorded one shot per PELLET of a salvo is not killed, because every weapon in CombatTests'
      fixture and in GameData has shotsPerSalvo of 1, so there is no salvo to tell apart yet.
      WHAT IS NOT IN THIS TASK. The walk from a Shot event to a drawn projectile is C8's, which
      this unblocks and which holds the ConvergenceTests case written for it. And Shot is the only
      kind produced, by the owner's ruling: the producer is a list the host loop walks, so a second
      kind is a second entry in it.

  - id: K6
    title: The client applies the flatten and re-meshes the chunks the render view names
    intent: >
      Core/RenderView.h's `changedChunks` has no consumer and never has had one. RenderViewBuilder
      works out which terrain chunks the structures this commander can SEE have flattened - the one
      piece of state it keeps, and correct, including for a razed building - and hands them over;
      nothing reads them. Nor does anything write the flatten into the client's own Landscape:
      Sim/Landscape.h has ApplyDelta, and Match never calls it, because RenderViewBuilder holds no
      landscape and only reports the change. So every structure in M1 stands on unflattened ground
      on the client, and the capture - the only picture anyone reviews - shows bases half-buried in
      hillsides. Found while writing App (G1a); the owner ruled on 2026-09-19 that it is its own
      task and that it comes before G2, so that the frames G2 uploads are worth accepting.
    project: Client
    depends_on: [G1a]
    files:
      - OutpostCommander/Match.h
      - OutpostCommander/Match.cpp
      - Client/TerrainPass.h
      - Client/TerrainPass.cpp
      - Client/TerrainChunk.h
      - Client/TerrainChunk.cpp
      - Client/Client.vcxproj
      - Client/Client.vcxproj.filters
      - Tests/ClientTests/TerrainChunkTests.cpp
      - Tests/ClientTests/ClientTests.vcxproj
      - Tests/ReplicaTests/ConvergenceTests.cpp
    acceptance:
      - "Match levels its own landscape under every structure the replica holds, from the footprint of the structure's row and the flattened height StructureState carries, using Sim/Placement.h's FlattenDelta and Landscape::ApplyDelta - the same arithmetic Sim/Construction.cpp runs on the host, so the two agree sample for sample"
      - "The flatten follows the fog and nothing else: a structure this commander cannot see does not flatten his ground, and a ghost flattens it exactly as the structure did when he last saw it, because that is what he remembers (TechnicalDesign.md §5.2 - 'the terrain under an unscouted base is not public')"
      - "TerrainPass re-meshes the chunks the render view names, at all four strides, into the ranges those chunks already own: a chunk's vertex and index counts are fixed by its stride and only the heights move, so the buffers keep their layout and a rebuild is an upload and a copy rather than a reallocation"
      - "A rebuild costs only the chunks that changed: a base of a dozen structures standing still rebuilds nothing on the frame after it arrived, which is what RenderViewBuilder's remembered flatten is for"
      - "Tests: the chunk rebuild against a synthetic height change, on the CPU, with no device; and a full-stack ConvergenceTests case that a structure the client can see leaves his landscape agreeing with the host's under its footprint, while one he cannot see leaves it untouched"
      - The capture of G2 shows structures standing on level ground
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      WHY IT FELL BETWEEN THE TASKS, recorded so the next gap is found earlier. S4 flattens on the
      HOST and stops there, correctly - the wire carries no height deltas, because they would hand
      a commander the shape of ground he has never scouted. R2 computes the changed chunks and says
      in its own notes that "the client generates the landscape from the definition it was given at
      the join and flattens it under the structures it can SEE" - which describes work and assigns
      it to nobody. K1 is the geometry pass and K2 is the fog pass, and neither touches terrain.
      G1a's acceptance is the two loops and says nothing about ground. Four tasks each did their own
      half correctly and the seam between them was never anybody's.
      THE RE-MESH IS THE REAL WORK AND THE SHAPE OF IT IS ALREADY DECIDED BY K1's BUFFERS.
      TerrainPass builds every chunk at four strides once, in its constructor, into one vertex and
      one index buffer, and holds only offsets afterwards; there is no rebuild path at all. What
      makes it tractable is that a flatten changes HEIGHTS and not TOPOLOGY: a chunk at a given
      stride has the same vertex and index counts whatever the ground does, so the rebuilt mesh
      fits the range it already occupies and the index buffer never moves. An upload heap and a
      CopyBufferRegion per changed chunk per stride is the whole mechanism.
      DO NOT REACH FOR THE HOST'S LANDSCAPE. It is one member away through Match and it is
      flattened under every structure in the match. Using it would make this task trivial and ship
      a wallhack; the point of the client holding its own Landscape is exactly this moment.
      DONE 2026-09-19. THE FILE LIST WAS SHORT BY TWO AND THE SEAM RUNS THROUGH BOTH OF THEM.
      Core/RenderView.h gains TerrainFlatten and a `flattens` list beside changedChunks, and
      Replica/RenderViewBuilder fills it in the very loop that fills changedChunks. The alternative
      was for Match to walk the replica's structures itself and keep its own record of what it had
      already levelled - and that is a SECOND copy of the one piece of state R2's header calls "the
      one piece of state it keeps, and correct, including for a razed building". Two records of
      "has this structure's flatten changed" disagree the first time one of them is wrong, and the
      acceptance here is that the two machines agree sample for sample. One producer, two consumers:
      the flatten goes into the landscape and the chunks go into the mesh. It is the same bargain
      FogView::changedRows already strikes, and its header says so.
      AND THE LEVELLING IS IN Replica AND NOT IN Match, for the reason G1b found before it:
      OutpostCommander is an Application, so nothing in it can be linked into a test DLL, and the
      property worth testing here is a full-stack one. Replica/RenderViewBuilder.h's
      LevelUnderFlattens is what Match calls and what ConvergenceTests calls, so the code under test
      is the code that runs.
      THE ACCEPTANCE SAID "a chunk's vertex and index counts are fixed by its stride" AND THE INDEX
      COUNT IS NOT. The vertex count is: ChunkVertexCount(stride) exactly, measured at all four
      strides - 17,665 / 4,737 / 1,345 / 417. But BuildTerrainChunk SKIPS a quad whose four corners
      all lie at or below the water (SpeciesTerrain.md §6's underwater plain), so a flatten that
      lifts ground out of the sea, or drops it in, changes how many indices a chunk emits: the same
      chunk with half of it sunk meshes 52,992 indices against 101,376. So each level's index range
      is cut at MaxChunkIndexCount and the level's own indexCount says how much of it is drawn -
      which is a field DrawIndexedInstanced already reads, so the draw did not change. The padding
      costs about 8% of the index buffer on this landscape, because most quads are above water
      anyway, and it is what lets a rebuild go back into the range it came from with no offset
      moving.
      THE REBUILD IS SIMPLER THAN THIS TASK EXPECTED, because TerrainPass's buffers were already
      upload heaps the GPU reads in place - its own comment said "the copy to a default heap is a
      later task's, when a chunk rebuilds". So there is no upload heap and no CopyBufferRegion: a
      rebuild maps, writes over the range, and unmaps. What it DOES need is GraphicsDevice::
      WaitForIdle first, because a buffer a frame in flight is still reading is a race that shows as
      flickering geometry on somebody else's machine. A stall when a building goes up is cheaper
      than a second copy of a seven-megabyte buffer per frame in flight, and the header says so and
      says what to do instead if rebuilds ever become common.
      MEASURED END TO END ON A LINUX HARNESS, which this half of the executable allows: Match, Net,
      Replica, Sim, Content and Core have no Windows header in them (G1a's notes), so a probe stood
      up a real match over the loopback on the real Slice landscape. Over 400 client passes the view
      reported the flatten ONCE and the changed chunk ONCE - which is the acceptance line about a
      base of a dozen structures rebuilding nothing on the frame after it arrived, observed rather
      than argued - the commander's own command post left his ground levelled to 84 world units
      under its whole footprint, and the opposite commander's base, which he has never scouted, left
      his ground exactly as the generator made it.
      THE COMMITTED TESTS WERE RUN BEFORE THEY WERE PUSHED, by compiling the test SOURCE against a
      stand-in for the Microsoft framework - Assert and Logger as free functions, TEST_CLASS as a
      struct - so the file that runs in CI is the file that was checked. Both new ConvergenceTests
      cases pass, and so do TheRenderViewHoldsWhatTheConvergedReplicaDoes and
      TheReplicaEqualsTheHostsInterestSetOverAWholeMatch beside them.
      MUTATION TESTING FOUND THE FIRST CASE HALF-TESTED, and the second case exists because of it.
      Of three ways to break this, two go red: levelling nothing at all (91 failures), and keeping
      FlattenDelta's recomputed mean instead of the height the wire carried. That second one
      SURVIVED the first case, because with one structure on pristine ground the client's mean and
      the host's are the same number - which is why the defect could ship. The second case builds
      the situation where they differ: two structures whose flatten WINDOWS overlap (a window is the
      footprint's samples plus the boundary sample on the far side, so two footprints side by side
      share a column), the host levels the hidden one first so the second's mean is over ground
      already flattened, and the commander can see only the second. The host levels to 36 and the
      client's own ground would have said 5. It needed a hill put under the hidden one on purpose:
      the fixture's ground is nearly flat there and on flat ground the two answers agree, which is
      the whole reason this was invisible.
      ONE MUTATION SURVIVES AND IS LEFT SURVIVING, recorded rather than papered over. Spelling the
      height recovery as SubunitsFromWire instead of the exact divide changes nothing any test can
      see: the half-bucket it adds is an eighth of a world unit and the divide to whole world units
      floors it away, because the host levels to a whole world unit. The comment in the code said
      that spelling would be WRONG; it is not, it is merely less direct, and the comment now says
      what is actually true.
      NOT DONE HERE: the palette's height axis. HeightView::highest is kept as it was when the pass
      was built rather than re-derived on a rebuild, deliberately - it is the axis the terrain
      colours are looked up on, so re-deriving it would recolour the whole landscape the moment a
      building levelled the tallest ground anybody had seen. If a landscape ever gets a flatten that
      changes the highest sample, the chunk that changed will be coloured against the old axis and
      every other chunk will agree with it, which is the better of the two wrongs.

  - id: S13
    title: The viewer's eye, and the builder deadlock it uncovers
    intent: >
      A viewer's line of sight runs from its own ground height, and Sim/Landscape.h's CellHeight is
      a cell's TALLEST sample, so on any ground with relief every swell between two cells occludes.
      Measured over six positions of GameData/Landscapes/Slice.json, a structure with a 12-cell
      sight radius sees 27.7% of its own disc: the fog that reaches the screen is moth-eaten, with
      holes through ground the commander is standing next to, and the renderer draws it faithfully -
      which is what the owner saw and reported on 2026-09-19. The owner ruled the same day that the
      viewer looks out from 16 world units above its own ground, a quarter of a cell and half the
      32 GameDesign.md §8 already uses as its height step, which takes the same six positions to
      82.2%.
      THAT ONE-CONSTANT CHANGE BREAKS THE SCRIPTED COMMANDER, WHICH IS WHY THIS IS A TASK AND NOT A
      LINE. Measured: with the eye raised and nothing else changed, AiTests' acceptance match fails
      on every one of eight seeds - seat 0 never builds a factory and seat 1 never builds a
      generator - where the tree as it stands passes all eight. The mechanism is understood and is
      a defect of S12 rather than of the sight rule.
    project: Sim
    depends_on: [S9, S12]
    files:
      - Sim/Visibility.h
      - Sim/Visibility.cpp
      - Sim/AiSeat.cpp
      - Sim/Placement.h
      - Sim/Placement.cpp
      - Sim/Construction.cpp
      - Tests/SimTests/VisibilityTests.cpp
      - Tests/SimTests/AiTests.cpp
    acceptance:
      - "A viewer's line of sight leaves from VIEWER_EYE_WORLD_UNITS above its own ground and reaches the TARGET cell at ground level, because what has to be seen is the ground a target stands on and not the top of the target; the constant is 16 and is stated in Sim/Visibility.h with the measurement that chose it"
      - "A test asserts the fraction of its own disc a viewer sees over the slice landscape, so that a future change to CellHeight or to the sight radius cannot quietly take the fog back to moth-eaten without a number moving"
      - "AiSeat's Finish behaviour never orders a builder onto a site's own footprint: a structure occupies its cells from the tick construction begins (Sim/Plan.h's Occupies), the planner refuses a destination the drive cannot stand on, the device falls back to Stop, and the behaviour orders it there again on the next decision - for the rest of the match, starving the generator, the factory and the lab that answer after it. The destination is a cell BESIDE the footprint, within the builder module's reach of it, chosen deterministically"
      - "A site no builder can be got to is passed over rather than waited on, so that the slack roomToBuild already carries for 'a deposit on a summit no drive can climb' is reachable; today Finish answers first and that slack can never be used"
      - "AiTests' acceptance match passes with the eye raised, on the plan's seed and on two others, CUT FROM EIGHT BY THE OWNER ON 2026-09-19 once the cost was measured against what it bought, and the multi-seed run is a test rather than a probe"
      - "Stage 5's reach rule and the AI's reading of it are one function: DistanceSquaredTo(footprint, x, z) moves to Sim/Placement.h and both call it, so a builder the AI sends is a builder stage 5 counts"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      WHAT IS ALREADY MEASURED, so that the next reader starts from the evidence and not from the
      code. The eye height alone: 0 of 8 seeds meet S12's acceptance, against 8 of 8 on the tree as
      it stands. Do not read that as "the AI is seed-fragile" - the landscape is fixed and the seed
      turns out to perturb the opening very little, so the eight runs are nearly the same match; it
      says the sight change breaks it deterministically, and says nothing either way about
      robustness to the seed. A Finish that walks the builder to a cell beside the footprint moves
      the failure from "no factory" to "no lab" rather than curing it, and the remaining stall is a
      builder that cannot get back to a lab its own base has since been built around. That last one
      may be a third defect - placement that seals a gap - and this task should measure it before
      deciding whether it is in scope.
      THE DEADLOCK IS A CLASS AND NOT AN INSTANCE. Every behaviour in AiSeat that issues a Move and
      returns true starves everything below it when the Move cannot succeed: Finish has it and
      Scout has the same shape. Whatever guard this task settles on belongs in one place that both
      call, not copied into each.
      DO NOT GUARD IT WITH COMPONENT EQUALITY. ClusterGraph::ComponentAt returns a component that is
      local to a 16x16 cluster, so two cells a truck can drive between are in different components
      as soon as they are in different clusters; using equality as a reachability test refuses
      almost every real site. Reachability over the graph is a planner question and the planner
      answers it over several ticks, which one decision cannot wait for.

      DONE 2026-09-19, AND THE THIRD DEFECT WAS REAL BUT NOT WHAT THIS NOTE GUESSED. The guess was
      "a builder that cannot get back to a lab its own base has since been built around ... placement
      that seals a gap". Measured, it is nothing to do with reachability: after the Finish fix, seat
      0 ends the match with the lab still a PLAN at cell (38,90), CheckPlacement on it returning
      Occupied, and its two builders standing at cell (37,89) - diagonally touching its corner, 0.71
      of a cell away, inside a two-cell builder's reach, with 1,890 power banked and nothing to do.
      They got there perfectly well. What stopped the lab was that a plan does not occupy its cells
      (Sim/Plan.h), so two plans may overlap; the first to BEGIN construction flattens and marks its
      footprint, and Sim/Construction.cpp then refuses the other every tick for ever - which that
      file states outright as the rule ("a plan that can no longer be built stays a plan until it is
      cancelled") and which is right for a human, who can see it and press the button. Nothing was
      pressing it for a scripted commander. And AiBlackboard counts what a seat holds "built or
      building", so the dead plan still counted as the lab it was going to be and INDUSTRY never
      placed another. So the third defect is the AI's bookkeeping, not the pathfinder, and the fix
      is a behaviour - ABANDON, first in the list - that cancels a plan whose CheckPlacement no
      longer passes, asking exactly the question stage 5 asks before it begins one.
      THE THREE DEFECTS AND THE MEASUREMENTS, in the order they were found, each one measured on the
      slice landscape with the real tables before and after. Baseline, tree as it stood: 8 of 8 seeds
      pass, a 12-cell viewer sees 722 of the 2,646 cells of its own disc over six positions (27.3%).
      The eye alone at 16 world units: disc goes to 2,359 of 2,646 (89.2%), seeds go to 0 of 8, both
      commanders stalling with no factory and one device each. Plus the Finish fix (never onto the
      footprint, every site rather than the oldest): still 0 of 8, but the failure moves to "no lab"
      with seat 0 on 2 extractors, 2 generators, a factory and 30 devices - which is the plan's own
      prediction, and the point at which the third defect became visible. Plus Abandon: 16 of 16
      seeds pass. The figures this note quoted before the work - 27.7% and 82.2% over "six
      positions" - are within a rounding of these; the six positions here are quarters and halves of
      the side, written into VisibilityTests as a rule rather than a list, so the numbers are
      reproducible rather than remembered.
      MUTATION TESTED, THREE FOR THREE, and each mutation is killed by exactly one case. Send the
      builder to the footprint's own origin cell, as Finish used to: the never-onto-a-footprint case
      reports 403 violations of 414 Move orders, against 23 Move orders and 0 violations on the tree
      as it stands - and that ratio is the deadlock itself, an AI re-issuing the same refused order
      every decision. Let Finish see only unfinished.front() again: the passed-over case fails, and
      nothing else does. Take Abandon out: the cancelled-plan case fails, and the match goes back to
      0 of 2 seeds. No case is redundant and none of them passes for a reason other than its own.
      WHAT THE EIGHT-SEED RUN COSTS, so that keeping it is a decision. 11.5 seconds at -O2 on the
      session's Linux harness. The one existing 12,000-tick acceptance run takes 28.7 seconds in CI
      Debug against 2.3 seconds here, which is 12.5x, so the eight are about 143 seconds on the
      Windows runner - roughly 45% on top of a test step that took 321 seconds in run 130. The loop
      stops the moment a match has met every condition, which is exactly equivalent to the assertion
      (it is asserted WITHIN 12,000 ticks) and is what keeps it to that: every seed meets it at tick
      8,047. AND THAT NUMBER IS THE ARGUMENT AGAINST THE SPREAD: not "about 8,000" on each seed but
      8,047 on every one of them, with the second commander's base identical across sixteen seeds
      and only the first commander's device count moving, 16 to 20. These are nearly the same match
      eight times. The owner may well want it cut to two or three seeds; it is written as asked, and
      the cost is recorded here rather than discovered on the next red-eyed CI run.
      ONE EXISTING ASSERTION WAS CHECKED RATHER THAN ASSUMED, because the eye could have flipped it.
      VisibilityTests' ARidgeBetweenTheViewerAndTheTargetBlocks raises a wall to 400 world units over
      ground the flat fixture generates at -26, so the eye at -10 does not clear it and both of its
      assertions still hold - confirmed on the harness, not reasoned about. The height BONUS is
      untouched by any of this: StampDisc computes it from CellHeight directly and never from the
      sight line, so AHillExtendsTheRadiusByACellForEveryThirtyTwoUnitsOfHeight keeps the radius it
      asserts, including its negative case.
      WHAT IS NOT DONE HERE AND IS NOT HIDDEN. The AI can still place two plans that overlap - the
      placement rule permits it, deliberately, since a plan occupies nothing - so Abandon is a cure
      for the symptom and the commander still wastes a decision placing a site it will cancel. A
      placement rule that refused a footprint overlapping the seat's own pending plan would be the
      cure for the cause, and it is a GAME rule affecting human commanders too (GameDesign.md §5),
      so it is the owner's to take and not this task's. Recorded for M2's planner, which is where the
      placement heuristics are due to be rewritten anyway.
      THE SWEEP IS THREE SEEDS AND NOT NINE (owner, 2026-09-19). Written as nine, measured at about
      143 seconds on the Windows runner - roughly 45% on top of run 130's 321-second test step - and
      then cut, because the evidence says it buys very little: every seed meets the acceptance on
      tick 8,047 EXACTLY, and the second commander's base is identical across sixteen. Three keeps
      the guard against a fix that happened to suit one opening at a third of the cost. What
      actually caught the regressions is cheap and stays: the never-onto-a-footprint invariant over
      4,000 ticks, the passed-over site, and the cancelled plan.

  - id: K7
    title: The ground cursor - a ring that lies on the landscape and tilts with it
    intent: >
      The owner asked on 2026-09-19 for Species's cursor: not a quad at the pointer but a ring drawn
      ON the ground, tilted to the slope, so that pointing at a hillside points at the hillside.
      Design/Interface.md §4 specifies it and §12 ruling 3 makes it the cursor of aim mode, where
      there is no pointer and the ring sits at the centre of the screen. It is the one part of that
      ruling that is drawing rather than input, and it needs something the tree does not have: an
      answer to where a ray meets the landscape, and to which way the ground faces there
      (Design/Interface.md §11 row 14).
    project: Client
    depends_on: [K1, K6]
    files:
      - Core/HeightView.h
      - Client/GroundRay.h
      - Client/GroundRay.cpp
      - Client/CursorPass.h
      - Client/CursorPass.cpp
      - Client/Client.vcxproj
      - Client/Client.vcxproj.filters
      - Tests/ClientTests/GroundRayTests.cpp
      - Tests/ClientTests/ClientTests.vcxproj
      - Tests/ClientTests/ClientTests.vcxproj.filters
      - Replica/OrderInput.cpp
      - Tests/ReplicaTests/OrderInputTests.cpp
      - GameData/Textures/GroundRing.dds
      - GameData/Textures/GroundRingBlur.dds
      - Design/Interface.md
    acceptance:
      - "A ray against the heightfield: clip the ray to the landscape rectangle, march the sample grid cell by cell, and test the two triangles of each cell in the same split the terrain mesh uses, so that the cursor lands on the triangle the commander can see and not on the other diagonal; the nearest hit, and a clean miss when the ray leaves the landscape"
      - "A normal at a world point, bilinear over the four surrounding samples from central differences, with the DEGENERATE CASE branched explicitly: flat ground gives a normal of exactly (0,1,0), its cross product with world up is zero, XMVector3Normalize of a zero vector is zero, and the quad collapses to a point - which is a live fault in Species today (SpeciesLook.md §7.1) and would show on most of M1's landscape"
      - "Off the landscape the cursor is HIDDEN and the last position is not reused; Species falls back to a sphere thousands of units out and gets away with it because terrain fills its view, and M1 draws a horizon"
      - "Over water the hit's height is raised to at least one world unit, so the ring floats on the sea rather than sinking to the seabed the mesh carries on into"
      - "A pass that draws it: one four-vertex quad on the basis up = the normal, front = normalize(up x worldUp), right = normalize(front x up), sized 30 world units times sqrt(the camera's distance) / 40; depth tested and not written, no culling, and a slope-scaled depth bias toward the camera because it is coplanar with the ground - Species nudges its near plane out 5% for the cursor pass and that is a GL trick with no clean D3D12 equivalent"
      - "The blurred pass then the sharp one, at SRC_ALPHA/INV_SRC_COLOR and SRC_ALPHA/ONE, which is what makes a bright ring readable over bright sand; the six meanings of §4 choose the tint rather than a different bitmap"
      - "The ring's own texture and its pre-blurred twin ship with it, through C4's importer and at C4's provenance rules, because they are not cells of Icons.dds and C4 is done - Design/Interface.md §11 row 15, and its heading is the reason the row does not name C4"
      - "Replica/OrderInput.cpp's GroundPoint uses the same ray, so that the point a right click orders and the point the ring is drawn at are one point - today it intersects a flat plane at y = 0 and over ground 200 units up at the default 26.6 degree pitch that is 400 world units out, six cells"
      - "Tests on the CPU with no device: the ray against a synthetic heightfield with a known hill, including a grazing ray, a ray that leaves the landscape, and a ray down a valley whose first CELL hit is not its nearest TRIANGLE hit; the normal at a flat cell, at a slope of known gradient, and at the edge; and the basis on flat ground, which is the degenerate case"
    verify:
      - python3 Tools/CheckTaskDag.py
      - python3 Build/CheckFormat.py --clang-format clang-format-18
      - python3 Build/CheckProjectFiles.py
      - msbuild OutpostCommander.slnx /p:Configuration=Debug /p:Platform=x64 /m /warnaserror
      - vstest.console.exe x64\Debug\*Tests.dll /Platform:x64
    parallel_safe: true
    status: done
    notes: >
      IT DEPENDS ON K6 AND THE REASON IS THE POINT OF K6. A cursor that lies on the client's
      landscape is only in the right place if the client's landscape is the right shape, and until
      K6 lands the client's ground is unflattened under every structure - so the ring would float
      above a levelled base, or sink into it, by exactly the flatten K6 applies.
      THE RAY IS NOT Replica/Picking.h's. That one is ray-versus-sphere over the render view's
      objects and never touches the ground; this one is ray-versus-heightfield. They meet in App,
      which casts one ray and asks both: Picking answers what is under it, GroundRay answers where
      the ground under it is. Neither grows a dependency on the other, and GroundRay sits in Client
      beside the terrain mesh whose triangle split it has to match.
      MATCH THE MESH'S DIAGONAL OR THE CURSOR WILL LIE. Client/TerrainChunk.cpp decides how a cell
      is split into two triangles, and a ray test that splits it the other way answers with a
      surface that is not the one on the screen - by up to the cell's relief, which on this
      landscape is tens of units. The test for it is the third ray case above, and it is written
      against TerrainChunk's split rather than against a guess.

      THE ARITHMETIC LANDED AND THE DRAW CALL HAS NOT. Client/GroundRay.h answers the two questions
      Design/Interface.md §11 row 14 says the tree does not have - where a ray meets the landscape
      and which way the ground faces there - and it names no Direct3D and no DirectXMath, so it is
      a unit test on any machine rather than something only the capture can show. CursorPass, the
      two textures and the OrderInput change are what is left, and those need a device.
      THE MARCH IS AGAINST THE DRAWN DIAGONAL, which is the whole reason it is a march. A cell is
      two triangles and there are two ways to split it; on a saddle the two splits differ by most
      of a cell's height down the middle, so a cursor tested against the other diagonal floats over
      the ground the commander is looking at. The split is Client/TerrainChunk.cpp's own, and a
      case pins it against a saddle where the wrong diagonal would be a hundred units out.
      FLAT GROUND GIVES EXACTLY (0, 1, 0), bit for bit, which is what lets the pass BRANCH on the
      degenerate case rather than normalise a zero vector - the live fault SpeciesLook.md §7.1
      records. Both differences are zero, the vector is already unit length, and four of them mix
      to the same thing.
      ONE DEVIATION FROM THE ACCEPTANCE, DELIBERATE AND FOR THE OWNER TO RULE ON. The line says
      "over water the hit's height is raised to at least one world unit". Raising the height fixes
      the sinking and leaves the ring in the WRONG PLACE: the seabed is further along the ray than
      the surface the commander is looking at, so a slanted ray puts the ring a good way past where
      he is pointing - measured at 108 world units against 8 on the test's own geometry. So the
      water's own PLANE is intersected instead, and it wins when it is nearer than the ground and
      the ground under that crossing is below it. The ring floats on the sea, which is what the
      line is for, and it floats where he is pointing.
      FIFTEEN CASES, SEVEN MUTATIONS KILLED: the other diagonal, the farther triangle of a cell
      rather than the nearer, an unnormalised direction (which would scale the distance the ring is
      sized by), no water answer at all, a sea that wins over ground standing out of it, and a hit
      that answers straight up instead of the ground's normal. ONE SURVIVES AND IS NOT A DEFECT:
      removing the early return when the ray never enters the landscape changes nothing observable,
      because a ray outside the rectangle meets none of its triangles either - it is an early-out.
      What the clip is NOT optional for is where the march starts, and a case from off the edge
      pins that.

      THE DRAW CALL LANDED ON 2026-09-20, and with it the two textures and the one ray.
      THE TEXTURES ARE SPECIES'S OWN AND ITS OWN BLUR. Icons/MouseHighlight.bmp is the disc Species
      draws on the terrain under the pointer (GameCursor.cpp), and the blurred twin is not authored
      anywhere: Species makes one at LOAD TIME with ApplyBlurFilter(10.0f) and draws it first. This
      tree has no load-time blur and no reason for one - the input never changes - so
      Tools/ImportTextures.py ports the filter and writes both files, which is C4's importer at
      C4's provenance rules as the acceptance asks. THE FILTER IS NOT NORMALISED AND THAT IS THE
      POINT: its five weights are scaled so that the horizontal pass sums to one and the vertical
      one to TWO, so the result is twice as bright as the source before it saturates, which is what
      turns a thin ring into a halo wide enough to darken sand under it. A blur that conserved
      energy would be a smudge nobody could see. Measured on the two files: the sharp ring covers
      25.0% of its 128 by 128 and the blurred one 35.8%, at a mean alpha of 54.8 against 86.8.
      CursorPass IS TWO PIPELINES AND ONE ROOT SIGNATURE, because the blend mode lives in the
      pipeline state: SRC_ALPHA/INV_SRC_COLOR for the dark pass and SRC_ALPHA/ONE for the bright
      one (§4's table). One descriptor table of one SRV chooses WHICH ring each draw reads, so the
      shader has no branch and no index to get wrong. Depth tested, never written, no culling -
      the quad is oriented by the ground and a hillside can turn it away - and biased toward the
      camera because it is coplanar with what it lies on. THE BIAS IS POSITIVE, which is the
      reversed depth of ADR-005 and the opposite of every example written for the ordinary mapping:
      the near plane is 1 here, so "toward the camera" is a LARGER depth value.
      THE DEGENERATE BASIS IS BRANCHED IN THE PASS, where the acceptance asks for it: flat ground
      gives a normal of exactly (0, 1, 0), its cross with world up is the zero vector, and the
      fallback front of world +Z is NAMED rather than reached by normalising zero - which is the
      live fault in Species this ring is taken from.
      AND CI DRAWS IT, which took one more decision. The capture is an observer with no pointer and
      is never in aim mode, so the pass would have been compiled by CI and never RUN by it - and
      AGENTS.md §3 says a green build says nothing about whether the game draws. The capture now
      casts the ray through the middle of its own frame, exactly as aim mode does, and draws the
      ring there: every run puts the pipeline, both blends and both textures under the debug layer,
      every frame carries "ring on the ground", "ring on the sea" or "ring hidden" in the log, and
      the ring marks where the camera was pointed - which on the following vantages is the fight.
      THE ONE RAY IS THE EIGHTH ACCEPTANCE LINE AND IT WAS A REAL DIVERGENCE. OrderInput's
      GroundPoint intersects the plane y = 0, because Replica may not include Client (ADR-001 makes
      them siblings) and the heightfield ray is Client's. Over ground 200 world units up at the
      camera's default pitch of 26.6 degrees that plane is 400 units further along the ray - SIX
      CELLS - so the point a right click ordered and the point the ring is drawn at were different
      points. The executable holds both libraries, so Operator casts ONE ray against the heightfield
      and hands the answer to the orders and to the ring; OrderFrame carries it, and the plane
      remains the answer for a frame that has no landscape yet. Three cases.
      MEASURED, headless, with the ray cast through the middle of the slice landscape: it hits at
      (4098, 1, 4092) with a normal of exactly (0, 1, 0) and water true - the middle of this
      landscape is sea, and the water deviation above is what puts the ring at y = 1 rather than on
      the seabed under it.
      WHAT CI HAS STILL TO PROVE: that the two shaders compile, that WARP draws the ring where the
      log says it is, and that the depth bias is enough - the sign is reasoned from ADR-005 and the
      magnitude is not measured against anything. A ring that z-fights with the ground in the
      frames is that number and nothing else.

